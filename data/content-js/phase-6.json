{
  "6.1.1": {
    "body": "# Essential Linux Commands for Developers\n\n## Why Linux Matters for Backend Developers\n\nAs a backend developer, you will spend a significant portion of your career working with Linux servers. Whether you are deploying applications, debugging production issues, or managing infrastructure, Linux command-line proficiency is non-negotiable. Most cloud servers, Docker containers, and CI/CD environments run Linux.\n\n## The Linux File System Hierarchy\n\n```\n/\n├── bin/        # Essential user binaries (ls, cp, mv)\n├── etc/        # Configuration files\n├── home/       # User home directories\n├── var/        # Variable data (logs, databases)\n│   ├── log/    # System and application logs\n│   └── www/    # Web server files\n├── tmp/        # Temporary files\n├── usr/        # User programs and data\n│   ├── bin/    # Most user commands\n│   ├── lib/    # Libraries\n│   └── local/  # Locally installed software\n├── opt/        # Optional/third-party software\n└── proc/       # Virtual filesystem for process info\n```\n\nUnderstanding this hierarchy helps you know where to find configuration files (`/etc`), logs (`/var/log`), and installed software (`/usr/local`).\n\n## Navigation and File Operations\n\n### Moving Around\n\n```bash\n# Print current directory\npwd\n\n# Change directory\ncd /var/log\ncd ~           # Go to home directory\ncd -           # Go to previous directory\ncd ..          # Go up one level\n\n# List files\nls             # Basic listing\nls -la         # Long format, show hidden files\nls -lah        # Human-readable file sizes\nls -lt         # Sort by modification time\n```\n\n### File and Directory Operations\n\n```bash\n# Create directories\nmkdir myproject\nmkdir -p src/controllers/api    # Create nested directories\n\n# Create files\ntouch server.js\necho \"Hello\" > greeting.txt     # Create with content\n\n# Copy files and directories\ncp source.txt dest.txt\ncp -r src/ backup/              # Copy directory recursively\n\n# Move and rename\nmv old-name.js new-name.js\nmv file.txt /var/data/\n\n# Remove files and directories\nrm file.txt\nrm -r directory/                # Remove directory recursively\nrm -rf node_modules/            # Force remove (use with caution!)\n```\n\n## Viewing and Searching File Content\n\n### Reading Files\n\n```bash\n# Display entire file\ncat server.js\n\n# Display with line numbers\ncat -n server.js\n\n# View large files page by page\nless /var/log/syslog\n# In less: / to search, n for next match, q to quit\n\n# View first/last lines\nhead -n 20 server.js            # First 20 lines\ntail -n 50 /var/log/app.log     # Last 50 lines\ntail -f /var/log/app.log        # Follow log in real-time (crucial for debugging)\n```\n\n### Searching with grep\n\n```bash\n# Search for pattern in file\ngrep \"error\" /var/log/app.log\n\n# Case-insensitive search\ngrep -i \"error\" /var/log/app.log\n\n# Recursive search in directory\ngrep -r \"TODO\" src/\n\n# Show line numbers\ngrep -n \"function\" server.js\n\n# Show context (lines before/after match)\ngrep -B 3 -A 3 \"Error\" app.log\n\n# Invert match (show lines NOT matching)\ngrep -v \"DEBUG\" app.log\n\n# Count matches\ngrep -c \"error\" app.log\n\n# Search with regex\ngrep -E \"error|warning|critical\" app.log\n```\n\n### Finding Files with find\n\n```bash\n# Find by name\nfind /var/log -name \"*.log\"\n\n# Find by type (f=file, d=directory)\nfind . -type f -name \"*.js\"\n\n# Find recently modified files (last 24 hours)\nfind . -type f -mtime -1\n\n# Find large files (over 100MB)\nfind / -type f -size +100M\n\n# Find and execute command\nfind . -name \"*.tmp\" -exec rm {} \\;\n\n# Find and delete (simpler syntax)\nfind . -name \"node_modules\" -type d -prune -exec rm -rf {} +\n```\n\n## Text Processing\n\n### sed - Stream Editor\n\n```bash\n# Replace first occurrence on each line\nsed 's/old/new/' file.txt\n\n# Replace all occurrences\nsed 's/old/new/g' file.txt\n\n# Edit file in place\nsed -i 's/localhost/0.0.0.0/g' config.js\n\n# Delete lines matching pattern\nsed '/^#/d' config.txt          # Remove comment lines\n\n# Replace in specific line range\nsed '10,20s/foo/bar/g' file.txt\n```\n\n### awk - Pattern Processing\n\n```bash\n# Print specific columns\nawk '{print $1, $3}' access.log\n\n# Filter by condition\nawk '$9 == 500' access.log      # Lines where 9th field is 500\n\n# Sum a column\nawk '{sum += $5} END {print sum}' data.txt\n\n# Custom field separator\nawk -F',' '{print $2}' data.csv\n```\n\n## Pipes and Redirection\n\nPipes are the backbone of Linux command composition.\n\n```\nCommand A ──|pipe|──> Command B ──|pipe|──> Command C\n  stdout          stdin/stdout          stdin\n```\n\n```bash\n# Pipe output of one command to another\ncat access.log | grep \"POST\" | wc -l\n\n# Count unique IP addresses\ncat access.log | awk '{print $1}' | sort | uniq -c | sort -rn | head -10\n\n# Redirection\necho \"log entry\" > file.txt     # Overwrite\necho \"another entry\" >> file.txt # Append\ncommand 2> errors.txt            # Redirect stderr\ncommand > output.txt 2>&1        # Redirect both stdout and stderr\ncommand &> all.txt               # Same as above (bash shortcut)\n```\n\n## Process Management\n\n```bash\n# List running processes\nps aux                          # All processes\nps aux | grep node              # Filter for Node.js\n\n# Interactive process viewer\ntop                             # Basic\nhtop                            # Better (if installed)\n\n# Find process by port\nlsof -i :3000                   # What is using port 3000?\nnetstat -tlnp                   # All listening ports\nss -tlnp                        # Modern alternative to netstat\n\n# Kill processes\nkill PID                        # Graceful (SIGTERM)\nkill -9 PID                     # Force kill (SIGKILL)\nkillall node                    # Kill all node processes\n\n# Run in background\nnode server.js &                # Run in background\nnohup node server.js &          # Survives terminal close\n```\n\n## Disk and Memory\n\n```bash\n# Disk usage\ndf -h                           # Filesystem usage (human-readable)\ndu -sh /var/log                 # Size of directory\ndu -sh * | sort -h              # Size of each item, sorted\n\n# Memory usage\nfree -h                         # RAM usage\n```\n\n## Networking Commands\n\n```bash\n# Test connectivity\nping google.com\ncurl -I https://api.example.com  # HTTP headers only\ncurl -X POST -H \"Content-Type: application/json\" \\\n  -d '{\"key\":\"value\"}' http://localhost:3000/api\n\n# DNS lookup\nnslookup example.com\ndig example.com\n\n# Download files\nwget https://example.com/file.tar.gz\ncurl -O https://example.com/file.tar.gz\n\n# Check open ports\nnc -zv localhost 5432            # Check if PostgreSQL port is open\n```\n\n## Environment Variables\n\n```bash\n# View all environment variables\nenv\nprintenv\n\n# Set variable (current session)\nexport NODE_ENV=production\nexport PORT=3000\n\n# Use in commands\necho $NODE_ENV\n\n# Set for a single command\nNODE_ENV=test npm test\n\n# Persist in shell config\necho 'export NODE_ENV=production' >> ~/.bashrc\nsource ~/.bashrc                 # Reload config\n```\n\n## Archiving and Compression\n\n```bash\n# Create tar archive\ntar -czf backup.tar.gz ./myproject/\n\n# Extract tar archive\ntar -xzf backup.tar.gz\n\n# List contents without extracting\ntar -tzf backup.tar.gz\n\n# Flags: c=create, x=extract, z=gzip, f=file, v=verbose\n```\n\n## Practical Scenarios\n\n### Analyzing Node.js Application Logs\n\n```bash\n# Find all errors in the last hour\nfind /var/log/myapp -name \"*.log\" -mmin -60 -exec grep -l \"ERROR\" {} \\;\n\n# Count errors by type\ngrep \"ERROR\" app.log | awk -F'ERROR: ' '{print $2}' | sort | uniq -c | sort -rn\n\n# Watch logs in real-time, filtering for errors\ntail -f /var/log/myapp/app.log | grep --line-buffered \"ERROR\\|FATAL\"\n\n# Find which endpoint is getting the most 500 errors\ngrep \"500\" access.log | awk '{print $7}' | sort | uniq -c | sort -rn | head\n```\n\n### Server Health Check Script\n\n```bash\n#!/bin/bash\necho \"=== Server Health Check ===\"\necho \"Uptime: $(uptime)\"\necho \"Disk Usage:\"\ndf -h / | tail -1\necho \"Memory:\"\nfree -h | grep Mem\necho \"Node.js Processes:\"\nps aux | grep node | grep -v grep | wc -l\necho \"Port 3000:\"\nss -tlnp | grep 3000 || echo \"Not listening\"\n```\n\n## Common Pitfalls\n\n1. **Running `rm -rf /` or `rm -rf *` in wrong directory** - Always double-check `pwd` before destructive commands\n2. **Forgetting quotes around variables** - `rm $FILE` is dangerous if FILE contains spaces; use `rm \"$FILE\"`\n3. **Using `kill -9` as first resort** - Always try `kill` (SIGTERM) first to allow graceful shutdown\n4. **Not using `tail -f` for debugging** - Many developers `cat` entire log files instead of following them\n5. **Ignoring exit codes** - Use `$?` to check if the last command succeeded (0 = success)\n",
    "intuition": "Linux commands are like a Swiss Army knife for servers. Each tool does one thing well, and by combining them with pipes (like snapping tools together), you can accomplish complex tasks that would take hundreds of lines of code in a programming language.",
    "exercises": [
      {
        "title": "Log Analysis Challenge",
        "description": "Create a sample access log file with 100+ lines containing various HTTP status codes (200, 301, 404, 500). Write a one-liner pipeline that outputs the top 5 most frequent status codes with their counts, sorted in descending order.",
        "hint": "Use awk to extract the status code column, pipe to sort, then uniq -c, then sort -rn, then head -5."
      },
      {
        "title": "Find and Clean Build Artifacts",
        "description": "Write a command that finds all node_modules directories and .next directories under your home folder, prints their sizes, and calculates the total space used. Do NOT delete them yet - just report.",
        "hint": "Use find with -type d -name 'node_modules', pipe to xargs du -sh, and use awk to sum sizes."
      },
      {
        "title": "Process Detective",
        "description": "Write a shell script that takes a port number as an argument and reports: (1) whether anything is listening on that port, (2) the PID and process name, (3) how much memory that process is using.",
        "hint": "Use lsof -i :$PORT or ss -tlnp to find the process, then ps aux with the PID to get memory info."
      },
      {
        "title": "Environment Setup Script",
        "description": "Write a bash script that checks if Node.js, npm, Docker, and Git are installed, prints their versions, and warns if any are missing. The script should exit with code 1 if any required tool is missing.",
        "hint": "Use 'command -v' or 'which' to check if a command exists. Use $? to check exit codes."
      }
    ],
    "problems": [
      {
        "problem": "Permission denied when running a script",
        "cause": "The script file does not have execute permissions.",
        "solution": "Add execute permission with `chmod +x script.sh`. For directories, ensure you have execute permission to enter them (`chmod +x directory/`). Check file ownership with `ls -la`.",
        "code": "chmod +x deploy.sh && ./deploy.sh"
      },
      {
        "problem": "Command not found even though the program is installed",
        "cause": "The program's binary is not in your PATH environment variable.",
        "solution": "Find the binary location with `which program` or `find / -name program 2>/dev/null`. Add its directory to PATH: `export PATH=$PATH:/path/to/bin`. Make it permanent by adding to ~/.bashrc or ~/.zshrc.",
        "symptoms": "bash: node: command not found"
      },
      {
        "problem": "Disk space full on production server",
        "cause": "Log files, Docker images, or temporary files consuming all available space.",
        "solution": "Identify large files/directories with `du -sh /* | sort -h`. Common culprits: `/var/log` (rotate logs), Docker (`docker system prune`), `/tmp` (clean old files). Use `find /var/log -name '*.log' -mtime +30 -delete` to remove old logs."
      }
    ],
    "further_reading": [
      {
        "title": "The Linux Command Line (free book)",
        "url": "https://linuxcommand.org/tlcl.php",
        "type": "book",
        "note": "Comprehensive free book covering everything from basics to shell scripting"
      },
      {
        "title": "ExplainShell",
        "url": "https://explainshell.com/",
        "type": "tool",
        "note": "Paste any command to get a breakdown of what each part does"
      },
      {
        "title": "Linux Journey",
        "url": "https://linuxjourney.com/",
        "type": "tutorial"
      },
      {
        "title": "jlevy/the-art-of-command-line",
        "url": "https://github.com/jlevy/the-art-of-command-line",
        "type": "github"
      }
    ]
  },
  "6.1.2": {
    "body": "# SSH, Permissions, and File System\n\n## Introduction\n\nSSH (Secure Shell) is the primary way backend developers connect to remote servers. Understanding SSH, file permissions, and the Linux file system is essential for managing production servers, deploying applications, and securing your infrastructure.\n\n## SSH Fundamentals\n\n### How SSH Works\n\n```\n┌─────────────┐                          ┌─────────────┐\n│   Your Mac   │   Encrypted Tunnel       │  Server      │\n│   (client)   │ ◄══════════════════════► │  (sshd)      │\n│              │   Port 22 (default)       │              │\n│  ~/.ssh/     │                          │  ~/.ssh/      │\n│  id_ed25519  │                          │  authorized_  │\n│  (private)   │                          │  keys         │\n└─────────────┘                          └─────────────┘\n```\n\nSSH uses public-key cryptography. You generate a key pair: the private key stays on your machine (never share it), and the public key goes on every server you want to access.\n\n### Generating SSH Keys\n\n```bash\n# Generate a modern Ed25519 key (recommended)\nssh-keygen -t ed25519 -C \"your.email@example.com\"\n\n# Or RSA with 4096 bits (for older systems)\nssh-keygen -t rsa -b 4096 -C \"your.email@example.com\"\n\n# You'll be prompted for:\n# 1. File location (default: ~/.ssh/id_ed25519)\n# 2. Passphrase (strongly recommended)\n```\n\nThis creates two files:\n- `~/.ssh/id_ed25519` - Your private key (NEVER share this)\n- `~/.ssh/id_ed25519.pub` - Your public key (safe to share)\n\n### Connecting to Servers\n\n```bash\n# Basic connection\nssh username@server-ip\n\n# Specify port\nssh -p 2222 username@server-ip\n\n# Specify key file\nssh -i ~/.ssh/my-server-key username@server-ip\n\n# Execute a remote command without interactive session\nssh user@server \"pm2 status\"\n\n# Copy your public key to a server\nssh-copy-id username@server-ip\n# This adds your public key to the server's ~/.ssh/authorized_keys\n```\n\n### SSH Config File\n\nInstead of remembering IPs and usernames, use `~/.ssh/config`:\n\n```\n# ~/.ssh/config\n\nHost production\n    HostName 203.0.113.50\n    User deploy\n    Port 22\n    IdentityFile ~/.ssh/id_ed25519\n    ForwardAgent yes\n\nHost staging\n    HostName 203.0.113.51\n    User deploy\n    IdentityFile ~/.ssh/id_ed25519\n\nHost db-tunnel\n    HostName 203.0.113.50\n    User deploy\n    LocalForward 5433 localhost:5432\n\nHost *\n    ServerAliveInterval 60\n    ServerAliveCountMax 3\n    AddKeysToAgent yes\n```\n\nNow you can simply type:\n\n```bash\nssh production          # Instead of ssh deploy@203.0.113.50\nssh staging\nssh db-tunnel           # Opens a tunnel: local port 5433 -> remote port 5432\n```\n\n### SSH Tunneling (Port Forwarding)\n\nSSH tunnels are extremely useful for accessing databases and services behind firewalls.\n\n```\nLocal Port Forwarding:\n┌──────────┐      SSH Tunnel       ┌──────────┐      ┌──────────┐\n│  Local    │ ═══════════════════► │  Server   │ ───► │ Database │\n│  :5433    │                      │  (jump)   │      │  :5432   │\n└──────────┘                      └──────────┘      └──────────┘\n```\n\n```bash\n# Local port forwarding: access remote PostgreSQL locally\nssh -L 5433:localhost:5432 deploy@production\n# Now connect to localhost:5433 to reach the remote database\n\n# Access a remote Redis through a jump server\nssh -L 6380:redis-server:6379 deploy@jump-server\n\n# Dynamic SOCKS proxy (tunnel all traffic)\nssh -D 1080 deploy@production\n```\n\n### SCP and File Transfers\n\n```bash\n# Copy file to server\nscp ./deploy.sh production:/home/deploy/\n\n# Copy file from server\nscp production:/var/log/app.log ./\n\n# Copy directory recursively\nscp -r ./dist/ production:/var/www/app/\n\n# Use rsync for better transfers (resume, delta sync)\nrsync -avz --progress ./dist/ production:/var/www/app/\nrsync -avz --delete ./dist/ production:/var/www/app/  # Mirror exactly\n```\n\n## Linux File Permissions\n\n### Understanding Permission Notation\n\nEvery file and directory has three permission sets:\n\n```\n-rwxr-xr-- 1 deploy www-data 4096 Jan 15 10:30 server.js\n│├─┤├─┤├─┤   │      │\n│ │  │  │     │      └── Group\n│ │  │  │     └── Owner\n│ │  │  └── Others (everyone else)\n│ │  └── Group permissions\n│ └── Owner permissions\n└── File type (- = file, d = directory, l = symlink)\n\nr = read    (4)\nw = write   (2)\nx = execute (1)\n```\n\n### Numeric (Octal) Permissions\n\n```\nPermission    Octal    Meaning\nrwx           7        Read + Write + Execute\nrw-           6        Read + Write\nr-x           5        Read + Execute\nr--           4        Read only\n-wx           3        Write + Execute\n-w-           2        Write only\n--x           1        Execute only\n---           0        No permissions\n```\n\nCommon permission sets:\n\n```bash\nchmod 755 script.sh     # rwxr-xr-x  Owner: full, Others: read+execute\nchmod 644 config.json   # rw-r--r--  Owner: read+write, Others: read\nchmod 600 .env          # rw-------  Owner only (secrets!)\nchmod 700 .ssh/         # rwx------  Owner only (SSH directory)\nchmod 400 id_ed25519    # r--------  Read-only by owner (private key)\n```\n\n### Changing Permissions\n\n```bash\n# Numeric mode\nchmod 755 deploy.sh\n\n# Symbolic mode\nchmod u+x deploy.sh      # Add execute for user (owner)\nchmod g-w config.json     # Remove write for group\nchmod o-rwx .env          # Remove all for others\nchmod a+r README.md       # Add read for all\n\n# Recursive\nchmod -R 755 /var/www/app/\n```\n\n### File Ownership\n\n```bash\n# Change owner\nchown deploy server.js\nchown deploy:www-data server.js    # Change owner and group\n\n# Change recursively\nchown -R deploy:www-data /var/www/app/\n\n# Change group only\nchgrp www-data /var/www/app/\n```\n\n### Special Permissions\n\n```bash\n# Sticky bit - only owner can delete files in directory\nchmod +t /tmp\nchmod 1755 /shared-dir\n\n# SUID - run file as the file owner\nchmod u+s /usr/bin/program\nchmod 4755 /usr/bin/program\n\n# SGID - new files inherit directory's group\nchmod g+s /var/www/shared/\nchmod 2755 /var/www/shared/\n```\n\n## File System Deep Dive\n\n### Important Directories for Backend Devs\n\n```bash\n/etc/nginx/              # Nginx configuration\n/etc/systemd/system/     # systemd service files\n/etc/ssl/                # SSL certificates\n/var/log/                # Log files\n/var/log/nginx/          # Nginx logs\n/var/www/                # Web application files\n/opt/                    # Third-party applications\n/tmp/                    # Temporary files (cleared on reboot)\n```\n\n### Symlinks (Symbolic Links)\n\n```bash\n# Create a symbolic link\nln -s /var/www/app/current /var/www/app/releases/v2.1.0\n\n# This is how many deployment tools work:\n# /var/www/app/\n# ├── current -> releases/v2.1.0/    (symlink)\n# ├── releases/\n# │   ├── v2.0.0/\n# │   ├── v2.0.1/\n# │   └── v2.1.0/                    (latest)\n# └── shared/\n#     ├── .env\n#     └── uploads/\n\n# Remove symlink (not the target!)\nrm /var/www/app/current   # or unlink\n```\n\n### Inodes and Hard Links\n\n```bash\n# View inode numbers\nls -i file.txt\n\n# Check inode usage (running out of inodes is a real problem)\ndf -i\n\n# Hard link (same inode, cannot cross filesystems)\nln original.txt hardlink.txt\n```\n\n### /proc File System\n\nThe `/proc` virtual filesystem gives real-time process and system information:\n\n```bash\n# CPU information\ncat /proc/cpuinfo\n\n# Memory information\ncat /proc/meminfo\n\n# Process information\ncat /proc/1234/status    # Status of PID 1234\ncat /proc/1234/cmdline   # Command that started it\nls -la /proc/1234/fd/    # Open file descriptors\n\n# System limits\ncat /proc/sys/fs/file-max             # Max open files system-wide\ncat /proc/sys/net/core/somaxconn      # Max socket connections\n```\n\n## Practical Security Configuration\n\n### Securing SSH on a Server\n\nEdit `/etc/ssh/sshd_config`:\n\n```\n# Disable password authentication (use keys only)\nPasswordAuthentication no\nChallengeResponseAuthentication no\n\n# Disable root login\nPermitRootLogin no\n\n# Allow only specific users\nAllowUsers deploy\n\n# Change default port (optional, security through obscurity)\nPort 2222\n\n# Limit authentication attempts\nMaxAuthTries 3\n```\n\n```bash\n# Restart SSH after changes\nsudo systemctl restart sshd\n```\n\n### Setting Up a Deploy User\n\n```bash\n# Create a deploy user\nsudo adduser deploy\n\n# Add to sudo group (use sparingly)\nsudo usermod -aG sudo deploy\n\n# Set up SSH access\nsudo mkdir -p /home/deploy/.ssh\nsudo chmod 700 /home/deploy/.ssh\n# Add your public key\nsudo tee /home/deploy/.ssh/authorized_keys << EOF\nssh-ed25519 AAAAC3NzaC... your.email@example.com\nEOF\nsudo chmod 600 /home/deploy/.ssh/authorized_keys\nsudo chown -R deploy:deploy /home/deploy/.ssh\n```\n\n## Common Permission Patterns for Node.js Apps\n\n```bash\n# Application directory\nsudo chown -R deploy:deploy /var/www/myapp\nsudo chmod -R 755 /var/www/myapp\n\n# Environment file (contains secrets)\nchmod 600 /var/www/myapp/.env\n\n# Log directory\nsudo mkdir -p /var/log/myapp\nsudo chown deploy:deploy /var/log/myapp\nchmod 755 /var/log/myapp\n\n# Upload directory (writable by app)\nmkdir -p /var/www/myapp/uploads\nchmod 755 /var/www/myapp/uploads\n\n# SSL certificates\nsudo chmod 600 /etc/ssl/private/server.key\nsudo chmod 644 /etc/ssl/certs/server.crt\n```\n\n## Common Pitfalls\n\n1. **Leaving private keys with wrong permissions** - SSH refuses to use keys with permissions too open. Always `chmod 600` private keys.\n2. **Using `chmod 777` to \"fix\" permission issues** - This is a massive security risk. Diagnose the actual permission needed.\n3. **Forgetting to `chown` after creating files as root** - Files created with `sudo` are owned by root. Your app user cannot read them.\n4. **Not configuring SSH key passphrases** - If your laptop is stolen, an unprotected private key gives access to all your servers.\n5. **Port forwarding without understanding** - Make sure you are not exposing internal services publicly.\n",
    "intuition": "Think of SSH like a secure, invisible tunnel between your computer and a remote server. The key pair works like a physical lock and key: the public key is the lock you install on every door (server) you want to access, and the private key is the only key that opens those locks. Permissions are like a bouncer at a club checking three lists: what the owner can do, what the group can do, and what everyone else can do.",
    "exercises": [
      {
        "title": "SSH Config Setup",
        "description": "Create an SSH config file that defines three hosts: 'dev', 'staging', and 'production'. Each should use a different key file and user. Add a wildcard block that sets ServerAliveInterval to 60 seconds. Test that `ssh dev` would attempt the correct connection (you don't need a real server).",
        "hint": "Create ~/.ssh/config with Host blocks. Use 'ssh -v dev' to verify the config is being read correctly (it will fail to connect but show the parsed config)."
      },
      {
        "title": "Permission Audit Script",
        "description": "Write a bash script that audits a Node.js project directory and reports any security issues: files with 777 permissions, .env files readable by others, private keys with too-open permissions, and directories writable by others.",
        "hint": "Use find with -perm flags: find . -perm -o+w for world-writable, find . -name '.env' -perm -o+r for readable .env files."
      },
      {
        "title": "SSH Tunnel for Database Access",
        "description": "Document and practice the SSH tunnel command to forward local port 5433 to a remote PostgreSQL database on port 5432. Then connect using psql through the tunnel. Write a script that establishes the tunnel in the background, runs a query, and tears it down.",
        "hint": "Use ssh -f -N -L 5433:localhost:5432 user@host to create a background tunnel. Use $! to capture the PID for later cleanup."
      }
    ],
    "problems": [
      {
        "problem": "SSH connection refused or times out",
        "cause": "The SSH daemon is not running, a firewall is blocking port 22, or the server IP/port is wrong.",
        "solution": "Check if sshd is running: `sudo systemctl status sshd`. Check firewall: `sudo ufw status` or `sudo iptables -L`. Verify you can reach the port: `nc -zv server-ip 22`. Check security groups in cloud providers (AWS, GCP).",
        "symptoms": "ssh: connect to host 203.0.113.50 port 22: Connection refused"
      },
      {
        "problem": "Permission denied (publickey) when SSH connecting",
        "cause": "Your public key is not in the server's authorized_keys, key permissions are wrong, or you are using the wrong key.",
        "solution": "Verify key permissions: `chmod 700 ~/.ssh && chmod 600 ~/.ssh/id_ed25519`. Use verbose mode: `ssh -vvv user@host` to see which keys are tried. Ensure your public key is in `~/.ssh/authorized_keys` on the server. Check the server's `/var/log/auth.log` for details.",
        "symptoms": "Permission denied (publickey,gssapi-keyex,gssapi-with-mic)"
      },
      {
        "problem": "Application cannot read/write files after deployment",
        "cause": "Files were deployed as root or a different user, and the application user does not have the necessary permissions.",
        "solution": "After deploying, fix ownership: `sudo chown -R deploy:deploy /var/www/app`. Ensure the upload directory is writable: `chmod 755 /var/www/app/uploads`. For log files, ensure the app user owns the log directory."
      }
    ],
    "further_reading": [
      {
        "title": "SSH Essentials - DigitalOcean",
        "url": "https://www.digitalocean.com/community/tutorials/ssh-essentials-working-with-ssh-servers-clients-and-keys",
        "type": "tutorial"
      },
      {
        "title": "Linux File Permissions Explained",
        "url": "https://www.redhat.com/sysadmin/linux-file-permissions-explained",
        "type": "article"
      },
      {
        "title": "OpenSSH Manual Pages",
        "url": "https://www.openssh.com/manual.html",
        "type": "documentation"
      },
      {
        "title": "How SSH Works - Computerphile",
        "url": "https://www.youtube.com/watch?v=ORcvSkgdA58",
        "type": "video"
      }
    ]
  },
  "6.1.3": {
    "body": "# systemd, cron, and Process Management\n\n## Introduction\n\nIn production, your Node.js application needs to start automatically on boot, restart if it crashes, run scheduled tasks, and be manageable by operations teams. This lesson covers the Linux tools that make this possible: systemd for service management, cron for scheduled tasks, and general process management techniques.\n\n## systemd: The Modern Service Manager\n\nsystemd is the init system and service manager for most modern Linux distributions. It manages the lifecycle of services (daemons) on your system.\n\n### Architecture Overview\n\n```\n┌─────────────────────────────────────────────┐\n│              systemd (PID 1)                 │\n│                                              │\n│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │\n│  │ nginx    │  │ node-app │  │ postgres │  │\n│  │ .service │  │ .service │  │ .service │  │\n│  └──────────┘  └──────────┘  └──────────┘  │\n│                                              │\n│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │\n│  │ .timer   │  │ .socket  │  │ .target  │  │\n│  │ (cron-   │  │ (socket  │  │ (group   │  │\n│  │  like)   │  │  activ.) │  │  units)  │  │\n│  └──────────┘  └──────────┘  └──────────┘  │\n└─────────────────────────────────────────────┘\n```\n\n### Creating a Service for Your Node.js App\n\nCreate a service file at `/etc/systemd/system/myapp.service`:\n\n```ini\n[Unit]\nDescription=My Node.js Application\nDocumentation=https://github.com/yourorg/myapp\nAfter=network.target postgresql.service\nWants=postgresql.service\n\n[Service]\nType=simple\nUser=deploy\nGroup=deploy\nWorkingDirectory=/var/www/myapp\nExecStart=/usr/bin/node dist/server.js\nExecReload=/bin/kill -HUP $MAINPID\nRestart=on-failure\nRestartSec=5\nStartLimitBurst=5\nStartLimitIntervalSec=60\n\n# Environment\nEnvironment=NODE_ENV=production\nEnvironment=PORT=3000\nEnvironmentFile=/var/www/myapp/.env\n\n# Security hardening\nNoNewPrivileges=true\nProtectSystem=strict\nProtectHome=true\nReadWritePaths=/var/www/myapp/uploads /var/log/myapp\nPrivateTmp=true\n\n# Logging\nStandardOutput=journal\nStandardError=journal\nSyslogIdentifier=myapp\n\n# Resource limits\nLimitNOFILE=65535\nMemoryMax=512M\nCPUQuota=80%\n\n[Install]\nWantedBy=multi-user.target\n```\n\n### Managing Services\n\n```bash\n# Reload systemd after creating/modifying service files\nsudo systemctl daemon-reload\n\n# Start, stop, restart\nsudo systemctl start myapp\nsudo systemctl stop myapp\nsudo systemctl restart myapp\nsudo systemctl reload myapp     # Graceful reload (if supported)\n\n# Enable/disable auto-start on boot\nsudo systemctl enable myapp\nsudo systemctl disable myapp\n\n# Check status\nsudo systemctl status myapp\n\n# View logs\nsudo journalctl -u myapp              # All logs\nsudo journalctl -u myapp -f           # Follow (like tail -f)\nsudo journalctl -u myapp --since today\nsudo journalctl -u myapp --since \"2024-01-15 10:00\" --until \"2024-01-15 11:00\"\nsudo journalctl -u myapp -n 100       # Last 100 lines\nsudo journalctl -u myapp -p err       # Only errors\n\n# List all services\nsystemctl list-units --type=service\nsystemctl list-units --type=service --state=running\n```\n\n### Service Dependencies\n\n```ini\n# Ensure PostgreSQL starts before your app\n[Unit]\nAfter=postgresql.service redis.service\nRequires=postgresql.service         # Hard dependency - if PG fails, app fails\nWants=redis.service                 # Soft dependency - app starts even if Redis fails\n\n# Start multiple instances\n# myapp@.service (template)\n[Service]\nExecStart=/usr/bin/node dist/server.js\nEnvironment=PORT=%i\n\n# Then: systemctl start myapp@3000 myapp@3001 myapp@3002\n```\n\n### Understanding Restart Policies\n\n```ini\n# Restart policies:\nRestart=no                 # Never restart (default)\nRestart=on-failure         # Restart on non-zero exit code (RECOMMENDED)\nRestart=always             # Always restart (even on clean exit)\nRestart=on-abnormal        # Restart on signal/timeout\nRestart=on-abort           # Restart on signal only\n\n# Restart timing\nRestartSec=5               # Wait 5 seconds before restarting\nStartLimitBurst=5          # Max 5 restarts...\nStartLimitIntervalSec=60   # ...within 60 seconds, then give up\n```\n\n## cron: Scheduled Tasks\n\ncron executes commands on a schedule. Essential for backups, cleanup tasks, report generation, and periodic data processing.\n\n### Cron Schedule Format\n\n```\n┌───────────── minute (0-59)\n│ ┌───────────── hour (0-23)\n│ │ ┌───────────── day of month (1-31)\n│ │ │ ┌───────────── month (1-12)\n│ │ │ │ ┌───────────── day of week (0-7, 0 and 7 = Sunday)\n│ │ │ │ │\n* * * * *  command to execute\n```\n\n### Common Schedules\n\n```bash\n# Edit crontab for current user\ncrontab -e\n\n# List current user's crontab\ncrontab -l\n\n# Common schedules\n* * * * *     # Every minute\n*/5 * * * *   # Every 5 minutes\n0 * * * *     # Every hour (at minute 0)\n0 0 * * *     # Every day at midnight\n0 0 * * 0     # Every Sunday at midnight\n0 2 * * *     # Every day at 2 AM\n0 0 1 * *     # First day of every month\n30 4 * * 1-5  # Weekdays at 4:30 AM\n```\n\n### Real-World Cron Examples for Node.js\n\n```bash\n# Database backup every day at 2 AM\n0 2 * * * /var/www/myapp/scripts/backup-db.sh >> /var/log/myapp/backup.log 2>&1\n\n# Clean up temp files every hour\n0 * * * * find /var/www/myapp/tmp -type f -mmin +60 -delete\n\n# Run a Node.js script to send digest emails every morning\n0 8 * * * cd /var/www/myapp && /usr/bin/node scripts/send-digest.js >> /var/log/myapp/digest.log 2>&1\n\n# Rotate application logs weekly\n0 0 * * 0 /usr/sbin/logrotate /etc/logrotate.d/myapp\n\n# Health check every 5 minutes\n*/5 * * * * curl -sf http://localhost:3000/health || systemctl restart myapp\n\n# Certificate renewal (Let's Encrypt)\n0 0 1,15 * * certbot renew --quiet && systemctl reload nginx\n```\n\n### Cron Best Practices\n\n```bash\n# Always redirect output to a log file\n0 2 * * * /path/to/script.sh >> /var/log/cron-job.log 2>&1\n\n# Use full paths (cron has minimal PATH)\n0 * * * * /usr/bin/node /var/www/myapp/scripts/task.js\n\n# Set environment variables at the top of crontab\nSHELL=/bin/bash\nPATH=/usr/local/bin:/usr/bin:/bin\nNODE_ENV=production\nMAILTO=admin@example.com\n\n# Lock file to prevent overlapping runs\n*/5 * * * * flock -n /tmp/mytask.lock /var/www/myapp/scripts/long-task.sh\n```\n\n### systemd Timers (Modern Alternative to cron)\n\n```ini\n# /etc/systemd/system/myapp-backup.timer\n[Unit]\nDescription=Daily database backup\n\n[Timer]\nOnCalendar=*-*-* 02:00:00\nPersistent=true           # Run missed executions after downtime\nRandomizedDelaySec=300     # Random delay up to 5 min (avoid thundering herd)\n\n[Install]\nWantedBy=timers.target\n```\n\n```ini\n# /etc/systemd/system/myapp-backup.service\n[Unit]\nDescription=Database backup job\n\n[Service]\nType=oneshot\nUser=deploy\nExecStart=/var/www/myapp/scripts/backup-db.sh\n```\n\n```bash\n# Enable and start the timer\nsudo systemctl enable myapp-backup.timer\nsudo systemctl start myapp-backup.timer\n\n# Check timer status\nsystemctl list-timers --all\n```\n\n## Process Management\n\n### Understanding Signals\n\n```\nSignal     Number   Meaning                  Node.js Handler\n─────────────────────────────────────────────────────────────\nSIGTERM    15       Graceful shutdown         process.on('SIGTERM', ...)\nSIGINT     2        Interrupt (Ctrl+C)        process.on('SIGINT', ...)\nSIGKILL    9        Force kill (uncatchable)  Cannot be handled\nSIGHUP     1        Reload configuration      process.on('SIGHUP', ...)\nSIGUSR2    12       User-defined              Used by nodemon, PM2\n```\n\n### Graceful Shutdown in Node.js\n\n```javascript\n// server.js - Production-ready signal handling\nconst server = app.listen(PORT, () => {\n  console.log(`Server running on port ${PORT}`);\n});\n\nfunction gracefulShutdown(signal) {\n  console.log(`Received ${signal}. Starting graceful shutdown...`);\n\n  // Stop accepting new connections\n  server.close(() => {\n    console.log('HTTP server closed.');\n\n    // Close database connections\n    prisma.$disconnect().then(() => {\n      console.log('Database disconnected.');\n\n      // Close Redis\n      redis.quit().then(() => {\n        console.log('Redis disconnected.');\n        process.exit(0);\n      });\n    });\n  });\n\n  // Force shutdown after 30 seconds\n  setTimeout(() => {\n    console.error('Forced shutdown after timeout');\n    process.exit(1);\n  }, 30000);\n}\n\nprocess.on('SIGTERM', () => gracefulShutdown('SIGTERM'));\nprocess.on('SIGINT', () => gracefulShutdown('SIGINT'));\n\n// Handle uncaught errors\nprocess.on('uncaughtException', (err) => {\n  console.error('Uncaught Exception:', err);\n  gracefulShutdown('uncaughtException');\n});\n\nprocess.on('unhandledRejection', (reason) => {\n  console.error('Unhandled Rejection:', reason);\n  // Don't exit - just log. Or exit if you prefer fail-fast.\n});\n```\n\n### Monitoring Processes\n\n```bash\n# Real-time process monitoring\nhtop\n\n# Process tree (see parent-child relationships)\npstree -p\n\n# Detailed process info\nps aux | grep node\nps -eo pid,ppid,%cpu,%mem,cmd | grep node\n\n# Track system calls (debugging)\nstrace -p PID -e trace=network    # Network calls\nstrace -p PID -e trace=file       # File operations\n\n# Monitor file descriptors\nls -la /proc/PID/fd | wc -l      # Count open file descriptors\nlsof -p PID                       # List open files for process\n\n# Monitor network connections\nss -tlnp | grep node             # TCP connections by node\nss -s                             # Summary statistics\n```\n\n### Resource Limits\n\n```bash\n# Check current limits\nulimit -a\n\n# Increase open file limit (current session)\nulimit -n 65535\n\n# Permanent limits in /etc/security/limits.conf\ndeploy  soft  nofile  65535\ndeploy  hard  nofile  65535\n\n# Or in systemd service file\n[Service]\nLimitNOFILE=65535\nLimitNPROC=4096\n```\n\n## Putting It All Together: Production Deployment\n\nHere is a complete example of deploying a Node.js app with systemd:\n\n```bash\n#!/bin/bash\n# deploy.sh - Production deployment script\n\nset -euo pipefail\n\nAPP_DIR=/var/www/myapp\nDEPLOY_USER=deploy\n\necho \"=== Deploying application ===\"\n\n# Pull latest code\ncd $APP_DIR\ngit pull origin main\n\n# Install dependencies\nnpm ci --production\n\n# Build TypeScript\nnpm run build\n\n# Run database migrations\nnpx prisma migrate deploy\n\n# Restart with zero downtime\nsudo systemctl reload-or-restart myapp\n\n# Verify health\nsleep 5\nif curl -sf http://localhost:3000/health > /dev/null; then\n    echo \"Deployment successful!\"\nelse\n    echo \"Health check failed! Rolling back...\"\n    git checkout HEAD~1\n    npm ci --production\n    npm run build\n    sudo systemctl restart myapp\n    exit 1\nfi\n```\n\n## Common Pitfalls\n\n1. **Not using `daemon-reload` after editing service files** - systemd caches unit files. Always run `sudo systemctl daemon-reload`.\n2. **Cron jobs failing silently** - Always redirect output: `>> /var/log/job.log 2>&1`.\n3. **Not handling SIGTERM in Node.js** - Your app gets 30 seconds to shut down before SIGKILL. Use that time to close connections.\n4. **Running Node.js as root** - Always create a dedicated user. Use `cap_net_bind_service` if you need port 80.\n5. **Overlapping cron jobs** - Use `flock` to prevent a slow job from running concurrently with itself.\n",
    "intuition": "systemd is like a super-reliable babysitter for your application. You tell it exactly how to start your app, and it watches over it 24/7 - restarting it if it crashes, starting it when the server boots, and keeping logs of everything. cron is like an alarm clock that triggers tasks on a schedule, while signals are like tapping someone on the shoulder (SIGTERM) versus pushing them out the door (SIGKILL).",
    "exercises": [
      {
        "title": "Create a systemd Service",
        "description": "Write a complete systemd service file for a Node.js Express application. It should: run as the 'deploy' user, auto-restart on failure with a 5-second delay, load environment from a .env file, set a memory limit of 512MB, and log to journald. Enable it to start on boot.",
        "hint": "Use the [Unit], [Service], and [Install] sections. Key directives: User, WorkingDirectory, ExecStart, Restart, RestartSec, EnvironmentFile, MemoryMax."
      },
      {
        "title": "Cron Job Suite",
        "description": "Write a crontab with the following jobs: (1) database backup at 2 AM daily, (2) log cleanup every Sunday, (3) health check every 5 minutes that restarts the service if it fails, (4) SSL certificate renewal on the 1st and 15th of each month. Include proper logging for each job.",
        "hint": "Use crontab -e. Remember to redirect both stdout and stderr. Use flock for the backup job to prevent overlapping runs."
      },
      {
        "title": "Graceful Shutdown Implementation",
        "description": "Write a Node.js server that handles SIGTERM and SIGINT. On receiving these signals, it should: (1) stop accepting new connections, (2) wait for existing requests to complete (up to 30 seconds), (3) close database connections, and (4) exit. Test by sending signals with kill.",
        "hint": "Use server.close() to stop accepting connections. Track active connections with a counter. Use setTimeout as a safety net for forced shutdown."
      }
    ],
    "problems": [
      {
        "problem": "systemd service keeps restarting in a loop then stops",
        "cause": "The service failed too many times within the StartLimitIntervalSec window, so systemd stopped trying.",
        "solution": "Check logs with `journalctl -u myapp -n 50`. Fix the root cause (missing env vars, wrong path, port conflict). Reset the failure counter: `sudo systemctl reset-failed myapp`. Adjust StartLimitBurst and StartLimitIntervalSec if the restarts are expected during startup.",
        "symptoms": "Status shows 'failed' and 'start-limit-hit'"
      },
      {
        "problem": "Cron job works manually but not in cron",
        "cause": "Cron runs with a minimal environment (limited PATH, no shell profile loaded).",
        "solution": "Use full paths to all executables (`/usr/bin/node` not just `node`). Set PATH at the top of crontab. Set the working directory with `cd /path && command`. Add logging to capture errors: `>> /var/log/job.log 2>&1`."
      },
      {
        "problem": "Node.js app not releasing port after restart",
        "cause": "The old process did not shut down gracefully, and the port is still held by a lingering connection in TIME_WAIT state.",
        "solution": "Handle SIGTERM properly in your Node.js code. In your server, ensure server.close() is called. If the issue persists, check for child processes: `pgrep -P old_pid`. As a last resort, use SO_REUSEADDR (Express does this by default).",
        "symptoms": "Error: listen EADDRINUSE :::3000"
      }
    ],
    "further_reading": [
      {
        "title": "systemd for Developers",
        "url": "https://systemd.io/",
        "type": "documentation"
      },
      {
        "title": "Understanding systemd Units and Unit Files",
        "url": "https://www.digitalocean.com/community/tutorials/understanding-systemd-units-and-unit-files",
        "type": "tutorial"
      },
      {
        "title": "Crontab Guru",
        "url": "https://crontab.guru/",
        "type": "tool",
        "note": "Interactive cron schedule expression editor"
      },
      {
        "title": "Node.js Graceful Shutdown",
        "url": "https://blog.heroku.com/best-practices-nodejs-errors",
        "type": "article"
      }
    ]
  },
  "6.1.4": {
    "body": "# Shell Scripting Basics for Backend Devs\n\n## Why Shell Scripting?\n\nAs a backend developer, you will write shell scripts for deployment automation, server setup, backup procedures, health checks, and CI/CD pipelines. While Node.js scripts can handle many tasks, shell scripts are the lingua franca of server automation -- they work everywhere, require no runtime installation, and integrate naturally with system tools.\n\n## Shell Script Fundamentals\n\n### Your First Script\n\n```bash\n#!/bin/bash\n# deploy.sh - Deploy application to production\n# Usage: ./deploy.sh [environment]\n\nset -euo pipefail    # The most important line in any bash script\n\necho \"Starting deployment...\"\n```\n\nThe `set -euo pipefail` flags:\n- `-e`: Exit immediately on any error\n- `-u`: Treat unset variables as errors\n- `-o pipefail`: Pipe fails if any command in the pipeline fails\n\n### Variables\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# Variable assignment (no spaces around =)\nAPP_NAME=\"myapp\"\nPORT=3000\nDEPLOY_DIR=\"/var/www/${APP_NAME}\"\n\n# Command substitution\nTIMESTAMP=$(date +%Y%m%d_%H%M%S)\nGIT_HASH=$(git rev-parse --short HEAD)\nNODE_VERSION=$(node --version)\n\n# Default values\nENVIRONMENT=${1:-production}          # First arg, default \"production\"\nLOG_LEVEL=${LOG_LEVEL:-info}          # Env var with default\n\n# String operations\nFILENAME=\"server.js\"\necho \"${FILENAME%.js}\"                # Remove suffix: \"server\"\necho \"${FILENAME##*.}\"                # Get extension: \"js\"\necho \"${APP_NAME^^}\"                  # Uppercase: \"MYAPP\"\necho \"${APP_NAME,,}\"                  # Lowercase: \"myapp\"\n\n# Arrays\nSERVERS=(\"web1\" \"web2\" \"web3\")\necho \"${SERVERS[0]}\"                  # First element\necho \"${SERVERS[@]}\"                  # All elements\necho \"${#SERVERS[@]}\"                 # Length: 3\n```\n\n### Conditionals\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# if/elif/else\nif [[ \"$NODE_ENV\" == \"production\" ]]; then\n    echo \"Running in production\"\nelif [[ \"$NODE_ENV\" == \"staging\" ]]; then\n    echo \"Running in staging\"\nelse\n    echo \"Running in development\"\nfi\n\n# File tests\nif [[ -f \".env\" ]]; then\n    echo \".env file exists\"\nfi\n\nif [[ -d \"node_modules\" ]]; then\n    echo \"Dependencies installed\"\nfi\n\nif [[ ! -f \"dist/server.js\" ]]; then\n    echo \"Build not found, building...\"\n    npm run build\nfi\n\n# Numeric comparisons\nif [[ $PORT -lt 1024 ]]; then\n    echo \"Port requires root privileges\"\nfi\n\n# String checks\nif [[ -z \"$DATABASE_URL\" ]]; then\n    echo \"ERROR: DATABASE_URL is not set\"\n    exit 1\nfi\n\nif [[ -n \"$REDIS_URL\" ]]; then\n    echo \"Redis URL configured\"\nfi\n\n# Command success/failure\nif command -v docker &> /dev/null; then\n    echo \"Docker is installed\"\nelse\n    echo \"Docker is NOT installed\"\n    exit 1\nfi\n\n# Combining conditions\nif [[ -f \".env\" ]] && [[ \"$NODE_ENV\" == \"production\" ]]; then\n    source .env\nfi\n```\n\n### Loops\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# For loop\nSERVERS=(\"web1.example.com\" \"web2.example.com\" \"web3.example.com\")\nfor server in \"${SERVERS[@]}\"; do\n    echo \"Deploying to $server...\"\n    ssh \"deploy@${server}\" \"cd /var/www/app && git pull && npm ci && pm2 reload all\"\ndone\n\n# C-style for loop\nfor ((i=1; i<=5; i++)); do\n    echo \"Attempt $i of 5...\"\n    if curl -sf http://localhost:3000/health > /dev/null; then\n        echo \"Health check passed!\"\n        break\n    fi\n    sleep 2\ndone\n\n# While loop\nRETRIES=0\nMAX_RETRIES=10\nwhile [[ $RETRIES -lt $MAX_RETRIES ]]; do\n    if pg_isready -h localhost -p 5432 > /dev/null 2>&1; then\n        echo \"PostgreSQL is ready!\"\n        break\n    fi\n    echo \"Waiting for PostgreSQL... (attempt $((RETRIES+1))/$MAX_RETRIES)\"\n    RETRIES=$((RETRIES + 1))\n    sleep 2\ndone\n\nif [[ $RETRIES -eq $MAX_RETRIES ]]; then\n    echo \"PostgreSQL did not become ready in time\"\n    exit 1\nfi\n\n# Read file line by line\nwhile IFS= read -r line; do\n    echo \"Processing: $line\"\ndone < servers.txt\n\n# Iterate over files\nfor file in src/**/*.ts; do\n    echo \"TypeScript file: $file\"\ndone\n```\n\n### Functions\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# Function definition\nlog() {\n    local level=$1\n    shift\n    echo \"[$(date '+%Y-%m-%d %H:%M:%S')] [$level] $*\"\n}\n\nlog \"INFO\" \"Starting deployment\"\nlog \"ERROR\" \"Something went wrong\"\n\n# Function with return value\ncheck_dependency() {\n    local cmd=$1\n    if command -v \"$cmd\" &> /dev/null; then\n        return 0\n    else\n        return 1\n    fi\n}\n\nif check_dependency \"docker\"; then\n    log \"INFO\" \"Docker found\"\nfi\n\n# Function with output capture\nget_free_port() {\n    local port\n    port=$(python3 -c 'import socket; s=socket.socket(); s.bind((\"\",0)); print(s.getsockname()[1]); s.close()')\n    echo \"$port\"\n}\n\nFREE_PORT=$(get_free_port)\necho \"Using port: $FREE_PORT\"\n\n# Cleanup function with trap\ncleanup() {\n    log \"INFO\" \"Cleaning up temporary files...\"\n    rm -rf \"$TMP_DIR\"\n}\n\nTMP_DIR=$(mktemp -d)\ntrap cleanup EXIT    # Runs cleanup on script exit (even on error)\n```\n\n## Real-World Scripts\n\n### Deployment Script\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# Configuration\nAPP_DIR=\"/var/www/myapp\"\nRELEASES_DIR=\"$APP_DIR/releases\"\nCURRENT_LINK=\"$APP_DIR/current\"\nSHARED_DIR=\"$APP_DIR/shared\"\nKEEP_RELEASES=5\n\nTIMESTAMP=$(date +%Y%m%d%H%M%S)\nRELEASE_DIR=\"$RELEASES_DIR/$TIMESTAMP\"\n\nlog() { echo \"[$(date '+%H:%M:%S')] $*\"; }\n\n# Create release directory\nlog \"Creating release $TIMESTAMP\"\nmkdir -p \"$RELEASE_DIR\"\n\n# Clone/copy application\nlog \"Copying application files...\"\ngit archive HEAD | tar -x -C \"$RELEASE_DIR\"\n\n# Install dependencies\nlog \"Installing dependencies...\"\ncd \"$RELEASE_DIR\"\nnpm ci --production\n\n# Build\nlog \"Building...\"\nnpm run build\n\n# Link shared files\nlog \"Linking shared resources...\"\nln -sf \"$SHARED_DIR/.env\" \"$RELEASE_DIR/.env\"\nln -sf \"$SHARED_DIR/uploads\" \"$RELEASE_DIR/uploads\"\n\n# Run migrations\nlog \"Running database migrations...\"\nnpx prisma migrate deploy\n\n# Switch symlink (atomic deployment)\nlog \"Switching to new release...\"\nln -sfn \"$RELEASE_DIR\" \"$CURRENT_LINK\"\n\n# Restart application\nlog \"Restarting application...\"\nsudo systemctl restart myapp\n\n# Health check\nlog \"Running health check...\"\nsleep 3\nif curl -sf http://localhost:3000/health > /dev/null; then\n    log \"Deployment successful!\"\nelse\n    log \"Health check failed! Rolling back...\"\n    PREVIOUS=$(ls -1t \"$RELEASES_DIR\" | sed -n '2p')\n    if [[ -n \"$PREVIOUS\" ]]; then\n        ln -sfn \"$RELEASES_DIR/$PREVIOUS\" \"$CURRENT_LINK\"\n        sudo systemctl restart myapp\n        log \"Rolled back to $PREVIOUS\"\n    fi\n    exit 1\nfi\n\n# Clean up old releases\nlog \"Cleaning old releases...\"\ncd \"$RELEASES_DIR\"\nls -1t | tail -n +$((KEEP_RELEASES + 1)) | xargs -r rm -rf\n\nlog \"Done!\"\n```\n\n### Database Backup Script\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# Configuration\nDB_NAME=\"myapp_production\"\nDB_USER=\"myapp\"\nBACKUP_DIR=\"/var/backups/postgresql\"\nS3_BUCKET=\"s3://myapp-backups/database\"\nRETENTION_DAYS=30\n\nTIMESTAMP=$(date +%Y%m%d_%H%M%S)\nBACKUP_FILE=\"$BACKUP_DIR/${DB_NAME}_${TIMESTAMP}.sql.gz\"\n\nlog() { echo \"[$(date '+%Y-%m-%d %H:%M:%S')] $*\"; }\n\n# Create backup directory\nmkdir -p \"$BACKUP_DIR\"\n\n# Create backup\nlog \"Starting backup of $DB_NAME...\"\npg_dump -U \"$DB_USER\" -h localhost \"$DB_NAME\" | gzip > \"$BACKUP_FILE\"\n\n# Verify backup\nBACKUP_SIZE=$(stat -f%z \"$BACKUP_FILE\" 2>/dev/null || stat -c%s \"$BACKUP_FILE\")\nif [[ $BACKUP_SIZE -lt 1000 ]]; then\n    log \"ERROR: Backup file is suspiciously small ($BACKUP_SIZE bytes)\"\n    exit 1\nfi\n\nlog \"Backup created: $BACKUP_FILE ($BACKUP_SIZE bytes)\"\n\n# Upload to S3 (if aws cli is available)\nif command -v aws &> /dev/null; then\n    log \"Uploading to S3...\"\n    aws s3 cp \"$BACKUP_FILE\" \"$S3_BUCKET/\"\n    log \"Upload complete\"\nfi\n\n# Clean up old local backups\nlog \"Removing backups older than $RETENTION_DAYS days...\"\nfind \"$BACKUP_DIR\" -name \"*.sql.gz\" -mtime +$RETENTION_DAYS -delete\n\nlog \"Backup completed successfully\"\n```\n\n### Health Check Script\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\nSERVICES=(\n    \"http://localhost:3000/health|Node.js API\"\n    \"http://localhost:5432|PostgreSQL\"\n    \"http://localhost:6379|Redis\"\n)\n\nSTATUS=0\n\ncheck_http() {\n    local url=$1\n    local name=$2\n    local response\n    local http_code\n\n    http_code=$(curl -sf -o /dev/null -w \"%{http_code}\" --max-time 5 \"$url\" 2>/dev/null) || true\n\n    if [[ \"$http_code\" == \"200\" ]]; then\n        echo \"  [OK]   $name ($url)\"\n    else\n        echo \"  [FAIL] $name ($url) - HTTP $http_code\"\n        STATUS=1\n    fi\n}\n\ncheck_port() {\n    local host=$1\n    local port=$2\n    local name=$3\n\n    if nc -z -w5 \"$host\" \"$port\" 2>/dev/null; then\n        echo \"  [OK]   $name ($host:$port)\"\n    else\n        echo \"  [FAIL] $name ($host:$port)\"\n        STATUS=1\n    fi\n}\n\necho \"=== Health Check Report ===\"\necho \"Time: $(date)\"\necho \"\"\n\n# Check HTTP endpoints\ncheck_http \"http://localhost:3000/health\" \"Node.js API\"\n\n# Check ports\ncheck_port \"localhost\" 5432 \"PostgreSQL\"\ncheck_port \"localhost\" 6379 \"Redis\"\n\n# Check disk space\nDISK_USAGE=$(df / | tail -1 | awk '{print $5}' | tr -d '%')\nif [[ $DISK_USAGE -gt 90 ]]; then\n    echo \"  [WARN] Disk usage is at ${DISK_USAGE}%\"\n    STATUS=1\nelse\n    echo \"  [OK]   Disk usage: ${DISK_USAGE}%\"\nfi\n\n# Check memory\nMEM_AVAILABLE=$(free -m | awk '/Mem:/ {print $7}')\nif [[ $MEM_AVAILABLE -lt 256 ]]; then\n    echo \"  [WARN] Low memory: ${MEM_AVAILABLE}MB available\"\n    STATUS=1\nelse\n    echo \"  [OK]   Memory available: ${MEM_AVAILABLE}MB\"\nfi\n\necho \"\"\nif [[ $STATUS -eq 0 ]]; then\n    echo \"All checks passed!\"\nelse\n    echo \"Some checks FAILED!\"\nfi\n\nexit $STATUS\n```\n\n## Script Safety Patterns\n\n### Input Validation\n\n```bash\n#!/bin/bash\nset -euo pipefail\n\n# Require arguments\nif [[ $# -lt 1 ]]; then\n    echo \"Usage: $0 <environment>\"\n    echo \"Environments: staging, production\"\n    exit 1\nfi\n\nENVIRONMENT=$1\n\n# Validate input\ncase \"$ENVIRONMENT\" in\n    staging|production)\n        echo \"Deploying to $ENVIRONMENT\"\n        ;;\n    *)\n        echo \"ERROR: Invalid environment '$ENVIRONMENT'\"\n        echo \"Must be 'staging' or 'production'\"\n        exit 1\n        ;;\nesac\n```\n\n### Confirmation Prompts\n\n```bash\nif [[ \"$ENVIRONMENT\" == \"production\" ]]; then\n    read -r -p \"Deploy to PRODUCTION? Type 'yes' to confirm: \" confirm\n    if [[ \"$confirm\" != \"yes\" ]]; then\n        echo \"Deployment cancelled.\"\n        exit 0\n    fi\nfi\n```\n\n### Lock Files\n\n```bash\nLOCKFILE=\"/tmp/deploy.lock\"\n\nif [[ -f \"$LOCKFILE\" ]]; then\n    PID=$(cat \"$LOCKFILE\")\n    if kill -0 \"$PID\" 2>/dev/null; then\n        echo \"Deployment already in progress (PID $PID)\"\n        exit 1\n    else\n        echo \"Stale lock file found, removing\"\n        rm \"$LOCKFILE\"\n    fi\nfi\n\necho $$ > \"$LOCKFILE\"\ntrap 'rm -f \"$LOCKFILE\"' EXIT\n```\n\n## Debugging Shell Scripts\n\n```bash\n# Run with debug output\nbash -x deploy.sh\n\n# Enable debug in script\nset -x          # Turn on debug\n# ... commands ...\nset +x          # Turn off debug\n\n# Print commands as they execute (at top of script)\n#!/bin/bash -x\n```\n\n## Common Pitfalls\n\n1. **Forgetting `set -euo pipefail`** - Without this, scripts continue after errors, potentially doing damage.\n2. **Not quoting variables** - `$VAR` vs `\"$VAR\"`. Unquoted variables break on spaces.\n3. **Using `==` in `[ ]` vs `[[ ]]`** - Always use `[[ ]]` in bash; `[ ]` is POSIX but less powerful.\n4. **Not using `local` in functions** - Variables leak to global scope without `local`.\n5. **Assuming GNU tools on macOS** - macOS uses BSD versions of `sed`, `find`, etc. They differ from Linux. Use Homebrew's `coreutils` or write portable commands.\n",
    "intuition": "A shell script is like a recipe card for your server. Instead of manually typing commands every time you deploy, back up, or check health, you write them down once and let the script repeat the exact same steps perfectly every time. The `set -euo pipefail` at the top is like telling the chef: 'If any step fails, stop immediately and tell me -- don't try to serve a half-cooked dish.'",
    "exercises": [
      {
        "title": "Build a Deployment Script",
        "description": "Write a bash deployment script that: (1) accepts an environment argument (staging/production), (2) validates the argument, (3) asks for confirmation if production, (4) creates a timestamped release directory, (5) runs npm ci and npm run build, (6) runs a health check, and (7) rolls back on failure.",
        "hint": "Use set -euo pipefail, trap for cleanup, functions for each step, and curl for health checks."
      },
      {
        "title": "Server Setup Script",
        "description": "Write a script that sets up a fresh Ubuntu server for Node.js: install Node.js (via nvm or NodeSource), install PM2 globally, create a deploy user, set up the application directory structure, and configure firewall rules (ufw).",
        "hint": "Use apt-get for package installation. Check if each tool is already installed before installing. Use adduser for user creation."
      },
      {
        "title": "Log Rotation Script",
        "description": "Write a script that rotates application logs: compress logs older than 1 day, delete compressed logs older than 30 days, and report the total space recovered. The script should use flock to prevent concurrent execution.",
        "hint": "Use find with -mtime, gzip for compression, and stat to calculate file sizes before/after."
      }
    ],
    "problems": [
      {
        "problem": "Script works when run manually but fails in cron or CI",
        "cause": "The script depends on environment variables, PATH, or working directory that are set in your interactive shell but not available in cron/CI environments.",
        "solution": "Always use full paths to executables. Set PATH explicitly at the top of the script. Source environment files explicitly. Use `cd` to set the working directory. Never rely on ~ (use $HOME instead).",
        "code": "#!/bin/bash\\nset -euo pipefail\\nexport PATH=/usr/local/bin:/usr/bin:/bin\\ncd /var/www/myapp"
      },
      {
        "problem": "Script fails with 'unexpected end of file'",
        "cause": "Unclosed quotes, missing 'fi', 'done', or 'esac', or the file has Windows line endings (\\r\\n).",
        "solution": "Check for matching if/fi, while/done, case/esac pairs. Fix line endings with `sed -i 's/\\r$//' script.sh` or `dos2unix script.sh`. Use shellcheck for static analysis: `shellcheck script.sh`."
      },
      {
        "problem": "Variable expansion not working as expected",
        "cause": "Using single quotes instead of double quotes. Single quotes prevent all variable expansion.",
        "solution": "Use double quotes for strings with variables: `echo \"Hello $NAME\"`. Use single quotes only for literal strings. For complex cases, use ${variable} syntax for clarity.",
        "code": "# Wrong:\\necho 'Hello $NAME'  # Outputs: Hello $NAME\\n# Right:\\necho \"Hello $NAME\"  # Outputs: Hello World"
      }
    ],
    "further_reading": [
      {
        "title": "ShellCheck - Shell Script Analysis Tool",
        "url": "https://www.shellcheck.net/",
        "type": "tool",
        "note": "Paste your scripts to find bugs and best practice violations"
      },
      {
        "title": "Bash Scripting Cheatsheet",
        "url": "https://devhints.io/bash",
        "type": "reference"
      },
      {
        "title": "Google Shell Style Guide",
        "url": "https://google.github.io/styleguide/shellguide.html",
        "type": "documentation"
      },
      {
        "title": "Advanced Bash-Scripting Guide",
        "url": "https://tldp.org/LDP/abs/html/",
        "type": "book"
      }
    ]
  },
  "6.2.1": {
    "body": "# Dockerfile: Building Node.js Application Images\n\n## Why Docker for Node.js?\n\nDocker packages your application with its exact runtime environment - Node.js version, system dependencies, npm packages - into a portable image. This eliminates \"works on my machine\" problems and ensures identical behavior across development, CI, staging, and production.\n\n## Docker Fundamentals\n\n### Architecture Overview\n\n```\n┌──────────────────────────────────────────┐\n│              Docker Host                  │\n│                                          │\n│  ┌────────────┐  ┌────────────┐         │\n│  │ Container 1│  │ Container 2│         │\n│  │ Node.js API│  │ PostgreSQL │         │\n│  │ Port: 3000 │  │ Port: 5432 │         │\n│  └─────┬──────┘  └─────┬──────┘         │\n│        │                │                │\n│  ┌─────┴────────────────┴──────┐        │\n│  │      Docker Network          │        │\n│  └──────────────────────────────┘        │\n│                                          │\n│  ┌──────────────────────────────┐        │\n│  │      Docker Volumes          │        │\n│  │  (persistent data)           │        │\n│  └──────────────────────────────┘        │\n└──────────────────────────────────────────┘\n```\n\n### Image Layers\n\n```\n┌─────────────────────────────┐\n│  COPY . .                   │  Layer 5 (changes often)\n├─────────────────────────────┤\n│  RUN npm ci                 │  Layer 4 (changes with package.json)\n├─────────────────────────────┤\n│  COPY package*.json ./      │  Layer 3\n├─────────────────────────────┤\n│  WORKDIR /app               │  Layer 2\n├─────────────────────────────┤\n│  node:20-alpine             │  Layer 1 (base, rarely changes)\n└─────────────────────────────┘\n\nEach layer is cached. If a layer hasn't changed,\nDocker reuses the cached version.\n```\n\n## Writing Dockerfiles for Node.js\n\n### Basic Dockerfile\n\n```dockerfile\n# Dockerfile\nFROM node:20-alpine\n\n# Create app directory\nWORKDIR /app\n\n# Copy package files first (better caching)\nCOPY package.json package-lock.json ./\n\n# Install dependencies\nRUN npm ci\n\n# Copy application source\nCOPY . .\n\n# Build TypeScript (if applicable)\nRUN npm run build\n\n# Expose port (documentation only)\nEXPOSE 3000\n\n# Start the application\nCMD [\"node\", \"dist/server.js\"]\n```\n\n### Production-Optimized Dockerfile\n\n```dockerfile\n# ============================\n# Stage 1: Build\n# ============================\nFROM node:20-alpine AS builder\n\nWORKDIR /app\n\n# Copy dependency files\nCOPY package.json package-lock.json ./\n\n# Install ALL dependencies (including devDependencies for building)\nRUN npm ci\n\n# Copy source code\nCOPY tsconfig.json ./\nCOPY src/ ./src/\nCOPY prisma/ ./prisma/\n\n# Generate Prisma client\nRUN npx prisma generate\n\n# Build TypeScript\nRUN npm run build\n\n# Remove dev dependencies\nRUN npm ci --production && npm cache clean --force\n\n# ============================\n# Stage 2: Production\n# ============================\nFROM node:20-alpine AS production\n\n# Add non-root user\nRUN addgroup -g 1001 -S appgroup && \\\n    adduser -S appuser -u 1001 -G appgroup\n\n# Install security updates\nRUN apk update && apk upgrade && rm -rf /var/cache/apk/*\n\n# Install dumb-init for proper signal handling\nRUN apk add --no-cache dumb-init\n\nWORKDIR /app\n\n# Copy built artifacts from builder stage\nCOPY --from=builder --chown=appuser:appgroup /app/dist ./dist\nCOPY --from=builder --chown=appuser:appgroup /app/node_modules ./node_modules\nCOPY --from=builder --chown=appuser:appgroup /app/package.json ./\nCOPY --from=builder --chown=appuser:appgroup /app/prisma ./prisma\n\n# Set environment\nENV NODE_ENV=production\nENV PORT=3000\n\n# Switch to non-root user\nUSER appuser\n\n# Expose port\nEXPOSE 3000\n\n# Health check\nHEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \\\n    CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1\n\n# Use dumb-init as PID 1 for proper signal handling\nENTRYPOINT [\"dumb-init\", \"--\"]\n\n# Start the application\nCMD [\"node\", \"dist/server.js\"]\n```\n\n### The .dockerignore File\n\n```\n# .dockerignore\nnode_modules\nnpm-debug.log*\ndist\n.git\n.gitignore\n.env\n.env.*\n!.env.example\nDockerfile\ndocker-compose*.yml\n.dockerignore\nREADME.md\n.vscode\n.idea\ncoverage\n*.test.ts\n*.spec.ts\n__tests__\n.nyc_output\n```\n\n## Key Dockerfile Instructions\n\n### FROM\n\n```dockerfile\n# Use specific version (always pin versions in production)\nFROM node:20.11.0-alpine3.19\n\n# Bad: uses latest, unpredictable\nFROM node:latest\n\n# Alpine is smaller (~50MB vs ~350MB for debian-based)\nFROM node:20-alpine\n\n# Slim is a middle ground\nFROM node:20-slim\n```\n\n### COPY vs ADD\n\n```dockerfile\n# COPY - simple file/directory copy (preferred)\nCOPY package.json ./\nCOPY src/ ./src/\n\n# ADD - has extra features (auto-extract archives, URLs)\nADD archive.tar.gz /app/  # Auto-extracts\n# Prefer COPY unless you need ADD's features\n```\n\n### RUN - Reducing Layers\n\n```dockerfile\n# Bad: creates 3 layers\nRUN apk update\nRUN apk add curl\nRUN rm -rf /var/cache/apk/*\n\n# Good: single layer, smaller image\nRUN apk update && \\\n    apk add --no-cache curl && \\\n    rm -rf /var/cache/apk/*\n```\n\n### ENV vs ARG\n\n```dockerfile\n# ARG - only available during build\nARG NODE_VERSION=20\nFROM node:${NODE_VERSION}-alpine\n\nARG BUILD_DATE\nLABEL build-date=$BUILD_DATE\n\n# ENV - available during build AND in running container\nENV NODE_ENV=production\nENV PORT=3000\n```\n\n### ENTRYPOINT vs CMD\n\n```dockerfile\n# CMD - default command, easily overridden\nCMD [\"node\", \"server.js\"]\n# docker run myapp                    -> node server.js\n# docker run myapp node repl          -> node repl (overridden)\n\n# ENTRYPOINT - always runs, CMD becomes arguments\nENTRYPOINT [\"node\"]\nCMD [\"server.js\"]\n# docker run myapp                    -> node server.js\n# docker run myapp script.js          -> node script.js\n\n# Best practice for production:\nENTRYPOINT [\"dumb-init\", \"--\"]\nCMD [\"node\", \"dist/server.js\"]\n```\n\n## Essential Docker Commands\n\n```bash\n# Build an image\ndocker build -t myapp:latest .\ndocker build -t myapp:1.0.0 -f Dockerfile.production .\n\n# Run a container\ndocker run -d -p 3000:3000 --name myapp myapp:latest\ndocker run -d -p 3000:3000 --env-file .env --name myapp myapp:latest\ndocker run --rm -it myapp:latest sh  # Interactive shell\n\n# Container management\ndocker ps                   # Running containers\ndocker ps -a                # All containers (including stopped)\ndocker logs myapp           # View logs\ndocker logs -f myapp        # Follow logs\ndocker exec -it myapp sh    # Shell into running container\ndocker stop myapp           # Graceful stop (SIGTERM)\ndocker kill myapp           # Force stop (SIGKILL)\ndocker rm myapp             # Remove container\n\n# Image management\ndocker images               # List images\ndocker rmi myapp:latest     # Remove image\ndocker system prune -a      # Remove ALL unused images and containers\n\n# Debugging\ndocker inspect myapp        # Container details\ndocker stats                # Resource usage\ndocker top myapp            # Running processes\n```\n\n## Signal Handling with dumb-init\n\nNode.js as PID 1 in a container does not handle signals properly by default. `dumb-init` fixes this:\n\n```\nWithout dumb-init:\ndocker stop myapp\n  └─> SIGTERM sent to PID 1 (node)\n      └─> Node.js doesn't forward to children\n          └─> After 10s, Docker sends SIGKILL (ungraceful)\n\nWith dumb-init:\ndocker stop myapp\n  └─> SIGTERM sent to PID 1 (dumb-init)\n      └─> dumb-init forwards SIGTERM to node (PID 2)\n          └─> Node.js runs graceful shutdown handlers\n              └─> Clean exit\n```\n\n## Common Pitfalls\n\n1. **Running as root** - Always create and use a non-root user. Root in container = root on host (in many configurations).\n2. **Using `npm install` instead of `npm ci`** - `npm ci` gives deterministic builds from lockfile. `npm install` may update packages.\n3. **Not leveraging layer caching** - Copy package.json BEFORE copying source code. Dependencies change less often than code.\n4. **Including dev dependencies in production** - Use multi-stage builds or `npm ci --production`.\n5. **Not using .dockerignore** - Without it, `node_modules` and `.git` are copied into the image, bloating it.\n6. **Not pinning base image versions** - `node:latest` today may be different tomorrow.\n",
    "intuition": "A Dockerfile is like a recipe for creating a sealed, portable lunchbox. You list every ingredient (dependencies) and step (build commands) needed. Multi-stage builds are like using a full kitchen to cook (build stage) but only packing the finished dish into the lunchbox (production stage) -- you do not need the oven and pots in the lunchbox.",
    "exercises": [
      {
        "title": "Build a Production Dockerfile",
        "description": "Create a multi-stage Dockerfile for a TypeScript Express application with Prisma. The build stage should compile TypeScript and generate the Prisma client. The production stage should be based on Alpine, run as a non-root user, include a health check, and use dumb-init.",
        "hint": "Use 'FROM node:20-alpine AS builder' for the build stage and a fresh 'FROM node:20-alpine' for production. Copy only dist/, node_modules/, and prisma/ from the builder."
      },
      {
        "title": "Optimize Image Size",
        "description": "Start with a basic Dockerfile that produces a ~1GB image (using node:20, copying everything, including devDependencies). Optimize it step by step: switch to Alpine, add .dockerignore, use multi-stage build, and production-only dependencies. Document the size at each step.",
        "hint": "Use 'docker images' to check sizes after each optimization. Alpine alone should cut the base by ~300MB. Multi-stage should remove build tools and devDependencies."
      },
      {
        "title": "Docker Layer Caching Experiment",
        "description": "Build a Dockerfile two ways: (1) COPY everything then npm install, (2) COPY package*.json first, npm ci, then COPY the rest. Make a change to a source file and rebuild both. Measure and compare build times to demonstrate caching benefits.",
        "hint": "Use 'time docker build .' to measure. With proper caching, only the COPY . . and later layers rebuild when source changes."
      }
    ],
    "problems": [
      {
        "problem": "Docker build fails with 'npm ERR! code ERESOLVE' or dependency errors",
        "cause": "The package-lock.json is out of sync with package.json, or there are platform-specific dependencies (e.g., bcrypt native modules).",
        "solution": "Delete package-lock.json and node_modules locally, run `npm install` to regenerate the lockfile, then rebuild. For native modules, ensure the Alpine image has build tools: `RUN apk add --no-cache python3 make g++` or use pre-built alternatives (bcryptjs instead of bcrypt)."
      },
      {
        "problem": "Container runs but cannot connect to database",
        "cause": "localhost inside the container refers to the container itself, not the host machine. The database is on the host or another container.",
        "solution": "Use Docker networking. If database is in another container, use the container name as hostname. If on host, use `host.docker.internal` (Docker Desktop) or `--network host` (Linux). Best practice: use Docker Compose with a shared network."
      },
      {
        "problem": "SIGTERM not reaching Node.js process, container takes 10 seconds to stop",
        "cause": "Node.js as PID 1 does not handle signals properly. Or CMD is in shell form (`CMD node server.js`) which wraps in /bin/sh.",
        "solution": "Use exec form for CMD: `CMD [\"node\", \"server.js\"]`. Better: install and use dumb-init as ENTRYPOINT. Also ensure your Node.js code handles SIGTERM: `process.on('SIGTERM', () => server.close())`."
      }
    ],
    "further_reading": [
      {
        "title": "Docker Official Node.js Best Practices",
        "url": "https://github.com/nodejs/docker-node/blob/main/docs/BestPractices.md",
        "type": "github"
      },
      {
        "title": "Dockerfile Reference",
        "url": "https://docs.docker.com/engine/reference/builder/",
        "type": "documentation"
      },
      {
        "title": "Snyk - 10 Docker Security Best Practices",
        "url": "https://snyk.io/blog/10-docker-image-security-best-practices/",
        "type": "article"
      },
      {
        "title": "Dive - Explore Docker Image Layers",
        "url": "https://github.com/wagoodman/dive",
        "type": "tool",
        "note": "Visualize and optimize your Docker image layers"
      }
    ]
  },
  "6.2.2": {
    "body": "# Docker Compose: Multi-Container Applications\n\n## Why Docker Compose?\n\nReal applications rarely run alone. A typical Node.js backend needs PostgreSQL, Redis, maybe Elasticsearch and a message broker. Docker Compose lets you define and run all these services together with a single YAML file and one command.\n\n## Architecture\n\n```\ndocker-compose.yml\n┌──────────────────────────────────────────────┐\n│                                              │\n│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │\n│  │  api      │  │  postgres │  │  redis   │  │\n│  │  :3000    │──│  :5432    │  │  :6379   │  │\n│  │  Node.js  │  │          │  │          │  │\n│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │\n│       │              │              │        │\n│  ┌────┴──────────────┴──────────────┴──┐    │\n│  │         app-network (bridge)         │    │\n│  └──────────────────────────────────────┘    │\n│                                              │\n│  ┌──────────────┐  ┌──────────────┐         │\n│  │ pgdata       │  │ redis-data   │         │\n│  │ (volume)     │  │ (volume)     │         │\n│  └──────────────┘  └──────────────┘         │\n└──────────────────────────────────────────────┘\n```\n\n## Complete docker-compose.yml\n\n```yaml\n# docker-compose.yml\nversion: '3.8'\n\nservices:\n  # ================================\n  # Node.js API\n  # ================================\n  api:\n    build:\n      context: .\n      dockerfile: Dockerfile\n      target: production        # Multi-stage build target\n    container_name: myapp-api\n    ports:\n      - \"3000:3000\"\n    environment:\n      NODE_ENV: production\n      PORT: 3000\n      DATABASE_URL: postgresql://myapp:secretpassword@postgres:5432/myapp_db\n      REDIS_URL: redis://redis:6379\n    env_file:\n      - .env                    # Additional env vars from file\n    depends_on:\n      postgres:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n    restart: unless-stopped\n    networks:\n      - app-network\n    volumes:\n      - uploads:/app/uploads    # Persistent uploads\n    healthcheck:\n      test: [\"CMD\", \"wget\", \"--no-verbose\", \"--tries=1\", \"--spider\", \"http://localhost:3000/health\"]\n      interval: 30s\n      timeout: 5s\n      retries: 3\n      start_period: 10s\n\n  # ================================\n  # PostgreSQL Database\n  # ================================\n  postgres:\n    image: postgres:16-alpine\n    container_name: myapp-postgres\n    environment:\n      POSTGRES_DB: myapp_db\n      POSTGRES_USER: myapp\n      POSTGRES_PASSWORD: secretpassword\n    ports:\n      - \"5432:5432\"             # Expose for local development tools\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n      - ./init-scripts:/docker-entrypoint-initdb.d  # Auto-run SQL on first start\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U myapp -d myapp_db\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n    restart: unless-stopped\n    networks:\n      - app-network\n\n  # ================================\n  # Redis Cache\n  # ================================\n  redis:\n    image: redis:7-alpine\n    container_name: myapp-redis\n    command: redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru\n    ports:\n      - \"6379:6379\"\n    volumes:\n      - redis-data:/data\n    healthcheck:\n      test: [\"CMD\", \"redis-cli\", \"ping\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n    restart: unless-stopped\n    networks:\n      - app-network\n\n  # ================================\n  # Nginx Reverse Proxy\n  # ================================\n  nginx:\n    image: nginx:1.25-alpine\n    container_name: myapp-nginx\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro\n      - ./nginx/conf.d:/etc/nginx/conf.d:ro\n      - ./certbot/conf:/etc/letsencrypt:ro\n      - ./certbot/www:/var/www/certbot:ro\n    depends_on:\n      - api\n    restart: unless-stopped\n    networks:\n      - app-network\n\n# ================================\n# Volumes (persistent data)\n# ================================\nvolumes:\n  pgdata:\n    driver: local\n  redis-data:\n    driver: local\n  uploads:\n    driver: local\n\n# ================================\n# Networks\n# ================================\nnetworks:\n  app-network:\n    driver: bridge\n```\n\n## Development vs Production Compose Files\n\n### Base file (docker-compose.yml)\n\n```yaml\nversion: '3.8'\n\nservices:\n  api:\n    environment:\n      DATABASE_URL: postgresql://myapp:password@postgres:5432/myapp_db\n      REDIS_URL: redis://redis:6379\n    depends_on:\n      - postgres\n      - redis\n    networks:\n      - app-network\n\n  postgres:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_DB: myapp_db\n      POSTGRES_USER: myapp\n      POSTGRES_PASSWORD: password\n    networks:\n      - app-network\n\n  redis:\n    image: redis:7-alpine\n    networks:\n      - app-network\n\nnetworks:\n  app-network:\n```\n\n### Development overrides (docker-compose.dev.yml)\n\n```yaml\nversion: '3.8'\n\nservices:\n  api:\n    build:\n      context: .\n      dockerfile: Dockerfile.dev\n    volumes:\n      - .:/app                  # Mount source for hot reload\n      - /app/node_modules       # Exclude node_modules from mount\n    environment:\n      NODE_ENV: development\n    ports:\n      - \"3000:3000\"\n      - \"9229:9229\"            # Node.js debugger port\n    command: npx nodemon --inspect=0.0.0.0:9229\n\n  postgres:\n    ports:\n      - \"5432:5432\"            # Expose for local DB tools\n    volumes:\n      - pgdata-dev:/var/lib/postgresql/data\n\n  redis:\n    ports:\n      - \"6379:6379\"            # Expose for local Redis tools\n\n  # Additional dev services\n  adminer:\n    image: adminer\n    ports:\n      - \"8080:8080\"            # Database web UI\n    networks:\n      - app-network\n\n  mailhog:\n    image: mailhog/mailhog\n    ports:\n      - \"1025:1025\"            # SMTP\n      - \"8025:8025\"            # Web UI\n    networks:\n      - app-network\n\nvolumes:\n  pgdata-dev:\n```\n\n### Running with overrides\n\n```bash\n# Development (merges both files)\ndocker compose -f docker-compose.yml -f docker-compose.dev.yml up\n\n# Or use the COMPOSE_FILE env var\nexport COMPOSE_FILE=docker-compose.yml:docker-compose.dev.yml\ndocker compose up\n\n# Production\ndocker compose -f docker-compose.yml -f docker-compose.prod.yml up -d\n```\n\n## Docker Compose Commands\n\n```bash\n# Start all services\ndocker compose up                   # Foreground (see all logs)\ndocker compose up -d                # Detached (background)\n\n# Build and start\ndocker compose up --build           # Rebuild images before starting\n\n# Stop services\ndocker compose down                 # Stop and remove containers\ndocker compose down -v              # Also remove volumes (deletes data!)\n\n# View logs\ndocker compose logs                 # All services\ndocker compose logs api             # Specific service\ndocker compose logs -f api          # Follow logs\n\n# Service management\ndocker compose ps                   # List running services\ndocker compose exec api sh          # Shell into running service\ndocker compose run api npm test     # Run one-off command\ndocker compose restart api          # Restart a service\n\n# Scaling\ndocker compose up -d --scale api=3  # Run 3 API instances\n\n# Cleanup\ndocker compose down --rmi all       # Remove images too\ndocker system prune -a              # Nuclear option: clean everything\n```\n\n## Environment Variable Management\n\n```yaml\nservices:\n  api:\n    # Method 1: Inline\n    environment:\n      NODE_ENV: production\n      PORT: 3000\n\n    # Method 2: From .env file\n    env_file:\n      - .env\n      - .env.local             # Override with local values\n\n    # Method 3: From host environment\n    environment:\n      API_KEY: ${API_KEY}      # Must be set on host\n      DEBUG: ${DEBUG:-false}   # Default value if not set\n```\n\n### .env file for Compose\n\n```bash\n# .env (automatically loaded by docker compose)\nCOMPOSE_PROJECT_NAME=myapp\nPOSTGRES_PASSWORD=secretpassword\nNODE_ENV=development\n```\n\n## Health Checks and Dependencies\n\n```yaml\nservices:\n  api:\n    depends_on:\n      postgres:\n        condition: service_healthy    # Wait for healthy status\n      redis:\n        condition: service_started    # Just wait for start\n\n  postgres:\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U myapp\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n      start_period: 30s\n```\n\n## Named Volumes vs Bind Mounts\n\n```yaml\nservices:\n  api:\n    volumes:\n      # Bind mount: host path -> container path\n      - ./src:/app/src              # Development: live code reload\n      - ./nginx.conf:/etc/nginx/nginx.conf:ro  # :ro = read-only\n\n      # Named volume: managed by Docker\n      - uploads:/app/uploads        # Persistent data\n      - /app/node_modules           # Anonymous volume (exclude from bind)\n\nvolumes:\n  uploads:                          # Declare named volumes\n```\n\n## Practical Patterns\n\n### Database Initialization\n\n```sql\n-- init-scripts/01-create-extensions.sql\n-- Files in /docker-entrypoint-initdb.d/ run on first start\nCREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";\nCREATE EXTENSION IF NOT EXISTS \"pgcrypto\";\n```\n\n### Wait-for-it Pattern\n\n```yaml\nservices:\n  api:\n    command: >\n      sh -c \"\n        echo 'Waiting for database...' &&\n        npx prisma migrate deploy &&\n        echo 'Starting server...' &&\n        node dist/server.js\n      \"\n    depends_on:\n      postgres:\n        condition: service_healthy\n```\n\n### Running Migrations\n\n```bash\n# Run migrations as a one-off command\ndocker compose exec api npx prisma migrate deploy\n\n# Or as a separate service\ndocker compose run --rm api npx prisma migrate deploy\n```\n\n## Common Pitfalls\n\n1. **Using `depends_on` without health checks** - `depends_on` alone only waits for container start, not for the service to be ready. Always add health checks.\n2. **Storing secrets in docker-compose.yml** - Use `.env` files (gitignored) or Docker secrets for sensitive data.\n3. **Bind-mounting node_modules** - On macOS/Windows, this destroys performance. Use a named volume for node_modules.\n4. **Forgetting `docker compose down -v` deletes data** - Named volumes persist across `down` but not `down -v`.\n5. **Not using `restart: unless-stopped`** - Without restart policies, containers do not restart after host reboot.\n",
    "intuition": "Docker Compose is like a conductor's sheet music for an orchestra. Each service (instrument) has its own part, but the compose file coordinates them all -- ensuring the database (rhythm section) starts before the API (melody), that they can all hear each other (networking), and that the music is preserved (volumes) even if the orchestra takes a break.",
    "exercises": [
      {
        "title": "Full-Stack Docker Compose",
        "description": "Create a docker-compose.yml for a full-stack application with: Node.js API, PostgreSQL, Redis, and Nginx as reverse proxy. Include health checks for all services, proper dependency ordering, persistent volumes for database and uploads, and a shared network.",
        "hint": "Use depends_on with condition: service_healthy. Map Nginx port 80 to the API. Use pg_isready for Postgres health check and redis-cli ping for Redis."
      },
      {
        "title": "Dev vs Prod Setup",
        "description": "Create a base docker-compose.yml and two override files: docker-compose.dev.yml (with hot reload, debug ports, adminer for DB management) and docker-compose.prod.yml (with resource limits, no exposed DB ports, production env vars). Write a Makefile with 'make dev' and 'make prod' commands.",
        "hint": "Use bind mounts in dev for hot reload. In prod, use 'deploy.resources.limits' for resource constraints. The Makefile should compose the correct override file."
      },
      {
        "title": "Seed and Migrate",
        "description": "Add a migration service to your compose file that runs Prisma migrations and seeds the database on startup, then exits. The API service should wait for the migration service to complete successfully before starting.",
        "hint": "Use a separate service with 'profiles: [migrate]' or depends_on with condition: service_completed_successfully (Compose v2)."
      }
    ],
    "problems": [
      {
        "problem": "API container starts before database is ready, connection refused",
        "cause": "depends_on without health check conditions only waits for container creation, not for the service inside to be ready.",
        "solution": "Add healthcheck to the database service and use `depends_on: postgres: condition: service_healthy`. For PostgreSQL, use `test: [\"CMD-SHELL\", \"pg_isready -U username\"]`.",
        "symptoms": "Error: connect ECONNREFUSED 172.18.0.2:5432"
      },
      {
        "problem": "File changes not reflected in container during development",
        "cause": "The source code is baked into the image at build time. Without bind mounts, changes require a rebuild.",
        "solution": "Add a bind mount in your dev compose file: `volumes: ['.:/app', '/app/node_modules']`. The anonymous volume for node_modules prevents the host mount from overwriting container dependencies. Use nodemon or ts-node-dev for auto-restart."
      },
      {
        "problem": "Docker Compose extremely slow on macOS",
        "cause": "File system sharing between macOS and Linux containers is slow, especially with many files (node_modules).",
        "solution": "Use a named volume for node_modules instead of bind-mounting it. Enable VirtioFS in Docker Desktop settings (much faster than gRPC-FUSE). Consider using Docker's `watch` mode or mounting only `src/` instead of the entire project."
      }
    ],
    "further_reading": [
      {
        "title": "Docker Compose Documentation",
        "url": "https://docs.docker.com/compose/",
        "type": "documentation"
      },
      {
        "title": "Awesome Compose - Docker Examples",
        "url": "https://github.com/docker/awesome-compose",
        "type": "github",
        "note": "Official collection of Docker Compose examples for various stacks"
      },
      {
        "title": "Docker Compose v2 Specification",
        "url": "https://docs.docker.com/compose/compose-file/",
        "type": "documentation"
      },
      {
        "title": "Docker Networking Deep Dive",
        "url": "https://docs.docker.com/network/",
        "type": "documentation"
      }
    ]
  },
  "6.2.3": {
    "body": "# Multi-Stage Builds and Image Optimization\n\n## Why Image Size Matters\n\nEvery megabyte in your Docker image impacts: pull times (especially in CI/CD), storage costs, startup speed, and attack surface. A typical unoptimized Node.js image can be 1GB+. With proper optimization, you can reduce it to under 150MB.\n\n## Image Size Comparison\n\n```\nImage Base               Size (approximate)\n──────────────────────────────────────────\nnode:20                  ~1.1 GB\nnode:20-slim             ~250 MB\nnode:20-alpine           ~140 MB\ndistroless (gcr.io)      ~120 MB\n```\n\n## Multi-Stage Builds In Depth\n\n### The Problem\n\n```dockerfile\n# Single-stage: everything in one image\nFROM node:20-alpine\n\nWORKDIR /app\nCOPY . .\nRUN npm ci                    # Installs ALL dependencies (including dev)\nRUN npm run build             # TypeScript compiler, needed only at build time\n\nCMD [\"node\", \"dist/server.js\"]\n\n# Result: image contains TypeScript, dev tools, test frameworks, etc.\n# None of these are needed at runtime!\n```\n\n### The Solution: Multi-Stage Builds\n\n```dockerfile\n# ============================\n# Stage 1: Dependencies\n# ============================\nFROM node:20-alpine AS deps\n\nWORKDIR /app\nCOPY package.json package-lock.json ./\nRUN npm ci\n\n# ============================\n# Stage 2: Build\n# ============================\nFROM node:20-alpine AS builder\n\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\n\n# Build TypeScript\nRUN npm run build\n\n# Generate Prisma client\nRUN npx prisma generate\n\n# ============================\n# Stage 3: Production dependencies only\n# ============================\nFROM node:20-alpine AS prod-deps\n\nWORKDIR /app\nCOPY package.json package-lock.json ./\nRUN npm ci --production\n\n# ============================\n# Stage 4: Production runtime\n# ============================\nFROM node:20-alpine AS runner\n\nRUN addgroup -g 1001 -S nodejs && \\\n    adduser -S nodejs -u 1001 -G nodejs\n\nRUN apk add --no-cache dumb-init\n\nWORKDIR /app\n\n# Copy ONLY what's needed to run\nCOPY --from=prod-deps --chown=nodejs:nodejs /app/node_modules ./node_modules\nCOPY --from=builder --chown=nodejs:nodejs /app/dist ./dist\nCOPY --from=builder --chown=nodejs:nodejs /app/prisma ./prisma\nCOPY --chown=nodejs:nodejs package.json ./\n\nENV NODE_ENV=production\nUSER nodejs\nEXPOSE 3000\n\nENTRYPOINT [\"dumb-init\", \"--\"]\nCMD [\"node\", \"dist/server.js\"]\n```\n\n### Size Reduction Visualization\n\n```\nSingle Stage Build:\n┌─────────────────────────────────────────┐\n│ node:20-alpine base           (~140 MB) │\n│ ALL node_modules (dev+prod)   (~400 MB) │\n│ TypeScript source              (~5 MB)  │\n│ Test files                     (~2 MB)  │\n│ Build tools config             (~1 MB)  │\n│ Compiled dist/                 (~3 MB)  │\n│ Prisma engine                  (~20 MB) │\n│                               =========  │\n│ Total:                        ~571 MB    │\n└─────────────────────────────────────────┘\n\nMulti-Stage Build:\n┌─────────────────────────────────────────┐\n│ node:20-alpine base           (~140 MB) │\n│ Production node_modules only  (~120 MB) │\n│ Compiled dist/                 (~3 MB)  │\n│ Prisma engine                  (~20 MB) │\n│ dumb-init                      (~1 MB)  │\n│                               =========  │\n│ Total:                        ~284 MB    │\n└─────────────────────────────────────────┘\n\nSavings: ~50% reduction\n```\n\n## Layer Caching Optimization\n\n### Order Matters\n\n```dockerfile\n# SLOW: Any source change invalidates npm ci cache\nCOPY . .\nRUN npm ci\nRUN npm run build\n\n# FAST: npm ci only reruns when package files change\nCOPY package.json package-lock.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n```\n\n### Using BuildKit Cache Mounts\n\n```dockerfile\n# syntax=docker/dockerfile:1\n\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package.json package-lock.json ./\n\n# Cache npm packages between builds\nRUN --mount=type=cache,target=/root/.npm \\\n    npm ci\n\nCOPY . .\nRUN npm run build\n```\n\n```bash\n# Enable BuildKit\nexport DOCKER_BUILDKIT=1\ndocker build .\n\n# Or use docker buildx\ndocker buildx build .\n```\n\n## Alpine-Specific Optimizations\n\n### Handling Native Dependencies\n\n```dockerfile\nFROM node:20-alpine AS builder\n\n# Install build dependencies for native modules\nRUN apk add --no-cache python3 make g++\n\nWORKDIR /app\nCOPY package.json package-lock.json ./\nRUN npm ci\n\n# In production stage, no build tools needed\nFROM node:20-alpine\n# Native modules are already compiled in node_modules\nCOPY --from=builder /app/node_modules ./node_modules\n```\n\n### Avoiding Common Alpine Issues\n\n```dockerfile\n# Problem: some packages need glibc (Alpine uses musl)\n# Solution: Install compatibility layer if needed\nRUN apk add --no-cache libc6-compat\n\n# Problem: timezone data missing in Alpine\nRUN apk add --no-cache tzdata\nENV TZ=UTC\n```\n\n## Advanced Optimization Techniques\n\n### Removing Unnecessary Files from node_modules\n\n```dockerfile\nFROM node:20-alpine AS deps\nWORKDIR /app\nCOPY package.json package-lock.json ./\nRUN npm ci --production\n\n# Remove unnecessary files from node_modules\nRUN find node_modules -name \"*.md\" -delete && \\\n    find node_modules -name \"*.map\" -delete && \\\n    find node_modules -name \"*.ts\" -not -name \"*.d.ts\" -delete && \\\n    find node_modules -name \"LICENSE*\" -delete && \\\n    find node_modules -name \"CHANGELOG*\" -delete && \\\n    rm -rf node_modules/*/{test,tests,__tests__,spec,specs} && \\\n    rm -rf node_modules/*/{example,examples,doc,docs}\n```\n\n### Using node-prune\n\n```dockerfile\nRUN npm ci --production && \\\n    npx node-prune          # Removes test files, docs, etc. from node_modules\n```\n\n### Distroless Images\n\nGoogle's distroless images contain only the runtime -- no shell, no package manager, no OS utilities. Maximum security.\n\n```dockerfile\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY . .\nRUN npm ci && npm run build && npm ci --production\n\n# Distroless: no shell, no package manager, minimal attack surface\nFROM gcr.io/distroless/nodejs20-debian12\nCOPY --from=builder /app/dist /app/dist\nCOPY --from=builder /app/node_modules /app/node_modules\nCOPY --from=builder /app/package.json /app/\nWORKDIR /app\nCMD [\"dist/server.js\"]\n```\n\n## Analyzing Image Layers\n\n```bash\n# See layer sizes\ndocker history myapp:latest\n\n# Use dive for interactive exploration\n# Install: brew install dive (macOS)\ndive myapp:latest\n\n# Output of docker history:\n# IMAGE          CREATED       SIZE      COMMAND\n# abc123         2 min ago     3.2MB     COPY dist/ ./dist/\n# def456         2 min ago     45MB      COPY node_modules ./\n# ghi789         5 min ago     0B        ENV NODE_ENV=production\n# jkl012         10 min ago    142MB     /bin/sh -c #(nop) base image\n```\n\n## Building for Multiple Platforms\n\n```bash\n# Build for both AMD64 and ARM64 (Apple Silicon + Intel + AWS Graviton)\ndocker buildx create --name mybuilder --use\n\ndocker buildx build \\\n  --platform linux/amd64,linux/arm64 \\\n  -t myorg/myapp:latest \\\n  --push .\n```\n\n## CI/CD Cache Optimization\n\n```yaml\n# GitHub Actions with Docker layer caching\n- name: Build and push\n  uses: docker/build-push-action@v5\n  with:\n    context: .\n    push: true\n    tags: myorg/myapp:latest\n    cache-from: type=gha         # GitHub Actions cache\n    cache-to: type=gha,mode=max\n```\n\n## Security Scanning\n\n```bash\n# Scan image for vulnerabilities\ndocker scout cve myapp:latest\n\n# Use Trivy (popular open-source scanner)\ntrivy image myapp:latest\n\n# Snyk\nsnyk container test myapp:latest\n```\n\n## Common Pitfalls\n\n1. **Not using multi-stage builds** - The single biggest optimization. Separating build from runtime can halve your image size.\n2. **Installing dev dependencies in production stage** - Always use `npm ci --production` or separate dependency stages.\n3. **Cache-busting with COPY . .** - Place frequently changing operations (COPY source) after rarely changing ones (npm ci).\n4. **Not scanning for vulnerabilities** - Alpine images are smaller but can still have CVEs. Scan regularly.\n5. **Ignoring build context size** - A large `.dockerignore`-less context slows down every build. Always check what is being sent.\n",
    "intuition": "Multi-stage builds are like building a house. You need scaffolding, cranes, and power tools during construction (build stage), but once the house is finished, you remove all the construction equipment. The final image is just the house itself (runtime), not the entire construction site. Each stage is a clean room where you pick only what you need for the next stage.",
    "exercises": [
      {
        "title": "Size Optimization Challenge",
        "description": "Start with a Dockerfile using node:20 (Debian) with a single stage. Measure its size. Then optimize in steps: (1) switch to Alpine, (2) add .dockerignore, (3) implement multi-stage build, (4) prune node_modules. Document the size at each step and calculate total reduction.",
        "hint": "Use 'docker images --format \"table {{.Repository}}\\t{{.Tag}}\\t{{.Size}}\"' to compare. Tag each iteration differently."
      },
      {
        "title": "BuildKit Cache Mounts",
        "description": "Modify your Dockerfile to use BuildKit cache mounts for npm. Benchmark build times with and without cache mounts by building, making a small change to package.json (add a comment), and rebuilding.",
        "hint": "Add '# syntax=docker/dockerfile:1' at the top. Use 'RUN --mount=type=cache,target=/root/.npm npm ci'. Enable with DOCKER_BUILDKIT=1."
      },
      {
        "title": "Security Audit",
        "description": "Build your optimized image, then scan it with 'docker scout' or Trivy. Fix any HIGH or CRITICAL vulnerabilities by updating base images or packages. Document the before/after vulnerability count.",
        "hint": "Install trivy with brew. Run 'trivy image myapp:latest'. Often, simply updating the Alpine base image or specific npm packages resolves CVEs."
      }
    ],
    "problems": [
      {
        "problem": "Multi-stage build fails: file not found in COPY --from",
        "cause": "The file path in the builder stage does not match what you are trying to copy, or the build step did not produce output.",
        "solution": "Add a debug step in the builder stage: `RUN ls -la /app/dist/`. Ensure the build command actually produces output in the expected directory. Check that WORKDIR is set correctly in both stages."
      },
      {
        "problem": "Native npm packages crash in Alpine production stage",
        "cause": "Native modules (bcrypt, sharp, etc.) compiled in the builder stage may link against libraries not present in the production stage.",
        "solution": "Ensure the production stage has necessary runtime libraries: `RUN apk add --no-cache libc6-compat`. For sharp, add `vips-dev`. Better yet, use pure-JS alternatives (bcryptjs) or install native deps in the production stage too."
      },
      {
        "problem": "Build takes forever despite 'no changes'",
        "cause": "Docker build context is too large (node_modules, .git, etc. being sent to daemon), or cache is not being utilized due to layer ordering.",
        "solution": "Check .dockerignore is properly excluding node_modules and .git. Verify layer order: package.json copy and npm ci should come BEFORE source code copy. Use `docker build --progress=plain .` to see which layers are rebuilding."
      }
    ],
    "further_reading": [
      {
        "title": "Multi-Stage Builds - Docker Docs",
        "url": "https://docs.docker.com/build/building/multi-stage/",
        "type": "documentation"
      },
      {
        "title": "Dive - Docker Image Layer Explorer",
        "url": "https://github.com/wagoodman/dive",
        "type": "tool"
      },
      {
        "title": "Google Distroless Images",
        "url": "https://github.com/GoogleContainerTools/distroless",
        "type": "github"
      },
      {
        "title": "Docker BuildKit Documentation",
        "url": "https://docs.docker.com/build/buildkit/",
        "type": "documentation"
      }
    ]
  },
  "6.2.4": {
    "body": "# Docker Networking and Volumes\n\n## Docker Networking\n\nNetworking is how containers communicate with each other, the host, and the outside world. Understanding Docker networking is critical for building multi-container applications.\n\n### Network Types\n\n```\n┌────────────────────────────────────────────────────────────┐\n│                     Docker Host                             │\n│                                                            │\n│  Bridge Network (default)          Host Network            │\n│  ┌─────────────────────┐          ┌──────────────┐        │\n│  │  172.17.0.0/16      │          │ Shares host   │        │\n│  │  ┌─────┐ ┌─────┐   │          │ network stack │        │\n│  │  │:3000│ │:5432│   │          │ ┌─────┐      │        │\n│  │  │ api │ │ db  │   │          │ │:3000│      │        │\n│  │  └─────┘ └─────┘   │          │ │ api │      │        │\n│  └─────────────────────┘          │ └─────┘      │        │\n│                                    └──────────────┘        │\n│  None Network                                              │\n│  ┌─────────────────────┐                                   │\n│  │  No network access   │                                   │\n│  │  ┌─────┐            │                                   │\n│  │  │batch│ (isolated) │                                   │\n│  │  └─────┘            │                                   │\n│  └─────────────────────┘                                   │\n└────────────────────────────────────────────────────────────┘\n```\n\n### Bridge Networks (Default)\n\n```bash\n# Create a custom bridge network\ndocker network create app-network\n\n# Run containers on the network\ndocker run -d --name api --network app-network myapp\ndocker run -d --name postgres --network app-network postgres:16\n\n# Inside the 'api' container, connect to postgres using container name:\n# postgresql://user:pass@postgres:5432/mydb\n# Docker's embedded DNS resolves 'postgres' to the container's IP\n\n# List networks\ndocker network ls\n\n# Inspect a network\ndocker network inspect app-network\n\n# Connect/disconnect running container\ndocker network connect app-network existing-container\ndocker network disconnect app-network existing-container\n```\n\n### Why Custom Bridge Networks?\n\n```\nDefault bridge:\n- Containers communicate by IP only (no DNS)\n- All containers on same network (no isolation)\n- Must use --link (deprecated)\n\nCustom bridge:\n- Automatic DNS resolution by container name\n- Network isolation between different apps\n- Better security\n```\n\n### Host Network (Linux only)\n\n```bash\n# Container shares host's network stack\ndocker run --network host myapp\n\n# No port mapping needed - container uses host ports directly\n# Pros: Best performance (no NAT overhead)\n# Cons: Port conflicts possible, less isolation\n```\n\n### Port Mapping\n\n```bash\n# Map host port to container port\ndocker run -p 3000:3000 myapp          # host:container\ndocker run -p 8080:3000 myapp          # Different host port\ndocker run -p 127.0.0.1:3000:3000 myapp  # Bind to localhost only\ndocker run -P myapp                      # Random host port for all EXPOSE'd ports\n```\n\n### Container-to-Container Communication\n\n```\nWith Docker Compose (recommended):\n┌──────────────────────────────────────────┐\n│  app-network (auto-created by compose)   │\n│                                          │\n│  ┌─────────┐       ┌──────────┐        │\n│  │   api    │──────►│ postgres  │        │\n│  │          │ DNS:  │          │        │\n│  │          │\"postgres:5432\"   │        │\n│  └─────────┘       └──────────┘        │\n│       │                                  │\n│       │             ┌──────────┐        │\n│       └────────────►│  redis   │        │\n│             DNS:    │          │        │\n│         \"redis:6379\"│          │        │\n│                     └──────────┘        │\n└──────────────────────────────────────────┘\n```\n\n```javascript\n// In your Node.js app (inside Docker):\nconst dbConfig = {\n  host: 'postgres',     // Container name = hostname\n  port: 5432,\n  database: 'myapp_db',\n};\n\nconst redisConfig = {\n  host: 'redis',        // Container name = hostname\n  port: 6379,\n};\n```\n\n## Docker Volumes\n\nVolumes persist data beyond container lifecycles. Without volumes, all data inside a container is lost when the container is removed.\n\n### Volume Types\n\n```\nNamed Volumes:         Bind Mounts:           tmpfs:\n┌──────────┐          ┌──────────┐          ┌──────────┐\n│ Container │          │ Container │          │ Container │\n│ /data     │          │ /app/src  │          │ /tmp      │\n└────┬─────┘          └────┬─────┘          └────┬─────┘\n     │                      │                      │\n┌────┴──────────┐    ┌────┴──────────┐    ┌────┴──────────┐\n│ Docker manages│    │ Host filesystem│    │ RAM only      │\n│ /var/lib/     │    │ ./src on host  │    │ (no disk)     │\n│ docker/volumes│    │                │    │               │\n└───────────────┘    └───────────────┘    └───────────────┘\nBest for: data      Best for: dev code   Best for: secrets\n```\n\n### Named Volumes\n\n```bash\n# Create a volume\ndocker volume create pgdata\n\n# Use in a container\ndocker run -v pgdata:/var/lib/postgresql/data postgres:16\n\n# List volumes\ndocker volume ls\n\n# Inspect volume\ndocker volume inspect pgdata\n\n# Remove volume\ndocker volume rm pgdata\n\n# Remove all unused volumes\ndocker volume prune\n```\n\n### Bind Mounts\n\n```bash\n# Mount host directory into container\ndocker run -v $(pwd)/src:/app/src myapp\n\n# Read-only mount\ndocker run -v $(pwd)/config:/app/config:ro myapp\n\n# Mount single file\ndocker run -v $(pwd)/nginx.conf:/etc/nginx/nginx.conf:ro nginx\n```\n\n### Volume Patterns in Docker Compose\n\n```yaml\nversion: '3.8'\n\nservices:\n  api:\n    volumes:\n      # Development: bind mount for hot reload\n      - ./src:/app/src\n      # Exclude node_modules from bind mount\n      - /app/node_modules\n      # Named volume for uploads\n      - uploads:/app/uploads\n      # Read-only config\n      - ./config/production.json:/app/config/production.json:ro\n\n  postgres:\n    volumes:\n      # Named volume for database data (persistent)\n      - pgdata:/var/lib/postgresql/data\n      # Init scripts (bind mount, read-only)\n      - ./db/init:/docker-entrypoint-initdb.d:ro\n\n  redis:\n    volumes:\n      - redis-data:/data\n\nvolumes:\n  pgdata:\n    driver: local\n  redis-data:\n    driver: local\n  uploads:\n    driver: local\n```\n\n### Backup and Restore Volumes\n\n```bash\n# Backup a named volume to a tar file\ndocker run --rm \\\n  -v pgdata:/source:ro \\\n  -v $(pwd):/backup \\\n  alpine tar czf /backup/pgdata-backup.tar.gz -C /source .\n\n# Restore from backup\ndocker run --rm \\\n  -v pgdata:/target \\\n  -v $(pwd):/backup \\\n  alpine sh -c \"cd /target && tar xzf /backup/pgdata-backup.tar.gz\"\n```\n\n## Practical Networking Scenarios\n\n### Multiple Environments on One Host\n\n```yaml\n# Project A\n# docker-compose.yml\nservices:\n  api:\n    networks:\n      - project-a-net\n  postgres:\n    networks:\n      - project-a-net\nnetworks:\n  project-a-net:\n\n# Project B (separate compose file, isolated network)\nservices:\n  api:\n    networks:\n      - project-b-net\n  postgres:\n    networks:\n      - project-b-net\nnetworks:\n  project-b-net:\n\n# Projects are completely isolated from each other\n```\n\n### Connecting to Host Services\n\n```yaml\nservices:\n  api:\n    # Docker Desktop (Mac/Windows):\n    extra_hosts:\n      - \"host.docker.internal:host-gateway\"\n    environment:\n      # Connect to PostgreSQL running on host machine\n      DATABASE_URL: postgresql://user:pass@host.docker.internal:5432/mydb\n```\n\n### Exposing Services Safely\n\n```yaml\nservices:\n  postgres:\n    ports:\n      # BAD: accessible from any network interface\n      - \"5432:5432\"\n\n      # GOOD: only accessible from localhost\n      - \"127.0.0.1:5432:5432\"\n\n  api:\n    ports:\n      # Public-facing service\n      - \"3000:3000\"\n```\n\n## Debugging Network Issues\n\n```bash\n# Check container's network settings\ndocker inspect --format='{{json .NetworkSettings.Networks}}' myapp | jq\n\n# Test connectivity between containers\ndocker exec api ping postgres\ndocker exec api nc -zv postgres 5432\n\n# Check DNS resolution\ndocker exec api nslookup postgres\n\n# View container logs for connection errors\ndocker logs api 2>&1 | grep -i \"connect\\|ECONNREFUSED\\|timeout\"\n\n# List port mappings\ndocker port myapp\n\n# Check which containers are on a network\ndocker network inspect app-network --format='{{range .Containers}}{{.Name}} {{end}}'\n```\n\n## Common Pitfalls\n\n1. **Using the default bridge network** - Default bridge does not support DNS. Always create custom bridge networks.\n2. **Hardcoding container IPs** - Container IPs change on restart. Always use container names for DNS resolution.\n3. **Exposing database ports to 0.0.0.0** - Bind to 127.0.0.1 to prevent external access: `127.0.0.1:5432:5432`.\n4. **Not understanding volume persistence** - `docker compose down` keeps volumes. `docker compose down -v` deletes them. Know the difference.\n5. **Bind mounting over important container paths** - Mounting an empty host directory over `/app/node_modules` effectively deletes all modules inside the container.\n",
    "intuition": "Docker networks are like private phone lines between containers -- they can call each other by name (DNS), but outsiders cannot listen in. Volumes are like external hard drives that you plug into containers: the data on the drive persists even if you throw away the container (computer) and plug the drive into a new one.",
    "exercises": [
      {
        "title": "Network Isolation Experiment",
        "description": "Create two separate Docker networks (frontend-net and backend-net). Run an Nginx container on frontend-net, a Node.js API on both networks, and PostgreSQL on backend-net only. Verify that Nginx can reach the API but NOT the database directly.",
        "hint": "Use docker network create for each network. Connect the API container to both networks. Use docker exec to test connectivity with ping or nc."
      },
      {
        "title": "Volume Backup Pipeline",
        "description": "Create a script that backs up a PostgreSQL Docker volume to a timestamped tar.gz file, uploads it to a local backup directory, and prunes backups older than 7 days. Test it by creating data, backing up, destroying the volume, and restoring.",
        "hint": "Use a temporary Alpine container to access the volume and create the tar. For restore, create a fresh volume and extract the tar into it."
      },
      {
        "title": "Debug a Networking Problem",
        "description": "Set up a Docker Compose file with an intentional networking bug (e.g., services on different networks, wrong hostname, missing port exposure). Document the symptoms and walk through the debugging process using docker inspect, docker exec, and docker logs.",
        "hint": "Common bugs to introduce: using 'localhost' instead of service name, putting services on different networks, forgetting to expose the right port."
      }
    ],
    "problems": [
      {
        "problem": "Container cannot resolve hostname of another container",
        "cause": "The containers are on different networks or using the default bridge network (which does not support DNS).",
        "solution": "Ensure both containers are on the same custom bridge network. In Docker Compose, services on the same compose file share a default network automatically. For manual docker run, use `--network mynetwork` on both containers."
      },
      {
        "problem": "Data lost after docker compose down",
        "cause": "You used `docker compose down -v` which removes named volumes, or you did not declare a named volume (used anonymous volume instead).",
        "solution": "Always declare named volumes in the `volumes:` section of docker-compose.yml. Use `docker compose down` (without -v) to preserve data. Back up volumes regularly for critical data."
      },
      {
        "problem": "Port already allocated error when starting container",
        "cause": "Another process (or another container) is already using the host port you are trying to map.",
        "solution": "Find what is using the port: `lsof -i :3000` or `docker ps` to check running containers. Either stop the conflicting process or map to a different host port: `-p 3001:3000`.",
        "symptoms": "Error: Bind for 0.0.0.0:3000 failed: port is already allocated"
      }
    ],
    "further_reading": [
      {
        "title": "Docker Networking Overview",
        "url": "https://docs.docker.com/network/",
        "type": "documentation"
      },
      {
        "title": "Manage Data in Docker",
        "url": "https://docs.docker.com/storage/",
        "type": "documentation"
      },
      {
        "title": "Docker Network Drivers",
        "url": "https://docs.docker.com/network/drivers/",
        "type": "documentation"
      },
      {
        "title": "Container Networking From Scratch",
        "url": "https://labs.iximiuz.com/tutorials/container-networking-from-scratch",
        "type": "tutorial"
      }
    ]
  },
  "6.3.1": {
    "body": "# GitHub Actions: Workflows, Jobs, and Steps\n\n## What is GitHub Actions?\n\nGitHub Actions is a CI/CD platform built into GitHub. It lets you automate your build, test, and deployment pipeline directly from your repository. Every push, pull request, or scheduled event can trigger automated workflows.\n\n## Architecture\n\n```\nGitHub Event (push, PR, schedule, manual)\n         │\n         ▼\n┌─────────────────────────────────────────┐\n│              Workflow                     │\n│  (.github/workflows/ci.yml)              │\n│                                          │\n│  ┌──────────────┐  ┌──────────────┐     │\n│  │    Job 1      │  │    Job 2      │     │\n│  │   (build)     │  │   (deploy)    │     │\n│  │               │  │               │     │\n│  │  ┌─────────┐ │  │  ┌─────────┐ │     │\n│  │  │ Step 1  │ │  │  │ Step 1  │ │     │\n│  │  │checkout │ │  │  │ setup   │ │     │\n│  │  └─────────┘ │  │  └─────────┘ │     │\n│  │  ┌─────────┐ │  │  ┌─────────┐ │     │\n│  │  │ Step 2  │ │  │  │ Step 2  │ │     │\n│  │  │npm ci   │ │  │  │ deploy  │ │     │\n│  │  └─────────┘ │  │  └─────────┘ │     │\n│  │  ┌─────────┐ │  │               │     │\n│  │  │ Step 3  │ │  │   needs:      │     │\n│  │  │npm test │ │  │   [build]     │     │\n│  │  └─────────┘ │  │               │     │\n│  └──────────────┘  └──────────────┘     │\n│                                          │\n│  Runner: ubuntu-latest                   │\n└─────────────────────────────────────────┘\n```\n\n## Your First Workflow\n\n```yaml\n# .github/workflows/ci.yml\nname: CI Pipeline\n\n# When to run\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n    branches: [main]\n\n# Environment variables for all jobs\nenv:\n  NODE_VERSION: '20'\n\njobs:\n  # ================================\n  # Build and Test\n  # ================================\n  build-and-test:\n    name: Build & Test\n    runs-on: ubuntu-latest\n\n    # Service containers (databases for testing)\n    services:\n      postgres:\n        image: postgres:16-alpine\n        env:\n          POSTGRES_DB: test_db\n          POSTGRES_USER: test_user\n          POSTGRES_PASSWORD: test_password\n        ports:\n          - 5432:5432\n        options: >-\n          --health-cmd pg_isready\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n\n      redis:\n        image: redis:7-alpine\n        ports:\n          - 6379:6379\n        options: >-\n          --health-cmd \"redis-cli ping\"\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n\n    steps:\n      # Checkout code\n      - name: Checkout repository\n        uses: actions/checkout@v4\n\n      # Setup Node.js with caching\n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: 'npm'\n\n      # Install dependencies\n      - name: Install dependencies\n        run: npm ci\n\n      # Lint\n      - name: Lint code\n        run: npm run lint\n\n      # Type check\n      - name: Type check\n        run: npx tsc --noEmit\n\n      # Run tests\n      - name: Run tests\n        run: npm test -- --coverage\n        env:\n          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db\n          REDIS_URL: redis://localhost:6379\n          NODE_ENV: test\n\n      # Upload coverage\n      - name: Upload coverage\n        uses: actions/upload-artifact@v4\n        with:\n          name: coverage-report\n          path: coverage/\n          retention-days: 7\n\n      # Build\n      - name: Build\n        run: npm run build\n\n      # Upload build artifact\n      - name: Upload build\n        uses: actions/upload-artifact@v4\n        with:\n          name: dist\n          path: dist/\n```\n\n## Workflow Triggers (on:)\n\n```yaml\non:\n  # Push to specific branches\n  push:\n    branches: [main, develop]\n    paths:\n      - 'src/**'               # Only when source changes\n      - 'package.json'\n    paths-ignore:\n      - '**.md'                # Ignore docs changes\n      - '.github/**'\n\n  # Pull requests\n  pull_request:\n    branches: [main]\n    types: [opened, synchronize, reopened]\n\n  # Scheduled (cron)\n  schedule:\n    - cron: '0 2 * * *'       # Daily at 2 AM UTC\n\n  # Manual trigger\n  workflow_dispatch:\n    inputs:\n      environment:\n        description: 'Target environment'\n        required: true\n        default: 'staging'\n        type: choice\n        options:\n          - staging\n          - production\n\n  # After another workflow\n  workflow_run:\n    workflows: [\"CI Pipeline\"]\n    types: [completed]\n\n  # Release published\n  release:\n    types: [published]\n```\n\n## Job Dependencies and Matrix\n\n```yaml\njobs:\n  lint:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run lint\n\n  test:\n    needs: lint              # Runs after lint succeeds\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        node-version: [18, 20, 22]    # Test on multiple versions\n        os: [ubuntu-latest]\n      fail-fast: false        # Don't cancel other matrix jobs on failure\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ matrix.node-version }}\n          cache: 'npm'\n      - run: npm ci\n      - run: npm test\n\n  deploy:\n    needs: [lint, test]      # Runs after BOTH succeed\n    if: github.ref == 'refs/heads/main'  # Only on main branch\n    runs-on: ubuntu-latest\n    steps:\n      - name: Deploy\n        run: echo \"Deploying...\"\n```\n\n## Secrets and Environment Variables\n\n```yaml\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment: production    # Links to GitHub environment\n    steps:\n      - name: Deploy to server\n        env:\n          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}\n          DATABASE_URL: ${{ secrets.DATABASE_URL }}\n          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}\n        run: |\n          echo \"$SSH_KEY\" > key.pem\n          chmod 600 key.pem\n          ssh -i key.pem deploy@${{ secrets.SERVER_IP }} \"cd /var/www/app && ./deploy.sh\"\n```\n\n### Setting Up Secrets\n\n1. Go to Repository Settings > Secrets and variables > Actions\n2. Click \"New repository secret\"\n3. Add name (e.g., `DATABASE_URL`) and value\n\n### Environments with Protection Rules\n\n1. Go to Repository Settings > Environments\n2. Create environments: `staging`, `production`\n3. Add protection rules: required reviewers, wait timer, branch restrictions\n\n## Caching\n\n```yaml\n# Automatic caching with setup-node\n- uses: actions/setup-node@v4\n  with:\n    node-version: '20'\n    cache: 'npm'           # Caches ~/.npm\n\n# Manual caching (for other things)\n- name: Cache Prisma engines\n  uses: actions/cache@v4\n  with:\n    path: node_modules/.prisma\n    key: prisma-${{ hashFiles('prisma/schema.prisma') }}\n\n# Cache Docker layers\n- name: Cache Docker layers\n  uses: actions/cache@v4\n  with:\n    path: /tmp/.buildx-cache\n    key: docker-${{ hashFiles('Dockerfile') }}\n    restore-keys: docker-\n```\n\n## Reusable Workflows\n\n```yaml\n# .github/workflows/reusable-test.yml\nname: Reusable Test Workflow\n\non:\n  workflow_call:\n    inputs:\n      node-version:\n        required: false\n        type: string\n        default: '20'\n    secrets:\n      DATABASE_URL:\n        required: true\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ inputs.node-version }}\n          cache: 'npm'\n      - run: npm ci\n      - run: npm test\n        env:\n          DATABASE_URL: ${{ secrets.DATABASE_URL }}\n```\n\n```yaml\n# .github/workflows/ci.yml - Calling the reusable workflow\nname: CI\non: [push]\n\njobs:\n  test:\n    uses: ./.github/workflows/reusable-test.yml\n    with:\n      node-version: '20'\n    secrets:\n      DATABASE_URL: ${{ secrets.DATABASE_URL }}\n```\n\n## Composite Actions\n\n```yaml\n# .github/actions/setup-project/action.yml\nname: Setup Project\ndescription: Install Node.js and dependencies\n\ninputs:\n  node-version:\n    description: 'Node.js version'\n    required: false\n    default: '20'\n\nruns:\n  using: 'composite'\n  steps:\n    - uses: actions/setup-node@v4\n      with:\n        node-version: ${{ inputs.node-version }}\n        cache: 'npm'\n    - run: npm ci\n      shell: bash\n    - run: npx prisma generate\n      shell: bash\n```\n\n```yaml\n# Usage in a workflow\nsteps:\n  - uses: actions/checkout@v4\n  - uses: ./.github/actions/setup-project\n    with:\n      node-version: '20'\n  - run: npm test\n```\n\n## Common Pitfalls\n\n1. **Not caching dependencies** - Without caching, npm ci runs on every build (~30-60 seconds). Always use setup-node's cache feature.\n2. **Running everything in one job** - Split lint, test, and build into separate jobs so failures are clear and parallel execution is possible.\n3. **Hardcoding secrets in workflows** - Never put secrets in YAML. Always use `${{ secrets.NAME }}`.\n4. **Not using `fail-fast: false` in matrix** - By default, if one matrix job fails, all are cancelled. Set false to see all results.\n5. **Ignoring workflow run time** - Free GitHub Actions minutes are limited. Optimize with caching, conditional jobs, and path filters.\n",
    "intuition": "GitHub Actions is like setting up dominoes in your repository. When someone pushes code (tips the first domino), a chain reaction begins: checkout the code, install dependencies, run tests, build, and deploy. Each domino (step) must fall successfully for the next one to trigger. If any domino fails, the chain stops and you are notified.",
    "exercises": [
      {
        "title": "Complete CI Pipeline",
        "description": "Create a GitHub Actions workflow that: (1) triggers on push to main and PRs, (2) sets up Node.js with npm caching, (3) runs lint, type-check, and tests in parallel jobs, (4) uses PostgreSQL and Redis service containers for integration tests, (5) uploads test coverage as an artifact.",
        "hint": "Use the 'services' key for database containers. Set health checks so the job waits for services to be ready. Use 'needs' for job dependencies."
      },
      {
        "title": "Matrix Testing",
        "description": "Create a workflow that tests your application across Node.js versions 18, 20, and 22 using a matrix strategy. Each should run independently (fail-fast: false). Add a summary step that reports which versions passed.",
        "hint": "Use strategy.matrix.node-version. Reference the version with ${{ matrix.node-version }}."
      },
      {
        "title": "Reusable Workflow",
        "description": "Extract the test job into a reusable workflow that accepts node-version as input and DATABASE_URL as a secret. Call it from both a CI workflow and a nightly scheduled workflow.",
        "hint": "Use workflow_call trigger for the reusable workflow. Use 'uses: ./.github/workflows/reusable.yml' to call it."
      }
    ],
    "problems": [
      {
        "problem": "Tests pass locally but fail in GitHub Actions",
        "cause": "Environment differences: different Node.js version, missing environment variables, different OS (Ubuntu vs macOS), or timing-dependent tests.",
        "solution": "Pin the Node.js version to match local. Set all required environment variables in the workflow. For timing issues, increase timeouts or use retries. Add `--verbose` flags to test commands for better debugging output."
      },
      {
        "problem": "Workflow runs are slow (10+ minutes)",
        "cause": "No dependency caching, unnecessary steps, sequential jobs that could be parallel, or large Docker image builds.",
        "solution": "Enable npm caching with setup-node. Run independent jobs in parallel. Use path filters to skip CI on doc-only changes. Cache Docker layers with actions/cache. Use the 'continue-on-error' strategy with matrix builds."
      },
      {
        "problem": "Service container (PostgreSQL) not ready when tests start",
        "cause": "The service container started but PostgreSQL inside it is still initializing.",
        "solution": "Add health check options to the service definition: `options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5`. GitHub Actions will wait for the health check to pass before starting job steps."
      }
    ],
    "further_reading": [
      {
        "title": "GitHub Actions Documentation",
        "url": "https://docs.github.com/en/actions",
        "type": "documentation"
      },
      {
        "title": "GitHub Actions Marketplace",
        "url": "https://github.com/marketplace?type=actions",
        "type": "tool"
      },
      {
        "title": "GitHub Actions Cheat Sheet",
        "url": "https://github.github.io/actions-cheat-sheet/actions-cheat-sheet.html",
        "type": "reference"
      },
      {
        "title": "Awesome GitHub Actions",
        "url": "https://github.com/sdras/awesome-actions",
        "type": "github"
      }
    ]
  },
  "6.3.2": {
    "body": "# Testing Pipelines: Lint, Type-Check, Test, Coverage\n\n## The Quality Gate Pipeline\n\nA testing pipeline enforces code quality at every push. It acts as an automated code reviewer that catches bugs, style issues, type errors, and regressions before they reach production.\n\n```\nPull Request\n    │\n    ▼\n┌─────────┐   ┌────────────┐   ┌─────────┐   ┌──────────┐   ┌────────┐\n│  Lint   │──►│ Type Check │──►│  Unit   │──►│Integration│──►│Coverage│\n│ ESLint  │   │    tsc     │   │  Tests  │   │  Tests    │   │ Report │\n│ Prettier│   │            │   │  Jest   │   │  Jest     │   │        │\n└─────────┘   └────────────┘   └─────────┘   └──────────┘   └────────┘\n    │              │                │              │              │\n    ▼              ▼                ▼              ▼              ▼\n  FAIL?          FAIL?           FAIL?          FAIL?        < 80%?\n  Block PR       Block PR       Block PR       Block PR      Warn\n```\n\n## Complete Testing Pipeline\n\n```yaml\n# .github/workflows/test.yml\nname: Test Pipeline\n\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n    branches: [main]\n\nenv:\n  NODE_VERSION: '20'\n\njobs:\n  # ================================\n  # Stage 1: Code Quality (fast)\n  # ================================\n  lint:\n    name: Lint & Format\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: 'npm'\n\n      - run: npm ci\n\n      # ESLint\n      - name: Run ESLint\n        run: npx eslint . --format=json --output-file=eslint-report.json\n        continue-on-error: true\n\n      - name: Annotate ESLint results\n        if: always()\n        uses: ataylorme/eslint-annotate-action@v3\n        with:\n          report-json: eslint-report.json\n\n      # Prettier\n      - name: Check formatting\n        run: npx prettier --check \"src/**/*.{ts,js,json}\"\n\n  typecheck:\n    name: Type Check\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: 'npm'\n      - run: npm ci\n      - run: npx prisma generate     # Generate types\n      - name: TypeScript check\n        run: npx tsc --noEmit --pretty\n\n  # ================================\n  # Stage 2: Unit Tests (medium)\n  # ================================\n  unit-tests:\n    name: Unit Tests\n    needs: [lint, typecheck]\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: 'npm'\n      - run: npm ci\n      - run: npx prisma generate\n\n      - name: Run unit tests\n        run: npx jest --testPathPattern='unit' --coverage --coverageReporters=json-summary --coverageReporters=text\n        env:\n          NODE_ENV: test\n\n      - name: Upload unit test coverage\n        uses: actions/upload-artifact@v4\n        with:\n          name: unit-coverage\n          path: coverage/\n\n  # ================================\n  # Stage 3: Integration Tests (slow)\n  # ================================\n  integration-tests:\n    name: Integration Tests\n    needs: [lint, typecheck]\n    runs-on: ubuntu-latest\n    services:\n      postgres:\n        image: postgres:16-alpine\n        env:\n          POSTGRES_DB: test_db\n          POSTGRES_USER: test_user\n          POSTGRES_PASSWORD: test_pass\n        ports:\n          - 5432:5432\n        options: >-\n          --health-cmd pg_isready\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n      redis:\n        image: redis:7-alpine\n        ports:\n          - 6379:6379\n        options: >-\n          --health-cmd \"redis-cli ping\"\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: 'npm'\n      - run: npm ci\n\n      - name: Setup database\n        run: npx prisma migrate deploy\n        env:\n          DATABASE_URL: postgresql://test_user:test_pass@localhost:5432/test_db\n\n      - name: Seed test data\n        run: npx prisma db seed\n        env:\n          DATABASE_URL: postgresql://test_user:test_pass@localhost:5432/test_db\n\n      - name: Run integration tests\n        run: npx jest --testPathPattern='integration' --coverage --forceExit\n        env:\n          DATABASE_URL: postgresql://test_user:test_pass@localhost:5432/test_db\n          REDIS_URL: redis://localhost:6379\n          NODE_ENV: test\n\n  # ================================\n  # Stage 4: Coverage Report\n  # ================================\n  coverage:\n    name: Coverage Report\n    needs: [unit-tests, integration-tests]\n    runs-on: ubuntu-latest\n    if: github.event_name == 'pull_request'\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Download unit coverage\n        uses: actions/download-artifact@v4\n        with:\n          name: unit-coverage\n          path: coverage/\n\n      - name: Coverage threshold check\n        run: |\n          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')\n          echo \"Line coverage: ${COVERAGE}%\"\n          if (( $(echo \"$COVERAGE < 80\" | bc -l) )); then\n            echo \"::error::Coverage ${COVERAGE}% is below 80% threshold\"\n            exit 1\n          fi\n\n      - name: Comment coverage on PR\n        uses: marocchino/sticky-pull-request-comment@v2\n        with:\n          header: coverage\n          message: |\n            ## Test Coverage Report\n            | Metric | Coverage |\n            |--------|----------|\n            | Lines  | ${{ env.LINE_COV }}% |\n            | Branch | ${{ env.BRANCH_COV }}% |\n```\n\n## ESLint Configuration for CI\n\n```javascript\n// eslint.config.js (flat config, ESLint 9+)\nimport eslint from '@eslint/js';\nimport tseslint from 'typescript-eslint';\n\nexport default tseslint.config(\n  eslint.configs.recommended,\n  ...tseslint.configs.recommended,\n  {\n    rules: {\n      '@typescript-eslint/no-unused-vars': 'error',\n      '@typescript-eslint/explicit-function-return-type': 'warn',\n      '@typescript-eslint/no-explicit-any': 'warn',\n      'no-console': ['warn', { allow: ['warn', 'error'] }],\n    },\n  },\n  {\n    ignores: ['dist/', 'node_modules/', 'coverage/'],\n  }\n);\n```\n\n## Jest Configuration for CI\n\n```javascript\n// jest.config.ts\nimport type { Config } from 'jest';\n\nconst config: Config = {\n  preset: 'ts-jest',\n  testEnvironment: 'node',\n  roots: ['<rootDir>/src'],\n  moduleNameMapper: {\n    '^@/(.*)$': '<rootDir>/src/$1',\n  },\n  coverageDirectory: 'coverage',\n  coveragePathIgnorePatterns: [\n    '/node_modules/',\n    '/dist/',\n    '/__tests__/',\n    '/test/',\n  ],\n  coverageThreshold: {\n    global: {\n      branches: 70,\n      functions: 80,\n      lines: 80,\n      statements: 80,\n    },\n  },\n  projects: [\n    {\n      displayName: 'unit',\n      testMatch: ['**/*.unit.test.ts'],\n      testEnvironment: 'node',\n    },\n    {\n      displayName: 'integration',\n      testMatch: ['**/*.integration.test.ts'],\n      testEnvironment: 'node',\n      globalSetup: '<rootDir>/test/setup.ts',\n      globalTeardown: '<rootDir>/test/teardown.ts',\n    },\n  ],\n};\n\nexport default config;\n```\n\n## Pre-Commit Hooks with Husky + lint-staged\n\n```json\n// package.json\n{\n  \"scripts\": {\n    \"prepare\": \"husky\",\n    \"lint\": \"eslint .\",\n    \"lint:fix\": \"eslint . --fix\",\n    \"format\": \"prettier --write 'src/**/*.{ts,js,json}'\",\n    \"typecheck\": \"tsc --noEmit\"\n  },\n  \"lint-staged\": {\n    \"*.{ts,js}\": [\n      \"eslint --fix\",\n      \"prettier --write\"\n    ],\n    \"*.{json,md}\": [\n      \"prettier --write\"\n    ]\n  }\n}\n```\n\n```bash\n# Setup husky\nnpx husky init\n\n# .husky/pre-commit\nnpx lint-staged\n\n# .husky/pre-push\nnpm run typecheck && npm test\n```\n\n## Package Scripts Setup\n\n```json\n{\n  \"scripts\": {\n    \"lint\": \"eslint .\",\n    \"lint:fix\": \"eslint . --fix\",\n    \"format\": \"prettier --write 'src/**/*.{ts,js}'\",\n    \"format:check\": \"prettier --check 'src/**/*.{ts,js}'\",\n    \"typecheck\": \"tsc --noEmit\",\n    \"test\": \"jest\",\n    \"test:unit\": \"jest --testPathPattern=unit\",\n    \"test:integration\": \"jest --testPathPattern=integration\",\n    \"test:coverage\": \"jest --coverage\",\n    \"test:ci\": \"jest --ci --coverage --forceExit\",\n    \"build\": \"tsc\",\n    \"ci\": \"npm run lint && npm run typecheck && npm run test:ci && npm run build\"\n  }\n}\n```\n\n## Common Pitfalls\n\n1. **Running all checks sequentially** - Lint, typecheck, and tests can run in parallel since they are independent. Use separate jobs.\n2. **Not failing the pipeline on lint errors** - Lint warnings that never become errors are ignored. Set strict rules.\n3. **Skipping integration tests in CI** - Unit tests alone miss real bugs. Always test with real databases in CI.\n4. **Coverage thresholds that are too high or too low** - 80% line coverage is a good starting point. 100% is rarely practical or necessary.\n5. **Not caching between pipeline runs** - Each run installs fresh dependencies without caching, wasting minutes.\n",
    "intuition": "A testing pipeline is like airport security. Each checkpoint catches different problems: the lint check catches formatting issues (metal detector), type checking catches type mismatches (X-ray machine), unit tests catch logic bugs (baggage screening), and integration tests ensure everything works together (the final boarding check). Only code that passes ALL checkpoints makes it to production.",
    "exercises": [
      {
        "title": "Build a Quality Gate",
        "description": "Create a complete GitHub Actions workflow with four parallel stages: lint (ESLint + Prettier), typecheck (tsc), unit tests (Jest), and integration tests (Jest + PostgreSQL service container). Add a final coverage stage that checks for 80% minimum coverage and comments the result on the PR.",
        "hint": "Use 'needs' for stage dependencies. Run lint and typecheck in parallel, then tests after both pass. Use actions/upload-artifact to share coverage between jobs."
      },
      {
        "title": "Pre-Commit Hook Setup",
        "description": "Set up Husky with lint-staged to run ESLint and Prettier on staged files before each commit, and TypeScript type-checking before each push. Verify that commits with lint errors are rejected.",
        "hint": "Use 'npx husky init' to set up. Configure lint-staged in package.json. Create .husky/pre-commit and .husky/pre-push files."
      },
      {
        "title": "Coverage Trend Tracking",
        "description": "Modify the CI workflow to fail if coverage drops more than 2% compared to the main branch. Store the main branch coverage in a GitHub Actions artifact and compare it in PR builds.",
        "hint": "On main pushes, upload coverage-summary.json as an artifact. On PRs, download the main branch artifact and compare the values using jq."
      }
    ],
    "problems": [
      {
        "problem": "ESLint passes locally but fails in CI",
        "cause": "Different ESLint versions (local vs CI) or different configurations loaded. The CI environment may have stricter settings.",
        "solution": "Pin ESLint version in package.json. Ensure .eslintrc is committed. Run `npm ci` (not `npm install`) in CI for deterministic installs. Check that .eslintignore covers generated files."
      },
      {
        "problem": "Integration tests flaky (pass sometimes, fail sometimes)",
        "cause": "Tests depend on timing, shared database state, or ordering. Parallel test execution can cause conflicts.",
        "solution": "Use unique database names per test run. Clean up data in beforeEach/afterEach. Use --runInBand for integration tests. Add --forceExit to prevent hanging. Increase timeouts for slow CI environments."
      },
      {
        "problem": "Coverage report shows 0% even though tests pass",
        "cause": "Jest is running compiled JavaScript but coverage is configured for TypeScript source, or coverage path patterns are wrong.",
        "solution": "Ensure ts-jest is configured properly. Check coveragePathIgnorePatterns is not accidentally excluding your source. Use `--collectCoverageFrom='src/**/*.ts'` explicitly."
      }
    ],
    "further_reading": [
      {
        "title": "ESLint Configuration (Flat Config)",
        "url": "https://eslint.org/docs/latest/use/configure/configuration-files",
        "type": "documentation"
      },
      {
        "title": "Jest Configuration",
        "url": "https://jestjs.io/docs/configuration",
        "type": "documentation"
      },
      {
        "title": "Husky - Modern Git Hooks",
        "url": "https://typicode.github.io/husky/",
        "type": "documentation"
      },
      {
        "title": "lint-staged",
        "url": "https://github.com/lint-staged/lint-staged",
        "type": "github"
      }
    ]
  },
  "6.3.3": {
    "body": "# Automated Deployment Strategies\n\n## From CI to CD\n\nContinuous Deployment (CD) extends CI by automatically deploying every change that passes tests. The deployment strategy you choose determines the risk, speed, and complexity of releases.\n\n## Deployment Strategy Comparison\n\n```\nStrategy         Risk    Downtime    Complexity    Rollback Speed\n──────────────────────────────────────────────────────────────────\nRecreate         High    Yes         Low           Slow\nRolling          Medium  No          Medium        Medium\nBlue/Green       Low     No          Medium        Instant\nCanary           Low     No          High          Fast\nFeature Flags    Low     No          High          Instant\n```\n\n### Recreate Deployment\n\n```\nVersion 1 (stop all) ──────── downtime ──────── Version 2 (start all)\n\n┌─────┐ ┌─────┐ ┌─────┐                  ┌─────┐ ┌─────┐ ┌─────┐\n│v1   │ │v1   │ │v1   │   ──────────►    │v2   │ │v2   │ │v2   │\n└─────┘ └─────┘ └─────┘    (downtime)    └─────┘ └─────┘ └─────┘\n```\n\n### Rolling Deployment\n\n```\n┌─────┐ ┌─────┐ ┌─────┐    ┌─────┐ ┌─────┐ ┌─────┐    ┌─────┐ ┌─────┐ ┌─────┐\n│v1   │ │v1   │ │v1   │ ─► │v2   │ │v1   │ │v1   │ ─► │v2   │ │v2   │ │v1   │ ─► all v2\n└─────┘ └─────┘ └─────┘    └─────┘ └─────┘ └─────┘    └─────┘ └─────┘ └─────┘\n```\n\n### Blue/Green Deployment\n\n```\nLoad Balancer\n      │\n      ├──── Blue (v1) ◄── current traffic\n      │\n      └──── Green (v2) ◄── deploy here, test, then switch\n\nAfter switch:\n      │\n      ├──── Blue (v1) ◄── standby (instant rollback)\n      │\n      └──── Green (v2) ◄── current traffic\n```\n\n### Canary Deployment\n\n```\nLoad Balancer\n      │\n      ├──── 95% ──► v1 instances (stable)\n      │\n      └──── 5%  ──► v2 instance  (canary)\n                    Monitor for errors...\n                    If OK, gradually increase to 100%\n```\n\n## GitHub Actions Deployment Workflows\n\n### Deploy to VPS/EC2 via SSH\n\n```yaml\n# .github/workflows/deploy.yml\nname: Deploy\n\non:\n  push:\n    branches: [main]\n\njobs:\n  test:\n    uses: ./.github/workflows/test.yml\n    secrets: inherit\n\n  deploy:\n    name: Deploy to Production\n    needs: test\n    runs-on: ubuntu-latest\n    environment: production        # Requires approval if configured\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Deploy via SSH\n        uses: appleboy/ssh-action@v1\n        with:\n          host: ${{ secrets.SERVER_HOST }}\n          username: ${{ secrets.SERVER_USER }}\n          key: ${{ secrets.SSH_PRIVATE_KEY }}\n          script: |\n            cd /var/www/myapp\n            git pull origin main\n            npm ci --production\n            npx prisma migrate deploy\n            pm2 reload ecosystem.config.js --env production\n```\n\n### Deploy Docker Image\n\n```yaml\nname: Build and Deploy Docker\n\non:\n  push:\n    branches: [main]\n\nenv:\n  REGISTRY: ghcr.io\n  IMAGE_NAME: ${{ github.repository }}\n\njobs:\n  build-and-push:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: read\n      packages: write\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Login to Container Registry\n        uses: docker/login-action@v3\n        with:\n          registry: ${{ env.REGISTRY }}\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n\n      - name: Build and push image\n        uses: docker/build-push-action@v5\n        with:\n          context: .\n          push: true\n          tags: |\n            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest\n            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}\n          cache-from: type=gha\n          cache-to: type=gha,mode=max\n\n  deploy:\n    needs: build-and-push\n    runs-on: ubuntu-latest\n    environment: production\n    steps:\n      - name: Deploy to server\n        uses: appleboy/ssh-action@v1\n        with:\n          host: ${{ secrets.SERVER_HOST }}\n          username: deploy\n          key: ${{ secrets.SSH_PRIVATE_KEY }}\n          script: |\n            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}\n            docker compose -f docker-compose.prod.yml up -d --no-deps api\n            docker image prune -f\n```\n\n### Deploy to AWS Lambda (Serverless)\n\n```yaml\nname: Deploy Serverless\n\non:\n  push:\n    branches: [main]\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n\n      - run: npm ci\n      - run: npm run build\n\n      - name: Configure AWS credentials\n        uses: aws-actions/configure-aws-credentials@v4\n        with:\n          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}\n          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}\n          aws-region: us-east-1\n\n      - name: Deploy with Serverless Framework\n        run: npx serverless deploy --stage production\n```\n\n### Blue/Green with AWS\n\n```yaml\nname: Blue/Green Deploy\n\non:\n  push:\n    branches: [main]\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment: production\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Configure AWS\n        uses: aws-actions/configure-aws-credentials@v4\n        with:\n          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}\n          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}\n          aws-region: us-east-1\n\n      - name: Deploy to Green environment\n        run: |\n          # Build and push Docker image\n          aws ecr get-login-password | docker login --username AWS --password-stdin $ECR_REGISTRY\n          docker build -t $ECR_REGISTRY/myapp:${{ github.sha }} .\n          docker push $ECR_REGISTRY/myapp:${{ github.sha }}\n\n          # Update ECS service (green)\n          aws ecs update-service \\\n            --cluster myapp-cluster \\\n            --service myapp-green \\\n            --task-definition myapp:${{ github.sha }}\n\n          # Wait for deployment\n          aws ecs wait services-stable \\\n            --cluster myapp-cluster \\\n            --services myapp-green\n\n      - name: Health check green\n        run: |\n          for i in $(seq 1 10); do\n            if curl -sf https://green.myapp.com/health; then\n              echo \"Green is healthy!\"\n              exit 0\n            fi\n            sleep 5\n          done\n          echo \"Green health check failed\"\n          exit 1\n\n      - name: Switch traffic to green\n        run: |\n          # Update ALB target group weights\n          aws elbv2 modify-rule \\\n            --rule-arn ${{ secrets.ALB_RULE_ARN }} \\\n            --actions '[{\"Type\":\"forward\",\"TargetGroupArn\":\"'$GREEN_TG_ARN'\",\"Weight\":100}]'\n\n      - name: Notify\n        if: always()\n        uses: slackapi/slack-github-action@v1\n        with:\n          payload: |\n            {\"text\": \"Deployment ${{ job.status }}: ${{ github.repository }}@${{ github.sha }}\"}\n        env:\n          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}\n```\n\n## Rollback Strategies\n\n```yaml\n# Manual rollback workflow\nname: Rollback\n\non:\n  workflow_dispatch:\n    inputs:\n      version:\n        description: 'Version to rollback to (git SHA or tag)'\n        required: true\n\njobs:\n  rollback:\n    runs-on: ubuntu-latest\n    environment: production\n    steps:\n      - name: Rollback deployment\n        uses: appleboy/ssh-action@v1\n        with:\n          host: ${{ secrets.SERVER_HOST }}\n          username: deploy\n          key: ${{ secrets.SSH_PRIVATE_KEY }}\n          script: |\n            cd /var/www/myapp\n            git checkout ${{ github.event.inputs.version }}\n            npm ci --production\n            npx prisma migrate deploy\n            pm2 reload all\n```\n\n## Database Migration Safety\n\n```yaml\n# Run migrations as a separate step with safeguards\n- name: Run database migrations\n  run: |\n    # Backup before migration\n    ssh deploy@$SERVER \"pg_dump myapp > /tmp/pre-migration-backup.sql\"\n\n    # Run migration\n    ssh deploy@$SERVER \"cd /var/www/myapp && npx prisma migrate deploy\"\n\n    # Verify application health after migration\n    sleep 10\n    if ! curl -sf https://myapp.com/health; then\n      echo \"App unhealthy after migration, check logs!\"\n      exit 1\n    fi\n```\n\n## Common Pitfalls\n\n1. **No rollback plan** - Every deployment should have a tested rollback procedure. Practice rollbacks regularly.\n2. **Deploying database migrations and code simultaneously** - Migrate first (forward-compatible), deploy code second. This avoids a window where new code runs against old schema.\n3. **No health checks after deployment** - Always verify the new version is actually working after deploy.\n4. **Deploying on Fridays** - Avoid deploying before weekends or holidays when the team is unavailable to fix issues.\n5. **No deployment notifications** - The team should know when deployments happen. Use Slack/Teams notifications.\n6. **No approval gates for production** - Use GitHub Environments with required reviewers for production deployments.\n",
    "intuition": "Deployment strategies are like different ways to renovate a restaurant while keeping it open. Recreate deployment closes the restaurant for renovation (downtime). Rolling deployment renovates one table at a time while serving guests at the others. Blue/Green is like having two identical restaurants -- renovate one while guests eat at the other, then move everyone over. Canary deployment is like inviting a few taste-testers to the new menu before offering it to all guests.",
    "exercises": [
      {
        "title": "Docker-Based CD Pipeline",
        "description": "Create a complete GitHub Actions workflow that: builds a Docker image, pushes it to GitHub Container Registry (ghcr.io), SSHs into a server and pulls the new image, and does a rolling restart with Docker Compose. Include health checks and Slack notification.",
        "hint": "Use docker/login-action for registry auth. Tag images with both 'latest' and the git SHA. Use 'docker compose up -d --no-deps service' for rolling restart."
      },
      {
        "title": "Blue/Green Deploy Script",
        "description": "Write a deployment script that implements blue/green deployment on a single server using Docker Compose. Maintain two compose files (blue and green). The script should: deploy to the inactive environment, run health checks, switch Nginx to point to the new environment, and keep the old one as rollback.",
        "hint": "Use Nginx upstream blocks with different ports for blue (3001) and green (3002). Swap the Nginx config and reload to switch traffic."
      },
      {
        "title": "Rollback Workflow",
        "description": "Create a manual GitHub Actions workflow (workflow_dispatch) that accepts a git SHA or tag and rolls back the production deployment to that version. Include database migration rollback considerations.",
        "hint": "Use workflow_dispatch with inputs for the version. SSH to the server, checkout the specific version, and restart. For database rollbacks, consider that not all migrations are reversible."
      }
    ],
    "problems": [
      {
        "problem": "Deployment succeeds but application returns 502 Bad Gateway",
        "cause": "The application started but crashed shortly after, or Nginx/load balancer cannot reach the new application instance.",
        "solution": "Check application logs (`pm2 logs` or `docker logs`). Verify the app is listening on the correct port. Ensure health check passes. Common causes: missing environment variables, database connection failure, port mismatch between app and Nginx."
      },
      {
        "problem": "Database migration fails during deployment",
        "cause": "The migration has a destructive change (dropping column) while old code is still running, or the migration has a syntax error.",
        "solution": "Always test migrations in staging first. Use expand-contract pattern: (1) add new column, (2) deploy code that writes to both, (3) migrate data, (4) deploy code that reads from new column, (5) drop old column. Never drop columns in the same deployment that stops using them."
      },
      {
        "problem": "Deployment works but SSH action times out",
        "cause": "The SSH connection is blocked by a firewall, security group, or the key/host configuration is wrong.",
        "solution": "Verify the server's IP is correct in secrets. Check that port 22 is open in the security group/firewall. Ensure the SSH key in secrets matches the authorized_keys on the server. Test SSH access manually first."
      }
    ],
    "further_reading": [
      {
        "title": "GitHub Actions: Deploying to Production",
        "url": "https://docs.github.com/en/actions/deployment",
        "type": "documentation"
      },
      {
        "title": "The Twelve-Factor App",
        "url": "https://12factor.net/",
        "type": "article",
        "note": "Methodology for building deployable SaaS applications"
      },
      {
        "title": "Blue/Green Deployments - Martin Fowler",
        "url": "https://martinfowler.com/bliki/BlueGreenDeployment.html",
        "type": "article"
      },
      {
        "title": "GitHub Environments",
        "url": "https://docs.github.com/en/actions/deployment/targeting-different-environments/using-environments-for-deployment",
        "type": "documentation"
      }
    ]
  },
  "6.4.1": {
    "body": "# AWS Fundamentals: EC2, RDS, S3\n\n## Why AWS for Backend Developers\n\nAmazon Web Services (AWS) is the most widely used cloud platform. Understanding its core services -- EC2 for compute, RDS for databases, and S3 for storage -- is essential for deploying and operating Node.js applications at scale.\n\n## AWS Architecture Overview\n\n```\n┌─────────────────────────────────────────────────────────┐\n│                        AWS Cloud                         │\n│                                                          │\n│  ┌─────────────── VPC (Virtual Private Cloud) ────────┐ │\n│  │                                                     │ │\n│  │  ┌── Public Subnet ──┐  ┌── Private Subnet ──────┐│ │\n│  │  │                    │  │                         ││ │\n│  │  │  ┌──────────────┐ │  │  ┌────────┐ ┌────────┐ ││ │\n│  │  │  │   EC2        │ │  │  │  RDS   │ │ ElastiC│ ││ │\n│  │  │  │   (Node.js)  │─┼──┼─►│ (PgSQL)│ │ (Redis)│ ││ │\n│  │  │  │              │ │  │  │        │ │        │ ││ │\n│  │  │  └──────────────┘ │  │  └────────┘ └────────┘ ││ │\n│  │  │                    │  │                         ││ │\n│  │  │  ┌──────────────┐ │  └─────────────────────────┘│ │\n│  │  │  │   ALB        │ │                              │ │\n│  │  │  │(Load Balancer)│ │                              │ │\n│  │  │  └──────────────┘ │                              │ │\n│  │  └────────────────────┘                              │ │\n│  └──────────────────────────────────────────────────────┘ │\n│                                                          │\n│  ┌──────────┐  ┌──────────────┐  ┌─────────────┐       │\n│  │    S3    │  │  CloudFront  │  │   Route 53  │       │\n│  │ (Storage)│  │    (CDN)     │  │   (DNS)     │       │\n│  └──────────┘  └──────────────┘  └─────────────┘       │\n└─────────────────────────────────────────────────────────┘\n```\n\n## EC2 (Elastic Compute Cloud)\n\nEC2 provides virtual servers (instances) in the cloud where you run your Node.js applications.\n\n### Launching an EC2 Instance\n\n```bash\n# Using AWS CLI\naws ec2 run-instances \\\n  --image-id ami-0c55b159cbfafe1f0 \\    # Amazon Linux 2023\n  --instance-type t3.small \\\n  --key-name my-key-pair \\\n  --security-group-ids sg-12345678 \\\n  --subnet-id subnet-12345678 \\\n  --tag-specifications 'ResourceType=instance,Tags=[{Key=Name,Value=myapp-server}]' \\\n  --user-data file://setup-script.sh\n```\n\n### User Data Script (runs on first boot)\n\n```bash\n#!/bin/bash\n# setup-script.sh - Automated EC2 setup\n\n# Update system\nyum update -y\n\n# Install Node.js 20\ncurl -fsSL https://rpm.nodesource.com/setup_20.x | bash -\nyum install -y nodejs\n\n# Install PM2\nnpm install -g pm2\n\n# Install Nginx\namazon-linux-extras install nginx1 -y\nsystemctl enable nginx\nsystemctl start nginx\n\n# Create deploy user\nuseradd -m deploy\nmkdir -p /home/deploy/.ssh\ncp /root/.ssh/authorized_keys /home/deploy/.ssh/\nchown -R deploy:deploy /home/deploy/.ssh\nchmod 700 /home/deploy/.ssh\n\n# Create app directory\nmkdir -p /var/www/myapp\nchown deploy:deploy /var/www/myapp\n\n# Configure PM2 startup\nsu - deploy -c \"pm2 startup systemd -u deploy --hp /home/deploy\"\n```\n\n### Instance Types for Node.js\n\n```\nType        vCPUs  Memory   Use Case\n─────────────────────────────────────────────────\nt3.micro    2      1 GB     Development, testing\nt3.small    2      2 GB     Small APIs, low traffic\nt3.medium   2      4 GB     Medium APIs\nt3.large    2      8 GB     APIs with caching\nm6i.large   2      8 GB     Production workloads\nc6i.large   2      4 GB     CPU-intensive (compute optimized)\nr6i.large   2      16 GB    Memory-intensive (caching, in-memory)\n```\n\n### Security Groups (Firewall)\n\n```bash\n# Create security group\naws ec2 create-security-group \\\n  --group-name myapp-sg \\\n  --description \"Security group for myapp\" \\\n  --vpc-id vpc-12345678\n\n# Allow SSH (from your IP only)\naws ec2 authorize-security-group-ingress \\\n  --group-id sg-12345678 \\\n  --protocol tcp \\\n  --port 22 \\\n  --cidr 203.0.113.50/32\n\n# Allow HTTP/HTTPS (from anywhere)\naws ec2 authorize-security-group-ingress \\\n  --group-id sg-12345678 \\\n  --protocol tcp \\\n  --port 80 \\\n  --cidr 0.0.0.0/0\n\naws ec2 authorize-security-group-ingress \\\n  --group-id sg-12345678 \\\n  --protocol tcp \\\n  --port 443 \\\n  --cidr 0.0.0.0/0\n```\n\n## RDS (Relational Database Service)\n\nRDS is a managed database service. It handles backups, patching, replication, and failover so you do not have to.\n\n### Creating a PostgreSQL RDS Instance\n\n```bash\naws rds create-db-instance \\\n  --db-instance-identifier myapp-db \\\n  --db-instance-class db.t3.small \\\n  --engine postgres \\\n  --engine-version 16.1 \\\n  --master-username myapp_admin \\\n  --master-user-password \"$DB_PASSWORD\" \\\n  --allocated-storage 20 \\\n  --storage-type gp3 \\\n  --vpc-security-group-ids sg-db-12345678 \\\n  --db-subnet-group-name myapp-db-subnet \\\n  --backup-retention-period 7 \\\n  --multi-az \\\n  --storage-encrypted \\\n  --no-publicly-accessible\n```\n\n### Connecting from Node.js\n\n```javascript\n// config/database.ts\nimport { PrismaClient } from '@prisma/client';\n\nconst prisma = new PrismaClient({\n  datasources: {\n    db: {\n      // RDS endpoint from AWS console or environment variable\n      url: process.env.DATABASE_URL,\n      // Example: postgresql://myapp_admin:password@myapp-db.abc123.us-east-1.rds.amazonaws.com:5432/myapp\n    },\n  },\n});\n\nexport default prisma;\n```\n\n### RDS Best Practices\n\n```\n1. NEVER make RDS publicly accessible\n   └── Keep in private subnet, access via EC2 or VPN\n\n2. Enable Multi-AZ for production\n   └── Automatic failover to standby in another AZ\n\n3. Enable automated backups\n   └── Point-in-time recovery up to 35 days\n\n4. Use read replicas for read-heavy workloads\n   └── Route read queries to replicas\n\n5. Enable encryption at rest\n   └── Uses AWS KMS keys\n\n6. Use IAM authentication (not passwords)\n   └── Short-lived tokens instead of static passwords\n```\n\n## S3 (Simple Storage Service)\n\nS3 stores files (objects) like user uploads, images, backups, and static assets.\n\n### S3 Operations with AWS SDK\n\n```javascript\n// services/storage.ts\nimport {\n  S3Client,\n  PutObjectCommand,\n  GetObjectCommand,\n  DeleteObjectCommand,\n} from '@aws-sdk/client-s3';\nimport { getSignedUrl } from '@aws-sdk/s3-request-presigner';\n\nconst s3 = new S3Client({\n  region: process.env.AWS_REGION || 'us-east-1',\n});\n\nconst BUCKET = process.env.S3_BUCKET!;\n\n// Upload a file\nexport async function uploadFile(\n  key: string,\n  body: Buffer,\n  contentType: string\n): Promise<string> {\n  await s3.send(\n    new PutObjectCommand({\n      Bucket: BUCKET,\n      Key: key,\n      Body: body,\n      ContentType: contentType,\n      ServerSideEncryption: 'AES256',\n    })\n  );\n  return `https://${BUCKET}.s3.amazonaws.com/${key}`;\n}\n\n// Generate pre-signed URL for direct upload\nexport async function getUploadUrl(\n  key: string,\n  contentType: string,\n  expiresIn = 3600\n): Promise<string> {\n  const command = new PutObjectCommand({\n    Bucket: BUCKET,\n    Key: key,\n    ContentType: contentType,\n  });\n  return getSignedUrl(s3, command, { expiresIn });\n}\n\n// Generate pre-signed URL for download\nexport async function getDownloadUrl(\n  key: string,\n  expiresIn = 3600\n): Promise<string> {\n  const command = new GetObjectCommand({\n    Bucket: BUCKET,\n    Key: key,\n  });\n  return getSignedUrl(s3, command, { expiresIn });\n}\n\n// Delete a file\nexport async function deleteFile(key: string): Promise<void> {\n  await s3.send(\n    new DeleteObjectCommand({\n      Bucket: BUCKET,\n      Key: key,\n    })\n  );\n}\n```\n\n### Express File Upload to S3\n\n```javascript\n// routes/upload.ts\nimport { Router } from 'express';\nimport multer from 'multer';\nimport { v4 as uuidv4 } from 'uuid';\nimport path from 'path';\nimport { uploadFile } from '../services/storage';\n\nconst router = Router();\nconst upload = multer({\n  storage: multer.memoryStorage(),\n  limits: { fileSize: 10 * 1024 * 1024 }, // 10MB\n  fileFilter: (req, file, cb) => {\n    const allowed = ['.jpg', '.jpeg', '.png', '.gif', '.webp'];\n    const ext = path.extname(file.originalname).toLowerCase();\n    if (allowed.includes(ext)) {\n      cb(null, true);\n    } else {\n      cb(new Error('Invalid file type'));\n    }\n  },\n});\n\nrouter.post('/upload', upload.single('image'), async (req, res) => {\n  if (!req.file) {\n    return res.status(400).json({ error: 'No file provided' });\n  }\n\n  const ext = path.extname(req.file.originalname);\n  const key = `uploads/${uuidv4()}${ext}`;\n\n  const url = await uploadFile(key, req.file.buffer, req.file.mimetype);\n\n  res.json({ url, key });\n});\n\nexport default router;\n```\n\n### S3 Bucket Policy\n\n```json\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Sid\": \"AllowAppAccess\",\n      \"Effect\": \"Allow\",\n      \"Principal\": {\n        \"AWS\": \"arn:aws:iam::123456789012:role/myapp-ec2-role\"\n      },\n      \"Action\": [\"s3:GetObject\", \"s3:PutObject\", \"s3:DeleteObject\"],\n      \"Resource\": \"arn:aws:s3:::myapp-uploads/*\"\n    }\n  ]\n}\n```\n\n## IAM (Identity and Access Management)\n\n```bash\n# Create a policy for your app\naws iam create-policy \\\n  --policy-name myapp-s3-policy \\\n  --policy-document '{\n    \"Version\": \"2012-10-17\",\n    \"Statement\": [\n      {\n        \"Effect\": \"Allow\",\n        \"Action\": [\"s3:GetObject\", \"s3:PutObject\"],\n        \"Resource\": \"arn:aws:s3:::myapp-uploads/*\"\n      }\n    ]\n  }'\n\n# Create an IAM role for EC2\naws iam create-role \\\n  --role-name myapp-ec2-role \\\n  --assume-role-policy-document '{\n    \"Version\": \"2012-10-17\",\n    \"Statement\": [\n      {\n        \"Effect\": \"Allow\",\n        \"Principal\": {\"Service\": \"ec2.amazonaws.com\"},\n        \"Action\": \"sts:AssumeRole\"\n      }\n    ]\n  }'\n```\n\n## Common Pitfalls\n\n1. **Making RDS publicly accessible** - Always place databases in private subnets. Access through EC2 or VPN only.\n2. **Hardcoding AWS credentials** - Use IAM roles for EC2, environment variables for local dev. Never commit credentials.\n3. **Not enabling S3 versioning** - Without versioning, deleted or overwritten files are gone forever.\n4. **Using t2/t3 burstable instances in production without understanding burst credits** - Once credits run out, CPU is throttled. Monitor CPU credit balance.\n5. **Not setting up billing alerts** - Always create a budget alert to avoid surprise charges.\n",
    "intuition": "Think of AWS like renting an apartment building for your application. EC2 is the apartment itself (the server where your code runs). RDS is the building's secure storage room (managed database -- the building handles maintenance, backups, and security). S3 is like a self-storage facility across the street (virtually unlimited space for files). IAM is the key card system that controls who can access what.",
    "exercises": [
      {
        "title": "Deploy Node.js to EC2",
        "description": "Launch a t3.micro EC2 instance, SSH into it, install Node.js and PM2, deploy a simple Express API, and configure a security group to allow HTTP traffic. Verify you can reach the API from your browser using the public IP.",
        "hint": "Use the AWS Free Tier. Create a key pair first. Install Node.js via NodeSource. Use PM2 to keep the app running. Open port 3000 in the security group."
      },
      {
        "title": "S3 File Upload API",
        "description": "Build an Express endpoint that accepts file uploads via multer, stores them in S3 with a unique key, and returns the S3 URL. Implement a second endpoint that generates pre-signed URLs for direct client-to-S3 uploads (bypassing your server).",
        "hint": "Use @aws-sdk/client-s3 and @aws-sdk/s3-request-presigner. For pre-signed URLs, generate a PutObject signed URL with expiration."
      },
      {
        "title": "RDS Connection Setup",
        "description": "Create a free-tier RDS PostgreSQL instance, configure it in a VPC with a private subnet, and connect to it from an EC2 instance using Prisma. Run migrations and verify the database is working.",
        "hint": "The EC2 and RDS security groups must allow traffic between them. Use SSH tunnel from your local machine to EC2 to manage the database with a GUI tool."
      }
    ],
    "problems": [
      {
        "problem": "Cannot connect to RDS from EC2",
        "cause": "The RDS security group does not allow inbound traffic from the EC2 security group on port 5432.",
        "solution": "Edit the RDS security group to allow inbound PostgreSQL (port 5432) from the EC2 instance's security group ID. Ensure both are in the same VPC. Verify the RDS endpoint is correct (not the IP, which can change)."
      },
      {
        "problem": "S3 upload fails with AccessDenied",
        "cause": "The IAM user/role does not have permission to write to the S3 bucket, or the bucket policy denies the action.",
        "solution": "Check the IAM policy attached to your EC2 role or IAM user. Ensure it has s3:PutObject permission on the correct bucket/prefix. Check the bucket policy for explicit denies. Use AWS CloudTrail to see the exact authorization failure."
      },
      {
        "problem": "EC2 instance runs out of memory and becomes unresponsive",
        "cause": "Node.js application consuming too much memory on a small instance, or a memory leak in the application.",
        "solution": "Add swap space: `dd if=/dev/zero of=/swapfile bs=1M count=2048 && mkswap /swapfile && swapon /swapfile`. Set Node.js memory limit: `--max-old-space-size=512`. Upgrade to a larger instance type. Use PM2's max_memory_restart to auto-restart on memory threshold."
      }
    ],
    "further_reading": [
      {
        "title": "AWS Getting Started - Deploy Node.js",
        "url": "https://docs.aws.amazon.com/sdk-for-javascript/v3/developer-guide/getting-started-nodejs.html",
        "type": "documentation"
      },
      {
        "title": "AWS SDK for JavaScript v3",
        "url": "https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/",
        "type": "documentation"
      },
      {
        "title": "AWS Free Tier",
        "url": "https://aws.amazon.com/free/",
        "type": "documentation"
      },
      {
        "title": "AWS Well-Architected Framework",
        "url": "https://docs.aws.amazon.com/wellarchitected/latest/framework/welcome.html",
        "type": "documentation"
      }
    ]
  },
  "6.4.2": {
    "body": "# AWS Lambda and Serverless Node.js\n\n## What is Serverless?\n\nServerless does not mean \"no servers.\" It means you do not manage servers. AWS Lambda runs your code in response to events, automatically scaling from zero to thousands of concurrent executions. You pay only for the compute time you consume.\n\n## Serverless vs Traditional Architecture\n\n```\nTraditional (EC2):\n┌────────────┐\n│  EC2 Server │  Always running, always costing money\n│  ┌────────┐ │  Handles 0-100 requests/sec\n│  │Node.js │ │  You manage: OS, patching, scaling\n│  │        │ │  Fixed capacity\n│  └────────┘ │\n└────────────┘\n\nServerless (Lambda):\n┌──────┐ ┌──────┐ ┌──────┐\n│ fn() │ │ fn() │ │ fn() │  Spins up per-request\n└──────┘ └──────┘ └──────┘  Scales to 1000s automatically\n   │         │         │     Costs $0 when idle\n   └─────────┼─────────┘     AWS manages everything\n             │\n        ┌────┴────┐\n        │API GW   │\n        └─────────┘\n```\n\n## Lambda Function Basics\n\n### Simple Lambda Handler\n\n```javascript\n// src/handlers/hello.ts\nimport { APIGatewayProxyEvent, APIGatewayProxyResult } from 'aws-lambda';\n\nexport const handler = async (\n  event: APIGatewayProxyEvent\n): Promise<APIGatewayProxyResult> => {\n  const name = event.queryStringParameters?.name || 'World';\n\n  return {\n    statusCode: 200,\n    headers: {\n      'Content-Type': 'application/json',\n    },\n    body: JSON.stringify({\n      message: `Hello, ${name}!`,\n      timestamp: new Date().toISOString(),\n    }),\n  };\n};\n```\n\n### CRUD API with Lambda\n\n```javascript\n// src/handlers/users.ts\nimport { APIGatewayProxyEvent, APIGatewayProxyResult } from 'aws-lambda';\nimport { PrismaClient } from '@prisma/client';\n\n// Initialize outside handler for connection reuse\nconst prisma = new PrismaClient();\n\nexport const getUsers = async (\n  event: APIGatewayProxyEvent\n): Promise<APIGatewayProxyResult> => {\n  try {\n    const limit = parseInt(event.queryStringParameters?.limit || '20');\n    const offset = parseInt(event.queryStringParameters?.offset || '0');\n\n    const users = await prisma.user.findMany({\n      take: limit,\n      skip: offset,\n      select: { id: true, name: true, email: true, createdAt: true },\n    });\n\n    return {\n      statusCode: 200,\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ data: users }),\n    };\n  } catch (error) {\n    console.error('Error fetching users:', error);\n    return {\n      statusCode: 500,\n      body: JSON.stringify({ error: 'Internal server error' }),\n    };\n  }\n};\n\nexport const createUser = async (\n  event: APIGatewayProxyEvent\n): Promise<APIGatewayProxyResult> => {\n  try {\n    const body = JSON.parse(event.body || '{}');\n\n    const user = await prisma.user.create({\n      data: {\n        name: body.name,\n        email: body.email,\n      },\n    });\n\n    return {\n      statusCode: 201,\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ data: user }),\n    };\n  } catch (error) {\n    console.error('Error creating user:', error);\n    return {\n      statusCode: 500,\n      body: JSON.stringify({ error: 'Internal server error' }),\n    };\n  }\n};\n\nexport const getUserById = async (\n  event: APIGatewayProxyEvent\n): Promise<APIGatewayProxyResult> => {\n  const id = event.pathParameters?.id;\n\n  const user = await prisma.user.findUnique({\n    where: { id },\n  });\n\n  if (!user) {\n    return {\n      statusCode: 404,\n      body: JSON.stringify({ error: 'User not found' }),\n    };\n  }\n\n  return {\n    statusCode: 200,\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ data: user }),\n  };\n};\n```\n\n## Serverless Framework\n\nThe Serverless Framework simplifies Lambda deployment with a YAML configuration file.\n\n```yaml\n# serverless.yml\nservice: myapp-api\n\nframeworkVersion: '3'\n\nprovider:\n  name: aws\n  runtime: nodejs20.x\n  region: us-east-1\n  stage: ${opt:stage, 'dev'}\n  memorySize: 256\n  timeout: 10\n  environment:\n    DATABASE_URL: ${env:DATABASE_URL}\n    NODE_ENV: production\n  iam:\n    role:\n      statements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n            - s3:PutObject\n          Resource: arn:aws:s3:::${self:service}-uploads-${self:provider.stage}/*\n\nplugins:\n  - serverless-offline         # Local development\n  - serverless-prune-plugin    # Clean old Lambda versions\n\ncustom:\n  serverless-offline:\n    httpPort: 3000\n  prune:\n    automatic: true\n    number: 3\n\nfunctions:\n  getUsers:\n    handler: dist/handlers/users.getUsers\n    events:\n      - httpApi:\n          path: /users\n          method: get\n\n  createUser:\n    handler: dist/handlers/users.createUser\n    events:\n      - httpApi:\n          path: /users\n          method: post\n\n  getUserById:\n    handler: dist/handlers/users.getUserById\n    events:\n      - httpApi:\n          path: /users/{id}\n          method: get\n\n  # Scheduled function (cron)\n  dailyCleanup:\n    handler: dist/handlers/cron.cleanup\n    events:\n      - schedule: rate(1 day)\n\n  # S3 event trigger\n  processUpload:\n    handler: dist/handlers/s3.processUpload\n    events:\n      - s3:\n          bucket: ${self:service}-uploads-${self:provider.stage}\n          event: s3:ObjectCreated:*\n\nresources:\n  Resources:\n    UploadsBucket:\n      Type: AWS::S3::Bucket\n      Properties:\n        BucketName: ${self:service}-uploads-${self:provider.stage}\n```\n\n### Deploy and Manage\n\n```bash\n# Install Serverless Framework\nnpm install -g serverless\n\n# Deploy to AWS\nserverless deploy --stage production\n\n# Deploy single function (faster)\nserverless deploy function -f getUsers\n\n# Invoke function locally\nserverless invoke local -f getUsers -d '{\"queryStringParameters\": {\"limit\": \"10\"}}'\n\n# Run locally with serverless-offline\nserverless offline\n\n# View logs\nserverless logs -f getUsers -t    # Tail logs\n\n# Remove entire stack\nserverless remove\n```\n\n## Express on Lambda\n\nYou can run your entire Express application on Lambda using a wrapper:\n\n```javascript\n// src/lambda.ts\nimport serverless from 'serverless-http';\nimport app from './app';     // Your existing Express app\n\nexport const handler = serverless(app);\n```\n\n```yaml\n# serverless.yml\nfunctions:\n  api:\n    handler: dist/lambda.handler\n    events:\n      - httpApi: '*'          # Catch all routes\n```\n\n## Cold Starts\n\n```\nCold Start (first request to a new instance):\n┌─────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────┐\n│ Download     │ │ Init runtime │ │ Init handler  │ │ Execute  │\n│ code package │ │ (Node.js)    │ │ (your code)   │ │ handler  │\n│  ~200ms      │ │  ~100ms      │ │  ~300-2000ms  │ │  ~50ms   │\n└─────────────┘ └──────────────┘ └──────────────┘ └──────────┘\n     ▲                                  ▲\n     │                                  │\nTotal cold start: 300-2500ms     DB connections, imports\n\nWarm Start (reusing existing instance):\n┌──────────┐\n│ Execute  │\n│ handler  │\n│  ~50ms   │\n└──────────┘\n```\n\n### Mitigating Cold Starts\n\n```javascript\n// 1. Initialize outside the handler (reused across invocations)\nimport { PrismaClient } from '@prisma/client';\nconst prisma = new PrismaClient();     // Created once, reused\n\nexport const handler = async (event) => {\n  // prisma is already initialized on warm starts\n  const users = await prisma.user.findMany();\n  return { statusCode: 200, body: JSON.stringify(users) };\n};\n```\n\n```yaml\n# 2. Provisioned Concurrency (keeps instances warm)\nfunctions:\n  api:\n    handler: dist/lambda.handler\n    provisionedConcurrency: 5    # Keep 5 instances always warm\n```\n\n```yaml\n# 3. Scheduled warming (cheaper alternative)\nfunctions:\n  api:\n    handler: dist/lambda.handler\n  warmer:\n    handler: dist/handlers/warmer.handler\n    events:\n      - schedule: rate(5 minutes)\n    environment:\n      FUNCTION_TO_WARM: ${self:service}-${self:provider.stage}-api\n```\n\n## Lambda Limits\n\n```\nLimit                          Value\n─────────────────────────────────────────\nMemory                         128 MB - 10,240 MB\nTimeout                        Max 15 minutes\nPackage size (zipped)          50 MB\nPackage size (unzipped)        250 MB\n/tmp storage                   512 MB - 10 GB\nConcurrent executions          1,000 (default, can increase)\nPayload size (sync)            6 MB\nEnvironment variables          4 KB total\n```\n\n## Common Pitfalls\n\n1. **Not reusing database connections** - Initialize clients outside the handler. Lambda reuses the execution context across invocations.\n2. **Packaging node_modules in the zip** - Use Lambda layers or bundlers (esbuild) to reduce package size and cold starts.\n3. **Ignoring cold starts** - For user-facing APIs, cold starts of 2+ seconds are unacceptable. Use provisioned concurrency or warming.\n4. **Setting timeout too low** - Lambda default is 3 seconds. Database queries and external API calls may need 10-30 seconds.\n5. **Not handling Lambda lifecycle** - Lambda freezes the process between invocations. Timers, setInterval, and open connections behave unexpectedly.\n",
    "intuition": "Lambda functions are like vending machines. You do not run the factory (server) -- you just put your product (code) in the machine, and it dispenses results whenever someone presses a button (triggers an event). You pay per transaction, not for keeping the factory open 24/7. The downside is the first customer after the machine has been idle needs to wait while it warms up (cold start).",
    "exercises": [
      {
        "title": "Build a Serverless REST API",
        "description": "Create a simple CRUD API using the Serverless Framework with Lambda and API Gateway. Implement endpoints for creating, reading, updating, and deleting items. Use DynamoDB or connect to an RDS PostgreSQL instance.",
        "hint": "Start with serverless create --template aws-nodejs-typescript. Use serverless-offline for local testing. Define each CRUD operation as a separate function."
      },
      {
        "title": "Express App on Lambda",
        "description": "Take an existing Express application and deploy it to Lambda using serverless-http. Compare the local Express response times vs Lambda cold start and warm start times. Measure with curl and CloudWatch.",
        "hint": "Install serverless-http. Create a lambda.ts that wraps your Express app. Use a single catch-all route in serverless.yml."
      },
      {
        "title": "Event-Driven Processing",
        "description": "Create a Lambda function that triggers on S3 uploads, processes the uploaded image (resize using sharp), and saves the result to a different S3 prefix. Use the Serverless Framework to configure the S3 event trigger.",
        "hint": "Use the s3 event in serverless.yml. The event object contains the bucket name and object key. Use sharp for image processing. Write the result to a 'thumbnails/' prefix."
      }
    ],
    "problems": [
      {
        "problem": "Lambda function times out",
        "cause": "The function execution exceeds the configured timeout, often due to database connection issues or slow external API calls.",
        "solution": "Increase timeout in serverless.yml (max 900 seconds). Check if the Lambda can reach the database (VPC configuration, security groups). Initialize database connections outside the handler. Add logging to identify the slow operation."
      },
      {
        "problem": "Lambda package too large to deploy",
        "cause": "node_modules includes unnecessary packages, or native modules add bulk.",
        "solution": "Use a bundler like esbuild to tree-shake and bundle only used code. Move large dependencies to Lambda Layers. Exclude dev dependencies. Use serverless-esbuild plugin for automatic bundling."
      },
      {
        "problem": "Database connections exhausted under load",
        "cause": "Each Lambda instance creates its own database connection. With 100 concurrent Lambdas, that is 100 connections.",
        "solution": "Use connection pooling with RDS Proxy (AWS managed). Or use Prisma Data Proxy. Set connection limits in your Prisma schema. Consider using DynamoDB for serverless-native workloads."
      }
    ],
    "further_reading": [
      {
        "title": "AWS Lambda Developer Guide",
        "url": "https://docs.aws.amazon.com/lambda/latest/dg/welcome.html",
        "type": "documentation"
      },
      {
        "title": "Serverless Framework Documentation",
        "url": "https://www.serverless.com/framework/docs",
        "type": "documentation"
      },
      {
        "title": "Serverless Node.js Best Practices",
        "url": "https://docs.aws.amazon.com/lambda/latest/dg/best-practices.html",
        "type": "documentation"
      },
      {
        "title": "SST (Serverless Stack)",
        "url": "https://sst.dev/",
        "type": "tool",
        "note": "Modern framework for serverless apps with live Lambda development"
      }
    ]
  },
  "6.4.3": {
    "body": "# GCP and Heroku: Alternative Platforms\n\n## Platform Comparison\n\n```\nFeature          AWS              GCP              Heroku\n─────────────────────────────────────────────────────────────\nComplexity       High             Medium           Low\nLearning curve   Steep            Moderate         Gentle\nCost at scale    Lowest           Competitive      Highest\nFree tier        12 months        Always Free      Limited\nServerless       Lambda           Cloud Functions  N/A\nContainers       ECS/EKS/Fargate  Cloud Run        Container Stack\nPaaS             Elastic Bean.    App Engine       Heroku (native)\nBest for         Enterprise       ML/Data/K8s      Prototypes/SMBs\n```\n\n## Google Cloud Platform (GCP)\n\n### Cloud Run (Serverless Containers)\n\nCloud Run is one of GCP's best offerings: deploy any Docker container and it scales automatically, including to zero.\n\n```dockerfile\n# Dockerfile for Cloud Run\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine\nRUN adduser -D appuser\nWORKDIR /app\nCOPY --from=builder /app/dist ./dist\nCOPY --from=builder /app/node_modules ./node_modules\nCOPY package.json ./\nUSER appuser\n# Cloud Run sets PORT environment variable\nCMD [\"node\", \"dist/server.js\"]\n```\n\n```bash\n# Deploy to Cloud Run\ngcloud run deploy myapp \\\n  --source . \\\n  --region us-central1 \\\n  --allow-unauthenticated \\\n  --set-env-vars NODE_ENV=production \\\n  --set-env-vars DATABASE_URL=\"$DATABASE_URL\" \\\n  --memory 512Mi \\\n  --cpu 1 \\\n  --min-instances 0 \\\n  --max-instances 10 \\\n  --port 3000\n```\n\n### Cloud Functions (Serverless Functions)\n\n```javascript\n// index.ts - Google Cloud Function\nimport { HttpFunction } from '@google-cloud/functions-framework';\n\nexport const helloWorld: HttpFunction = (req, res) => {\n  const name = req.query.name || 'World';\n  res.json({ message: `Hello, ${name}!` });\n};\n```\n\n```bash\n# Deploy Cloud Function\ngcloud functions deploy helloWorld \\\n  --runtime nodejs20 \\\n  --trigger-http \\\n  --allow-unauthenticated \\\n  --region us-central1\n```\n\n### App Engine\n\n```yaml\n# app.yaml\nruntime: nodejs20\n\nenv_variables:\n  NODE_ENV: production\n\ninstance_class: F2\n\nautomatic_scaling:\n  min_instances: 1\n  max_instances: 10\n  target_cpu_utilization: 0.65\n\nhandlers:\n  - url: /.*\n    script: auto\n    secure: always\n```\n\n```bash\ngcloud app deploy\n```\n\n### Cloud SQL (Managed PostgreSQL)\n\n```bash\n# Create Cloud SQL instance\ngcloud sql instances create myapp-db \\\n  --database-version POSTGRES_16 \\\n  --tier db-f1-micro \\\n  --region us-central1\n\n# Create database\ngcloud sql databases create myapp --instance myapp-db\n\n# Connect from Cloud Run (use Cloud SQL proxy)\ngcloud run deploy myapp \\\n  --add-cloudsql-instances myproject:us-central1:myapp-db \\\n  --set-env-vars DATABASE_URL=\"postgresql://user:pass@localhost:5432/myapp?host=/cloudsql/myproject:us-central1:myapp-db\"\n```\n\n## Heroku\n\nHeroku is a PaaS (Platform as a Service) that makes deployment extremely simple. It is ideal for prototypes, MVPs, and small to medium applications.\n\n### Heroku Deployment Flow\n\n```\ngit push heroku main\n       │\n       ▼\n┌──────────────┐     ┌───────────────┐     ┌──────────────┐\n│ Detect       │────►│ Install deps  │────►│ Build        │\n│ (Node.js)    │     │ (npm ci)      │     │ (npm build)  │\n└──────────────┘     └───────────────┘     └──────┬───────┘\n                                                   │\n                                                   ▼\n                                            ┌──────────────┐\n                                            │ Launch       │\n                                            │ (npm start)  │\n                                            └──────────────┘\n```\n\n### Project Setup for Heroku\n\n```json\n// package.json\n{\n  \"name\": \"myapp\",\n  \"version\": \"1.0.0\",\n  \"engines\": {\n    \"node\": \"20.x\"\n  },\n  \"scripts\": {\n    \"start\": \"node dist/server.js\",\n    \"build\": \"tsc\",\n    \"heroku-postbuild\": \"npm run build && npx prisma migrate deploy\"\n  }\n}\n```\n\n```\n# Procfile\nweb: node dist/server.js\nworker: node dist/workers/queue.js\nrelease: npx prisma migrate deploy\n```\n\n### Heroku CLI Commands\n\n```bash\n# Login\nheroku login\n\n# Create app\nheroku create myapp-api\n\n# Add PostgreSQL\nheroku addons:create heroku-postgresql:essential-0\n\n# Add Redis\nheroku addons:create heroku-redis:mini\n\n# Set environment variables\nheroku config:set NODE_ENV=production\nheroku config:set JWT_SECRET=your-secret-here\n\n# Deploy\ngit push heroku main\n\n# View logs\nheroku logs --tail\n\n# Run one-off commands\nheroku run npx prisma migrate deploy\nheroku run node scripts/seed.js\n\n# Scale\nheroku ps:scale web=2 worker=1\n\n# Open app in browser\nheroku open\n\n# Restart\nheroku restart\n```\n\n### Heroku with Docker\n\n```\n# heroku.yml\nbuild:\n  docker:\n    web: Dockerfile\nrun:\n  web: node dist/server.js\nrelease:\n  command:\n    - npx prisma migrate deploy\n```\n\n```bash\nheroku stack:set container\ngit push heroku main\n```\n\n## Platform Decision Matrix\n\n```\nChoose AWS when:\n  - Enterprise scale requirements\n  - Need specific services (Lambda, SQS, etc.)\n  - Cost optimization is priority at scale\n  - Team has AWS expertise\n\nChoose GCP when:\n  - Using Kubernetes (GKE is excellent)\n  - Machine learning workloads\n  - Cloud Run fits your use case\n  - BigQuery for analytics\n\nChoose Heroku when:\n  - Speed of deployment is priority\n  - Small team / solo developer\n  - Prototype or MVP\n  - Don't want to manage infrastructure\n  - Budget is not the primary concern\n\nChoose Vercel/Railway/Render when:\n  - Modern JS/TS stack\n  - Want Heroku-like simplicity\n  - Better pricing than Heroku\n  - GitHub-integrated deployment\n```\n\n## Modern Alternatives\n\n### Railway\n\n```bash\n# Deploy from GitHub or CLI\nrailway init\nrailway up\n\n# Add PostgreSQL\nrailway add --plugin postgresql\n\n# Environment variables set automatically\n```\n\n### Render\n\n```yaml\n# render.yaml\nservices:\n  - type: web\n    name: myapp-api\n    runtime: node\n    buildCommand: npm ci && npm run build\n    startCommand: node dist/server.js\n    envVars:\n      - key: NODE_ENV\n        value: production\n      - key: DATABASE_URL\n        fromDatabase:\n          name: myapp-db\n          property: connectionString\n\ndatabases:\n  - name: myapp-db\n    plan: free\n```\n\n### Fly.io\n\n```bash\n# Deploy globally (edge deployment)\nfly launch\nfly deploy\n\n# Add PostgreSQL\nfly postgres create\n\n# Scale to multiple regions\nfly regions add lhr sin\nfly scale count 3\n```\n\n## Common Pitfalls\n\n1. **Vendor lock-in** - Design your application with clean abstractions so you can switch platforms. Use environment variables for all configuration.\n2. **Not understanding pricing** - Each platform charges differently. Heroku charges per dyno/hour, AWS per resource, GCP per invocation/second.\n3. **Ignoring data residency** - Know which region your data lives in for compliance (GDPR, etc.).\n4. **Over-engineering for small apps** - A simple Heroku or Railway deployment is often better than a complex AWS setup for apps with < 10,000 users.\n5. **Not using managed databases** - Running your own PostgreSQL on a VM is asking for trouble. Use RDS, Cloud SQL, or Heroku Postgres.\n",
    "intuition": "Choosing a cloud platform is like choosing a restaurant for a group dinner. AWS is the massive buffet with hundreds of options but overwhelming choices. GCP is the modern fusion restaurant with some standout dishes (Cloud Run, BigQuery). Heroku is the fast-casual chain where you order at the counter and get food in minutes -- less customizable but incredibly convenient.",
    "exercises": [
      {
        "title": "Deploy to Cloud Run",
        "description": "Containerize a Node.js Express application and deploy it to Google Cloud Run. Configure it to scale to zero when idle and up to 5 instances under load. Connect it to a Cloud SQL PostgreSQL instance.",
        "hint": "Use gcloud run deploy --source . for automatic Dockerfile detection. Set --min-instances 0 and --max-instances 5. Use Cloud SQL proxy for database connection."
      },
      {
        "title": "Heroku Pipeline",
        "description": "Deploy a Node.js app to Heroku with a pipeline: staging and production environments. Configure review apps for pull requests. Set up automatic deploys from the main branch to staging and manual promotion to production.",
        "hint": "Use heroku pipelines:create. Add staging and production apps. Enable review apps in the Heroku Dashboard under Pipeline settings."
      },
      {
        "title": "Multi-Platform Comparison",
        "description": "Deploy the same Node.js application to AWS (EC2 or Lambda), GCP (Cloud Run), and Heroku. Document: deployment time, monthly cost estimate for 10,000 requests/day, cold start latency, and developer experience rating.",
        "hint": "Use free tiers where possible. Use curl to measure response times. Document each platform's deployment steps and count them."
      }
    ],
    "problems": [
      {
        "problem": "Heroku app crashes with H10 error",
        "cause": "The app is not binding to the PORT environment variable provided by Heroku (it uses a dynamic port, not 3000).",
        "solution": "Use `process.env.PORT || 3000` in your app: `app.listen(process.env.PORT || 3000)`. Check `heroku logs --tail` for the actual error. Ensure your start script in package.json is correct.",
        "symptoms": "heroku[router]: at=error code=H10 desc=\"App crashed\""
      },
      {
        "problem": "Cloud Run request timeout (504)",
        "cause": "The request took longer than the configured timeout (default 300 seconds), or the container is not responding to health checks.",
        "solution": "Ensure your app listens on the PORT environment variable (Cloud Run sets this). Increase the timeout with --timeout flag. Check container logs: `gcloud run services logs read myapp`. Verify the container starts within 4 minutes."
      },
      {
        "problem": "Database connection fails after platform sleep/idle",
        "cause": "The platform shut down the dyno/instance, and the database connection pool was lost. On restart, stale connections cause errors.",
        "solution": "Configure connection pool with proper idle timeout and retry logic. Use Prisma's built-in connection management. For Heroku, connections drop after 30 minutes of inactivity -- handle reconnection gracefully."
      }
    ],
    "further_reading": [
      {
        "title": "Google Cloud Run Documentation",
        "url": "https://cloud.google.com/run/docs",
        "type": "documentation"
      },
      {
        "title": "Heroku Dev Center - Node.js",
        "url": "https://devcenter.heroku.com/categories/nodejs-support",
        "type": "documentation"
      },
      {
        "title": "Railway Documentation",
        "url": "https://docs.railway.app/",
        "type": "documentation"
      },
      {
        "title": "Render Documentation",
        "url": "https://render.com/docs",
        "type": "documentation"
      }
    ]
  },
  "6.5.1": {
    "body": "# PM2: Process Management, Clustering, and Zero-Downtime Reload\n\n## Why PM2?\n\nNode.js runs on a single thread by default. If your server has 4 CPU cores, a single Node.js process uses only one of them. PM2 (Process Manager 2) solves this by managing multiple Node.js processes, providing clustering, zero-downtime reloads, log management, and monitoring.\n\n## PM2 Architecture\n\n```\n┌─────────────────────────────────────────────┐\n│                 PM2 Daemon                   │\n│                                              │\n│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │\n│  │ Worker 0 │  │ Worker 1 │  │ Worker 2 │  │\n│  │ (CPU 0)  │  │ (CPU 1)  │  │ (CPU 2)  │  │\n│  │ PID: 1234│  │ PID: 1235│  │ PID: 1236│  │\n│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │\n│       │              │              │        │\n│  ┌────┴──────────────┴──────────────┴──┐    │\n│  │     Cluster Module (Load Balancer)   │    │\n│  │     Round-robin distribution         │    │\n│  └──────────────────┬──────────────────┘    │\n│                     │                        │\n└─────────────────────┼────────────────────────┘\n                      │\n                 Port 3000\n```\n\n## Getting Started\n\n```bash\n# Install PM2 globally\nnpm install -g pm2\n\n# Start an application\npm2 start dist/server.js --name myapp\n\n# Start with cluster mode (use all CPU cores)\npm2 start dist/server.js --name myapp -i max\n\n# Start with specific number of instances\npm2 start dist/server.js --name myapp -i 4\n\n# Common management commands\npm2 list                    # List all processes\npm2 status                  # Same as list\npm2 show myapp              # Detailed info about a process\npm2 logs                    # View all logs\npm2 logs myapp              # View specific app logs\npm2 logs myapp --lines 100  # Last 100 lines\npm2 monit                   # Real-time monitoring dashboard\npm2 restart myapp           # Restart (with downtime)\npm2 reload myapp            # Graceful reload (zero downtime)\npm2 stop myapp              # Stop\npm2 delete myapp            # Remove from PM2 list\n\n# Scale instances\npm2 scale myapp 6           # Scale to 6 instances\npm2 scale myapp +2          # Add 2 more instances\n```\n\n## Ecosystem Configuration File\n\n```javascript\n// ecosystem.config.js\nmodule.exports = {\n  apps: [\n    {\n      name: 'myapp-api',\n      script: 'dist/server.js',\n      instances: 'max',           // Use all CPU cores\n      exec_mode: 'cluster',       // Enable cluster mode\n      \n      // Environment\n      env: {\n        NODE_ENV: 'development',\n        PORT: 3000,\n      },\n      env_staging: {\n        NODE_ENV: 'staging',\n        PORT: 3000,\n      },\n      env_production: {\n        NODE_ENV: 'production',\n        PORT: 3000,\n      },\n\n      // Restart behavior\n      max_memory_restart: '512M',  // Restart if memory exceeds 512MB\n      restart_delay: 3000,         // Wait 3s between restarts\n      max_restarts: 10,            // Max restarts before stopping\n      min_uptime: 5000,            // Min uptime to consider \"started\"\n      \n      // Graceful shutdown\n      kill_timeout: 5000,          // Time to wait for graceful shutdown\n      listen_timeout: 10000,       // Time to wait for app to listen\n      shutdown_with_message: true, // Send shutdown message\n\n      // Logging\n      log_file: '/var/log/myapp/combined.log',\n      out_file: '/var/log/myapp/out.log',\n      error_file: '/var/log/myapp/error.log',\n      log_date_format: 'YYYY-MM-DD HH:mm:ss Z',\n      merge_logs: true,            // Merge cluster logs into one file\n\n      // Advanced\n      node_args: '--max-old-space-size=512',\n      watch: false,                // Don't watch in production\n      autorestart: true,\n      cron_restart: '0 3 * * *',   // Restart daily at 3 AM\n    },\n\n    // Worker process (not cluster mode)\n    {\n      name: 'myapp-worker',\n      script: 'dist/workers/queue.js',\n      instances: 1,\n      exec_mode: 'fork',\n      env_production: {\n        NODE_ENV: 'production',\n      },\n      max_memory_restart: '256M',\n    },\n  ],\n};\n```\n\n### Using the Ecosystem File\n\n```bash\n# Start all apps\npm2 start ecosystem.config.js\n\n# Start with specific environment\npm2 start ecosystem.config.js --env production\n\n# Reload (zero downtime)\npm2 reload ecosystem.config.js --env production\n\n# Restart specific app\npm2 restart myapp-api\n```\n\n## Zero-Downtime Reload\n\n```\nStandard Restart (with downtime):\n  Stop all ──── gap (no service) ──── Start all\n\nGraceful Reload (zero downtime):\n  Worker 0: serving ─── stop ─── start (new) ─── serving\n  Worker 1: serving ─────────── serving ── stop ─── start (new) ─── serving\n  Worker 2: serving ─────────── serving ────────── serving ── stop ─── start\n  \n  At least one worker is always serving!\n```\n\n### Implementing Graceful Shutdown\n\n```javascript\n// server.ts\nimport express from 'express';\n\nconst app = express();\n\n// ... routes ...\n\nconst server = app.listen(process.env.PORT || 3000, () => {\n  console.log(`Worker ${process.pid} listening`);\n  \n  // Tell PM2 the app is ready\n  if (process.send) {\n    process.send('ready');\n  }\n});\n\n// Handle graceful shutdown\nprocess.on('SIGINT', gracefulShutdown);\nprocess.on('SIGTERM', gracefulShutdown);\n\nfunction gracefulShutdown() {\n  console.log(`Worker ${process.pid}: Received shutdown signal`);\n\n  // Stop accepting new connections\n  server.close(() => {\n    console.log(`Worker ${process.pid}: Closed all connections`);\n    \n    // Close database, redis, etc.\n    // ...\n    \n    process.exit(0);\n  });\n\n  // Force shutdown after timeout\n  setTimeout(() => {\n    console.error(`Worker ${process.pid}: Forced shutdown`);\n    process.exit(1);\n  }, 10000);\n}\n```\n\n```javascript\n// ecosystem.config.js - enable wait_ready\n{\n  wait_ready: true,            // Wait for process.send('ready')\n  listen_timeout: 10000,       // Max time to wait for ready signal\n  kill_timeout: 10000,         // Time for graceful shutdown\n  shutdown_with_message: true,\n}\n```\n\n## PM2 Startup and Save\n\n```bash\n# Generate startup script (auto-start PM2 on system boot)\npm2 startup\n# Follow the printed command (usually: sudo env PATH=... pm2 startup ...)\n\n# Save current process list (restored on startup)\npm2 save\n\n# Resurrect saved processes\npm2 resurrect\n```\n\n## Monitoring and Metrics\n\n```bash\n# Built-in monitoring\npm2 monit\n\n# PM2 Plus (web dashboard)\npm2 plus               # Connect to pm2.io for web monitoring\n\n# JSON status (for custom monitoring)\npm2 jlist              # Full JSON status\n\n# Programmatic access\npm2 describe myapp-api\n```\n\n### Custom Metrics\n\n```javascript\n// In your application\nimport pmx from '@pm2/io';\n\nconst reqCounter = pmx.counter({\n  name: 'Requests/min',\n  type: 'counter',\n});\n\nconst responseTime = pmx.histogram({\n  name: 'Response Time',\n  measurement: 'mean',\n});\n\napp.use((req, res, next) => {\n  reqCounter.inc();\n  const start = Date.now();\n  \n  res.on('finish', () => {\n    responseTime.update(Date.now() - start);\n  });\n  \n  next();\n});\n```\n\n## Log Management\n\n```bash\n# View logs\npm2 logs                      # All logs\npm2 logs myapp-api            # Specific app\npm2 logs --json               # JSON format\npm2 logs --format             # Formatted output\n\n# Flush logs\npm2 flush                     # Clear all log files\n\n# Log rotation\npm2 install pm2-logrotate\npm2 set pm2-logrotate:max_size 50M\npm2 set pm2-logrotate:retain 7\npm2 set pm2-logrotate:compress true\n```\n\n## Common Pitfalls\n\n1. **Using `pm2 restart` instead of `pm2 reload`** - `restart` kills all processes at once (downtime). `reload` does zero-downtime rolling restart.\n2. **Not using `pm2 save` after changes** - If the server reboots, PM2 restores the last saved state. Always save after adding/removing apps.\n3. **Running too many instances** - `i max` on a 2-core machine creates 2 workers. More instances than cores leads to context switching overhead.\n4. **Not implementing graceful shutdown** - Without SIGTERM handling, connections are dropped during reload.\n5. **Forgetting `pm2 startup`** - Without this, PM2 and your apps do not start after a server reboot.\n",
    "intuition": "PM2 is like a restaurant manager who oversees multiple chefs (Node.js processes). If one chef takes a break (crashes), the manager immediately brings in a replacement. During a menu change (code update), the manager swaps out chefs one at a time so customers (requests) are always being served. The manager also keeps track of each chef's performance and writes everything in a log book.",
    "exercises": [
      {
        "title": "Cluster Mode Setup",
        "description": "Create an Express app that logs the worker PID for each request. Deploy it with PM2 in cluster mode with 4 instances. Send multiple requests and verify that different workers handle them (round-robin). Then perform a zero-downtime reload and confirm no requests are dropped.",
        "hint": "Use process.pid in the response. Use 'ab' (Apache Bench) or 'autocannon' to send concurrent requests during reload. Check that no 502 errors occur."
      },
      {
        "title": "Ecosystem Configuration",
        "description": "Create a PM2 ecosystem.config.js for a production application with: API server (cluster mode, 4 instances), background worker (fork mode, 1 instance), and a scheduled job. Configure memory limits, log rotation, graceful shutdown, and environment-specific variables.",
        "hint": "Use exec_mode: 'cluster' for the API and 'fork' for the worker. Set max_memory_restart and cron_restart. Configure wait_ready and kill_timeout for graceful shutdown."
      },
      {
        "title": "Monitoring Dashboard",
        "description": "Set up PM2 monitoring with custom metrics: request count, average response time, and error rate. Install pm2-logrotate and configure it to keep 7 days of logs with max 50MB per file.",
        "hint": "Use @pm2/io for custom metrics. Install pm2-logrotate with 'pm2 install pm2-logrotate'. Use 'pm2 monit' for the terminal dashboard."
      }
    ],
    "problems": [
      {
        "problem": "PM2 cluster mode not distributing load evenly",
        "cause": "Node.js cluster module uses round-robin by default on Linux, but OS-level scheduling on other platforms. Some requests may be heavier than others.",
        "solution": "On Linux, round-robin is the default and works well. Ensure all workers are healthy with `pm2 list`. If one worker consistently has higher CPU, check for memory leaks or blocking operations. Use `pm2 monit` to compare workers."
      },
      {
        "problem": "PM2 reload causes brief 502 errors",
        "cause": "The application is not signaling 'ready' to PM2, so PM2 starts routing traffic before the app is fully initialized (database connected, etc.).",
        "solution": "Add `process.send('ready')` after your server starts listening and all connections are established. Set `wait_ready: true` and `listen_timeout: 10000` in ecosystem config. Implement proper SIGTERM handling for graceful shutdown."
      },
      {
        "problem": "PM2 processes not starting after server reboot",
        "cause": "PM2 startup script was not generated or the process list was not saved.",
        "solution": "Run `pm2 startup` and execute the printed sudo command. Then run `pm2 save` to persist the current process list. Verify with `pm2 list` after a reboot (or `sudo reboot` to test)."
      }
    ],
    "further_reading": [
      {
        "title": "PM2 Documentation",
        "url": "https://pm2.keymetrics.io/docs/usage/pm2-doc-single-page/",
        "type": "documentation"
      },
      {
        "title": "PM2 Cluster Mode",
        "url": "https://pm2.keymetrics.io/docs/usage/cluster-mode/",
        "type": "documentation"
      },
      {
        "title": "PM2 Ecosystem File",
        "url": "https://pm2.keymetrics.io/docs/usage/application-declaration/",
        "type": "documentation"
      },
      {
        "title": "Node.js Cluster Module",
        "url": "https://nodejs.org/api/cluster.html",
        "type": "documentation"
      }
    ]
  },
  "6.5.2": {
    "body": "# Nginx as Reverse Proxy and Load Balancer for Node.js\n\n## Why Nginx in Front of Node.js?\n\nNode.js can serve HTTP directly, but placing Nginx in front provides: SSL termination, static file serving, load balancing, request buffering, gzip compression, rate limiting, and security headers. Nginx handles these tasks more efficiently than Node.js.\n\n## Architecture\n\n```\nInternet\n   │\n   ▼\n┌──────────────────────────────────────────┐\n│           Nginx (Port 80/443)             │\n│                                          │\n│  - SSL Termination                       │\n│  - Static files (/public)               │\n│  - Gzip compression                      │\n│  - Rate limiting                         │\n│  - Security headers                      │\n│  - Request buffering                     │\n│                                          │\n│  ┌─────────── Upstream ───────────────┐  │\n│  │                                     │  │\n│  │  ┌──────┐  ┌──────┐  ┌──────┐    │  │\n│  │  │:3000 │  │:3001 │  │:3002 │    │  │\n│  │  │Node  │  │Node  │  │Node  │    │  │\n│  │  │(PM2) │  │(PM2) │  │(PM2) │    │  │\n│  │  └──────┘  └──────┘  └──────┘    │  │\n│  │                                     │  │\n│  └─────────────────────────────────────┘  │\n└──────────────────────────────────────────┘\n```\n\n## Basic Reverse Proxy Configuration\n\n```nginx\n# /etc/nginx/sites-available/myapp\nserver {\n    listen 80;\n    server_name myapp.com www.myapp.com;\n\n    # Redirect HTTP to HTTPS\n    return 301 https://$server_name$request_uri;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name myapp.com www.myapp.com;\n\n    # SSL certificates\n    ssl_certificate /etc/letsencrypt/live/myapp.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/myapp.com/privkey.pem;\n\n    # SSL settings\n    ssl_protocols TLSv1.2 TLSv1.3;\n    ssl_ciphers HIGH:!aNULL:!MD5;\n    ssl_prefer_server_ciphers on;\n    ssl_session_cache shared:SSL:10m;\n    ssl_session_timeout 10m;\n\n    # Security headers\n    add_header X-Frame-Options \"SAMEORIGIN\" always;\n    add_header X-Content-Type-Options \"nosniff\" always;\n    add_header X-XSS-Protection \"1; mode=block\" always;\n    add_header Strict-Transport-Security \"max-age=31536000; includeSubDomains\" always;\n    add_header Referrer-Policy \"strict-origin-when-cross-origin\" always;\n\n    # Gzip compression\n    gzip on;\n    gzip_vary on;\n    gzip_proxied any;\n    gzip_comp_level 6;\n    gzip_types text/plain text/css application/json application/javascript\n               text/xml application/xml application/xml+rss text/javascript;\n\n    # Static files (served directly by Nginx)\n    location /static/ {\n        alias /var/www/myapp/public/;\n        expires 1y;\n        add_header Cache-Control \"public, immutable\";\n        access_log off;\n    }\n\n    # API proxy to Node.js\n    location / {\n        proxy_pass http://localhost:3000;\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection 'upgrade';\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_cache_bypass $http_upgrade;\n\n        # Timeouts\n        proxy_connect_timeout 60s;\n        proxy_send_timeout 60s;\n        proxy_read_timeout 60s;\n    }\n\n    # Health check endpoint (no logging)\n    location /health {\n        proxy_pass http://localhost:3000/health;\n        access_log off;\n    }\n\n    # Block common attack paths\n    location ~ /\\. {\n        deny all;\n        access_log off;\n        log_not_found off;\n    }\n\n    # Request size limit\n    client_max_body_size 10M;\n\n    # Logging\n    access_log /var/log/nginx/myapp-access.log;\n    error_log /var/log/nginx/myapp-error.log;\n}\n```\n\n## Load Balancing\n\n```nginx\n# /etc/nginx/conf.d/upstream.conf\nupstream myapp_backend {\n    # Load balancing methods:\n    # (default) Round Robin - distributes sequentially\n    # least_conn - sends to server with fewest connections\n    # ip_hash - same client always goes to same server\n\n    least_conn;\n\n    server 127.0.0.1:3000 weight=3;    # Gets 3x traffic\n    server 127.0.0.1:3001 weight=2;\n    server 127.0.0.1:3002;\n    \n    server 127.0.0.1:3003 backup;       # Only used when others are down\n\n    # Health checks (Nginx Plus / OpenResty)\n    # Or use max_fails for passive checks\n    server 127.0.0.1:3000 max_fails=3 fail_timeout=30s;\n\n    keepalive 32;    # Persistent connections to upstream\n}\n\nserver {\n    listen 80;\n\n    location / {\n        proxy_pass http://myapp_backend;\n        proxy_http_version 1.1;\n        proxy_set_header Connection \"\";   # Enable keepalive\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n    }\n}\n```\n\n## WebSocket Support\n\n```nginx\n# WebSocket connections need special handling\nlocation /ws {\n    proxy_pass http://myapp_backend;\n    proxy_http_version 1.1;\n    proxy_set_header Upgrade $http_upgrade;\n    proxy_set_header Connection \"upgrade\";\n    proxy_set_header Host $host;\n    \n    # Longer timeouts for WebSockets\n    proxy_read_timeout 86400s;\n    proxy_send_timeout 86400s;\n}\n```\n\n## Rate Limiting\n\n```nginx\n# Define rate limit zones\nhttp {\n    # 10 requests per second per IP\n    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;\n    \n    # 1 request per second for login\n    limit_req_zone $binary_remote_addr zone=login:10m rate=1r/s;\n\n    server {\n        # API rate limit (allow burst of 20)\n        location /api/ {\n            limit_req zone=api burst=20 nodelay;\n            proxy_pass http://myapp_backend;\n        }\n\n        # Strict rate limit for auth endpoints\n        location /api/auth/login {\n            limit_req zone=login burst=5;\n            proxy_pass http://myapp_backend;\n        }\n    }\n}\n```\n\n## Caching\n\n```nginx\n# Define cache zone\nproxy_cache_path /var/cache/nginx levels=1:2 \n    keys_zone=myapp_cache:10m \n    max_size=1g \n    inactive=60m \n    use_temp_path=off;\n\nserver {\n    location /api/public/ {\n        proxy_pass http://myapp_backend;\n        proxy_cache myapp_cache;\n        proxy_cache_valid 200 10m;      # Cache 200 responses for 10 min\n        proxy_cache_valid 404 1m;       # Cache 404 for 1 min\n        proxy_cache_use_stale error timeout updating;\n        add_header X-Cache-Status $upstream_cache_status;\n    }\n    \n    # Don't cache authenticated routes\n    location /api/user/ {\n        proxy_pass http://myapp_backend;\n        proxy_no_cache 1;               # Never cache\n    }\n}\n```\n\n## Express Configuration for Nginx Proxy\n\n```javascript\n// In your Express app\nimport express from 'express';\n\nconst app = express();\n\n// Trust the Nginx proxy (important for req.ip, req.protocol)\napp.set('trust proxy', 1);\n\n// Now req.ip returns the real client IP (X-Forwarded-For)\n// req.protocol returns 'https' (X-Forwarded-Proto)\napp.get('/api/info', (req, res) => {\n  res.json({\n    ip: req.ip,              // Real client IP, not 127.0.0.1\n    protocol: req.protocol,  // 'https', not 'http'\n  });\n});\n```\n\n## Nginx Management Commands\n\n```bash\n# Test configuration (always do this before reload!)\nsudo nginx -t\n\n# Reload configuration (no downtime)\nsudo nginx -s reload\n\n# Start/stop\nsudo systemctl start nginx\nsudo systemctl stop nginx\nsudo systemctl restart nginx\n\n# View logs\ntail -f /var/log/nginx/access.log\ntail -f /var/log/nginx/error.log\n\n# Enable a site\nsudo ln -s /etc/nginx/sites-available/myapp /etc/nginx/sites-enabled/\nsudo nginx -t && sudo nginx -s reload\n```\n\n## Common Pitfalls\n\n1. **Not setting `proxy_set_header Host`** - Without this, your Node.js app sees 'localhost' as the host instead of the actual domain.\n2. **Forgetting `app.set('trust proxy', 1)` in Express** - Without this, `req.ip` returns 127.0.0.1 instead of the real client IP.\n3. **Not enabling WebSocket headers** - WebSocket connections fail without `Upgrade` and `Connection` headers.\n4. **Not running `nginx -t` before reload** - A syntax error in the config will take Nginx down on restart. Always test first.\n5. **Serving static files through Node.js** - Nginx serves static files 10-100x faster than Node.js. Offload static files to Nginx.\n",
    "intuition": "Nginx is like a hotel concierge standing at the front door. Guests (requests) arrive and the concierge handles simple tasks directly (serving static files, redirecting). For complex requests, the concierge directs guests to the right staff member (Node.js worker). The concierge also handles security (checking IDs/SSL), manages the queue (rate limiting), and ensures VIPs (WebSocket connections) get a dedicated line.",
    "exercises": [
      {
        "title": "Production Nginx Config",
        "description": "Write a complete Nginx configuration for a Node.js application with: HTTPS redirect, SSL termination, gzip compression, static file serving with cache headers, reverse proxy to Node.js, WebSocket support, rate limiting on API routes, and security headers.",
        "hint": "Create separate location blocks for /static, /api, /ws, and /health. Use proxy_set_header for all necessary headers. Test with 'nginx -t'."
      },
      {
        "title": "Load Balancing Setup",
        "description": "Configure Nginx to load-balance across 3 Node.js instances (ports 3000, 3001, 3002). Test with different algorithms (round-robin, least_conn, ip_hash). Use autocannon to benchmark each algorithm under load.",
        "hint": "Define an upstream block with all 3 servers. Change the algorithm and benchmark with 'autocannon -c 100 -d 10 http://localhost'. Compare requests/sec."
      },
      {
        "title": "Nginx + Docker Compose",
        "description": "Add Nginx as a reverse proxy to a Docker Compose setup with 2 Node.js API instances. Configure Nginx to load-balance between them, serve health checks, and handle SSL with self-signed certificates for development.",
        "hint": "Use Docker Compose service names as hostnames in the upstream block. Mount the nginx.conf as a volume. Generate self-signed certs with openssl."
      }
    ],
    "problems": [
      {
        "problem": "502 Bad Gateway from Nginx",
        "cause": "Nginx cannot connect to the upstream Node.js server. The app is not running, crashed, or is on a different port.",
        "solution": "Check if Node.js is running: `pm2 status` or `curl localhost:3000`. Check Nginx error log: `tail /var/log/nginx/error.log`. Verify the proxy_pass URL matches the Node.js listening port. Check firewall rules between Nginx and Node.js."
      },
      {
        "problem": "WebSocket connections drop after 60 seconds",
        "cause": "Nginx's default proxy_read_timeout is 60 seconds. Idle WebSocket connections are closed after this timeout.",
        "solution": "Increase timeouts for WebSocket locations: `proxy_read_timeout 86400s;` (24 hours). Also set `proxy_send_timeout 86400s;`. Implement ping/pong keepalive in your WebSocket application."
      },
      {
        "problem": "Real client IP not available in Express (shows 127.0.0.1)",
        "cause": "Express does not trust the proxy headers by default, so it ignores X-Forwarded-For and X-Real-IP.",
        "solution": "Add `app.set('trust proxy', 1)` in Express. Ensure Nginx sets the headers: `proxy_set_header X-Real-IP $remote_addr;` and `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`."
      }
    ],
    "further_reading": [
      {
        "title": "Nginx Documentation",
        "url": "https://nginx.org/en/docs/",
        "type": "documentation"
      },
      {
        "title": "Nginx Reverse Proxy Guide",
        "url": "https://docs.nginx.com/nginx/admin-guide/web-server/reverse-proxy/",
        "type": "documentation"
      },
      {
        "title": "DigitalOcean - Nginx as Node.js Reverse Proxy",
        "url": "https://www.digitalocean.com/community/tutorials/how-to-set-up-a-node-js-application-for-production-on-ubuntu-22-04",
        "type": "tutorial"
      },
      {
        "title": "Mozilla SSL Config Generator",
        "url": "https://ssl-config.mozilla.org/",
        "type": "tool",
        "note": "Generate secure SSL configuration for Nginx"
      }
    ]
  },
  "6.5.3": {
    "body": "# SSL/TLS Termination and HTTPS Setup\n\n## Why HTTPS is Non-Negotiable\n\nHTTPS encrypts traffic between clients and your server, preventing eavesdropping, tampering, and impersonation. Modern browsers mark HTTP sites as \"Not Secure.\" Search engines penalize HTTP. Many APIs (geolocation, service workers, HTTP/2) require HTTPS.\n\n## How TLS Works\n\n```\nClient                                   Server\n  │                                        │\n  │──── ClientHello (supported ciphers) ──►│\n  │                                        │\n  │◄── ServerHello (chosen cipher) ────────│\n  │◄── Certificate (public key) ───────────│\n  │                                        │\n  │──── Verify certificate with CA ────────│\n  │──── Key Exchange ──────────────────────►│\n  │                                        │\n  │◄════ Encrypted Connection ════════════►│\n  │      (symmetric encryption)            │\n  │                                        │\n```\n\n### TLS Termination at Nginx\n\n```\nClient ──HTTPS──► Nginx ──HTTP──► Node.js\n                  (TLS           (internal\n                  terminates     network,\n                  here)          no encryption\n                                 needed)\n```\n\n## Let's Encrypt with Certbot (Free SSL)\n\n### Installation and First Certificate\n\n```bash\n# Install Certbot\nsudo apt update\nsudo apt install certbot python3-certbot-nginx\n\n# Get a certificate (automatic Nginx configuration)\nsudo certbot --nginx -d myapp.com -d www.myapp.com\n\n# Or standalone mode (when Nginx is not yet configured)\nsudo certbot certonly --standalone -d myapp.com\n\n# Certificate files are stored in:\n# /etc/letsencrypt/live/myapp.com/fullchain.pem  (certificate + chain)\n# /etc/letsencrypt/live/myapp.com/privkey.pem    (private key)\n```\n\n### Auto-Renewal\n\n```bash\n# Test renewal\nsudo certbot renew --dry-run\n\n# Certbot installs a timer/cron automatically, but verify:\nsudo systemctl status certbot.timer\n\n# Or add manual cron\n# 0 0 1,15 * * certbot renew --quiet --post-hook \"systemctl reload nginx\"\n```\n\n### Certbot with Docker\n\n```yaml\n# docker-compose.yml\nservices:\n  nginx:\n    image: nginx:1.25-alpine\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./nginx/conf.d:/etc/nginx/conf.d:ro\n      - certbot-conf:/etc/letsencrypt:ro\n      - certbot-www:/var/www/certbot:ro\n\n  certbot:\n    image: certbot/certbot\n    volumes:\n      - certbot-conf:/etc/letsencrypt\n      - certbot-www:/var/www/certbot\n    command: certonly --webroot -w /var/www/certbot\n             --email admin@myapp.com -d myapp.com\n             --agree-tos --no-eff-email\n\nvolumes:\n  certbot-conf:\n  certbot-www:\n```\n\n```nginx\n# nginx/conf.d/default.conf\n# First, get the certificate with HTTP only:\nserver {\n    listen 80;\n    server_name myapp.com;\n\n    # Certbot challenge\n    location /.well-known/acme-challenge/ {\n        root /var/www/certbot;\n    }\n\n    location / {\n        return 301 https://$host$request_uri;\n    }\n}\n\n# Then add HTTPS server block:\nserver {\n    listen 443 ssl http2;\n    server_name myapp.com;\n\n    ssl_certificate /etc/letsencrypt/live/myapp.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/myapp.com/privkey.pem;\n\n    # Include recommended SSL settings\n    include /etc/letsencrypt/options-ssl-nginx.conf;\n    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;\n\n    location / {\n        proxy_pass http://api:3000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n}\n```\n\n## SSL Configuration Best Practices\n\n```nginx\nserver {\n    listen 443 ssl http2;\n    server_name myapp.com;\n\n    # Certificate\n    ssl_certificate /etc/letsencrypt/live/myapp.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/myapp.com/privkey.pem;\n\n    # Protocol versions (disable TLS 1.0 and 1.1)\n    ssl_protocols TLSv1.2 TLSv1.3;\n\n    # Cipher suites (modern configuration)\n    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;\n    ssl_prefer_server_ciphers off;\n\n    # OCSP Stapling (faster certificate verification)\n    ssl_stapling on;\n    ssl_stapling_verify on;\n    ssl_trusted_certificate /etc/letsencrypt/live/myapp.com/chain.pem;\n    resolver 8.8.8.8 8.8.4.4 valid=300s;\n    resolver_timeout 5s;\n\n    # Session settings\n    ssl_session_cache shared:SSL:10m;\n    ssl_session_timeout 1d;\n    ssl_session_tickets off;\n\n    # Security headers\n    add_header Strict-Transport-Security \"max-age=63072000; includeSubDomains; preload\" always;\n    add_header X-Frame-Options DENY always;\n    add_header X-Content-Type-Options nosniff always;\n    add_header X-XSS-Protection \"1; mode=block\" always;\n    add_header Content-Security-Policy \"default-src 'self'\" always;\n}\n```\n\n## Self-Signed Certificates (Development)\n\n```bash\n# Generate self-signed certificate for local development\nopenssl req -x509 -nodes -days 365 -newkey rsa:2048 \\\n  -keyout /etc/ssl/private/selfsigned.key \\\n  -out /etc/ssl/certs/selfsigned.crt \\\n  -subj \"/C=US/ST=State/L=City/O=Dev/CN=localhost\"\n\n# Generate for multiple domains (SAN)\nopenssl req -x509 -nodes -days 365 -newkey rsa:2048 \\\n  -keyout localhost.key \\\n  -out localhost.crt \\\n  -subj \"/CN=localhost\" \\\n  -addext \"subjectAltName=DNS:localhost,DNS:*.localhost,IP:127.0.0.1\"\n```\n\n## HTTPS in Node.js (Direct, Without Nginx)\n\n```javascript\n// For cases where you need Node.js to handle TLS directly\nimport https from 'https';\nimport fs from 'fs';\nimport express from 'express';\n\nconst app = express();\n\n// ... routes ...\n\nconst options = {\n  key: fs.readFileSync('/etc/ssl/private/server.key'),\n  cert: fs.readFileSync('/etc/ssl/certs/server.crt'),\n};\n\nhttps.createServer(options, app).listen(443, () => {\n  console.log('HTTPS server running on port 443');\n});\n\n// Redirect HTTP to HTTPS\nimport http from 'http';\nhttp.createServer((req, res) => {\n  res.writeHead(301, { Location: `https://${req.headers.host}${req.url}` });\n  res.end();\n}).listen(80);\n```\n\n## Testing SSL Configuration\n\n```bash\n# Test with curl\ncurl -vI https://myapp.com 2>&1 | grep -E \"SSL|TLS|subject|issuer|expire\"\n\n# Check certificate details\nopenssl s_client -connect myapp.com:443 -servername myapp.com < /dev/null 2>/dev/null | openssl x509 -noout -dates -subject -issuer\n\n# Check certificate expiration\necho | openssl s_client -servername myapp.com -connect myapp.com:443 2>/dev/null | openssl x509 -noout -enddate\n\n# SSL Labs grade (online)\n# https://www.ssllabs.com/ssltest/analyze.html?d=myapp.com\n```\n\n### Certificate Monitoring Script\n\n```bash\n#!/bin/bash\n# check-ssl.sh - Monitor certificate expiration\n\nDOMAIN=\"myapp.com\"\nWARN_DAYS=30\n\nEXPIRY=$(echo | openssl s_client -servername \"$DOMAIN\" -connect \"$DOMAIN:443\" 2>/dev/null | openssl x509 -noout -enddate | cut -d= -f2)\n\nEXPIRY_EPOCH=$(date -d \"$EXPIRY\" +%s 2>/dev/null || date -j -f \"%b %d %T %Y %Z\" \"$EXPIRY\" +%s)\nNOW_EPOCH=$(date +%s)\nDAYS_LEFT=$(( (EXPIRY_EPOCH - NOW_EPOCH) / 86400 ))\n\necho \"Certificate for $DOMAIN expires in $DAYS_LEFT days ($EXPIRY)\"\n\nif [[ $DAYS_LEFT -lt $WARN_DAYS ]]; then\n    echo \"WARNING: Certificate expiring soon!\"\n    # Send alert (email, Slack, etc.)\n    exit 1\nfi\n```\n\n## HSTS (HTTP Strict Transport Security)\n\n```nginx\n# Tell browsers to ALWAYS use HTTPS for this domain\nadd_header Strict-Transport-Security \"max-age=63072000; includeSubDomains; preload\" always;\n\n# max-age: how long (seconds) browsers should remember to use HTTPS\n# includeSubDomains: applies to all subdomains\n# preload: submit to browser preload list (hstspreload.org)\n```\n\n## Common Pitfalls\n\n1. **Not auto-renewing certificates** - Let's Encrypt certificates expire in 90 days. Always set up auto-renewal with certbot.\n2. **Mixed content** - HTTPS page loading HTTP resources (images, scripts) causes browser warnings. Ensure all resources use HTTPS or protocol-relative URLs.\n3. **Forgetting to redirect HTTP to HTTPS** - Users typing your domain without 'https://' get the insecure version. Always redirect.\n4. **Not setting HSTS** - Without HSTS, the first visit can be intercepted before the redirect. HSTS tells browsers to always use HTTPS.\n5. **Self-signed certificates in production** - Browsers will show scary warnings. Always use Let's Encrypt or a CA-signed certificate for production.\n",
    "intuition": "SSL/TLS is like putting your letter (HTTP request) in a tamper-proof, addressed envelope before mailing it. Without encryption, anyone who handles the mail (routers, ISPs, attackers on public WiFi) can read the contents. TLS termination at Nginx is like having a secure mailroom at the building entrance -- the mailroom opens the encrypted envelope and passes the letter internally through the secure building (private network) to the right office (Node.js).",
    "exercises": [
      {
        "title": "HTTPS with Let's Encrypt",
        "description": "Set up HTTPS for a Node.js application using Certbot and Nginx. Configure auto-renewal, HTTP to HTTPS redirect, HSTS header, and test with SSL Labs to achieve an A+ grade.",
        "hint": "Run certbot --nginx for automatic configuration. Add HSTS header. Disable TLS 1.0/1.1. Use ssl_prefer_server_ciphers and modern cipher suites. Check with ssllabs.com."
      },
      {
        "title": "Docker + HTTPS",
        "description": "Create a Docker Compose setup with Nginx handling SSL termination for a Node.js API. Use Certbot in a separate container for certificate management. Include a renewal script that runs via cron.",
        "hint": "Use shared volumes between Nginx and Certbot containers. The certbot container runs once for initial cert, then you add a cron or script to renew periodically."
      },
      {
        "title": "Certificate Monitoring",
        "description": "Write a script that checks SSL certificate expiration for a list of domains and alerts (via stdout or webhook) if any certificate expires within 30 days. Set it up as a cron job that runs daily.",
        "hint": "Use openssl s_client to connect and openssl x509 to read the expiration date. Compare with current date. Loop over an array of domains."
      }
    ],
    "problems": [
      {
        "problem": "Certbot fails with 'connection refused' during challenge",
        "cause": "Port 80 is not open in the firewall/security group, or another service is using port 80.",
        "solution": "Ensure port 80 is open: `sudo ufw allow 80`. Check if Nginx is running and listening on port 80. If using standalone mode, stop Nginx first. For AWS, check the security group allows inbound port 80."
      },
      {
        "problem": "Browser shows 'Your connection is not private' after setting up SSL",
        "cause": "Using a self-signed certificate, certificate does not match the domain, or the certificate chain is incomplete.",
        "solution": "In production, use Let's Encrypt (free and trusted). Ensure the domain in the certificate matches the URL. Use fullchain.pem (not just cert.pem) to include the full certificate chain."
      },
      {
        "problem": "Mixed content warnings after enabling HTTPS",
        "cause": "The page is served over HTTPS but includes resources (images, scripts, API calls) loaded over HTTP.",
        "solution": "Update all resource URLs to use HTTPS or protocol-relative URLs (//example.com/resource). Add Content-Security-Policy header: `upgrade-insecure-requests`. Search your codebase for 'http://' references."
      }
    ],
    "further_reading": [
      {
        "title": "Let's Encrypt Documentation",
        "url": "https://letsencrypt.org/docs/",
        "type": "documentation"
      },
      {
        "title": "Mozilla SSL Configuration Generator",
        "url": "https://ssl-config.mozilla.org/",
        "type": "tool"
      },
      {
        "title": "SSL Labs Server Test",
        "url": "https://www.ssllabs.com/ssltest/",
        "type": "tool",
        "note": "Test your server's SSL configuration and get a grade"
      },
      {
        "title": "OWASP TLS Cheat Sheet",
        "url": "https://cheatsheetseries.owasp.org/cheatsheets/Transport_Layer_Security_Cheat_Sheet.html",
        "type": "reference"
      }
    ]
  },
  "6.6.1": {
    "body": "# Terraform Basics: Providers, Resources, State\n\n## What is Infrastructure as Code (IaC)?\n\nInfrastructure as Code means defining your servers, databases, networks, and other infrastructure in declarative configuration files instead of manually clicking through a cloud console. Terraform is the most popular IaC tool, supporting AWS, GCP, Azure, and dozens of other providers.\n\n## Why Terraform?\n\n```\nManual (Console/CLI):                 Terraform (IaC):\n─────────────────────                 ─────────────────\nClick \"Create Instance\"               Write .tf file\nFill in form fields                   terraform plan (preview)\nClick \"Launch\"                        terraform apply (create)\nHope you remember settings            Config is version-controlled\nNo audit trail                        Full change history in git\nHard to replicate                     Exact replication every time\nHard to tear down                     terraform destroy\n```\n\n## Terraform Architecture\n\n```\n┌────────────────────────────────┐\n│     Terraform Configuration     │\n│         (.tf files)             │\n│                                 │\n│  ┌───────────┐  ┌───────────┐ │\n│  │ Providers │  │ Resources │ │\n│  │ (AWS,GCP) │  │ (EC2,RDS) │ │\n│  └───────────┘  └───────────┘ │\n└──────────┬─────────────────────┘\n           │\n           ▼\n┌────────────────────────────────┐\n│       Terraform Core            │\n│                                 │\n│  Plan ──► Apply ──► State       │\n│                                 │\n└──────────┬─────────────────────┘\n           │\n           ▼\n┌────────────────────────────────┐\n│     Cloud Provider APIs         │\n│  (AWS, GCP, Azure, etc.)       │\n└────────────────────────────────┘\n```\n\n## Getting Started\n\n```bash\n# Install Terraform\nbrew install terraform\n\n# Verify\nterraform version\n```\n\n### Project Structure\n\n```\ninfrastructure/\n├── main.tf              # Main resources\n├── variables.tf         # Input variables\n├── outputs.tf           # Output values\n├── providers.tf         # Provider configuration\n├── terraform.tfvars     # Variable values (gitignored!)\n├── backend.tf           # State storage configuration\n└── modules/\n    ├── vpc/\n    │   ├── main.tf\n    │   ├── variables.tf\n    │   └── outputs.tf\n    └── app/\n        ├── main.tf\n        ├── variables.tf\n        └── outputs.tf\n```\n\n## Core Concepts\n\n### Providers\n\n```hcl\n# providers.tf\nterraform {\n  required_version = \">= 1.5\"\n  \n  required_providers {\n    aws = {\n      source  = \"hashicorp/aws\"\n      version = \"~> 5.0\"\n    }\n  }\n}\n\nprovider \"aws\" {\n  region = var.aws_region\n  \n  default_tags {\n    tags = {\n      Environment = var.environment\n      ManagedBy   = \"terraform\"\n      Project     = \"myapp\"\n    }\n  }\n}\n```\n\n### Resources\n\n```hcl\n# main.tf\n\n# VPC\nresource \"aws_vpc\" \"main\" {\n  cidr_block           = \"10.0.0.0/16\"\n  enable_dns_hostnames = true\n  enable_dns_support   = true\n\n  tags = {\n    Name = \"${var.project}-vpc\"\n  }\n}\n\n# Public Subnet\nresource \"aws_subnet\" \"public\" {\n  count             = 2\n  vpc_id            = aws_vpc.main.id\n  cidr_block        = \"10.0.${count.index + 1}.0/24\"\n  availability_zone = data.aws_availability_zones.available.names[count.index]\n\n  map_public_ip_on_launch = true\n\n  tags = {\n    Name = \"${var.project}-public-${count.index + 1}\"\n  }\n}\n\n# Security Group\nresource \"aws_security_group\" \"app\" {\n  name_prefix = \"${var.project}-app-\"\n  vpc_id      = aws_vpc.main.id\n\n  ingress {\n    from_port   = 80\n    to_port     = 80\n    protocol    = \"tcp\"\n    cidr_blocks = [\"0.0.0.0/0\"]\n  }\n\n  ingress {\n    from_port   = 443\n    to_port     = 443\n    protocol    = \"tcp\"\n    cidr_blocks = [\"0.0.0.0/0\"]\n  }\n\n  ingress {\n    from_port   = 22\n    to_port     = 22\n    protocol    = \"tcp\"\n    cidr_blocks = [var.admin_cidr]\n  }\n\n  egress {\n    from_port   = 0\n    to_port     = 0\n    protocol    = \"-1\"\n    cidr_blocks = [\"0.0.0.0/0\"]\n  }\n}\n\n# EC2 Instance\nresource \"aws_instance\" \"app\" {\n  ami           = data.aws_ami.amazon_linux.id\n  instance_type = var.instance_type\n  subnet_id     = aws_subnet.public[0].id\n  key_name      = var.key_pair_name\n\n  vpc_security_group_ids = [aws_security_group.app.id]\n\n  user_data = templatefile(\"${path.module}/scripts/setup.sh\", {\n    node_version = var.node_version\n    app_name     = var.project\n  })\n\n  root_block_device {\n    volume_size = 20\n    volume_type = \"gp3\"\n  }\n\n  tags = {\n    Name = \"${var.project}-server\"\n  }\n}\n\n# RDS PostgreSQL\nresource \"aws_db_instance\" \"postgres\" {\n  identifier     = \"${var.project}-db\"\n  engine         = \"postgres\"\n  engine_version = \"16.1\"\n  instance_class = var.db_instance_class\n\n  allocated_storage     = 20\n  max_allocated_storage = 100\n  storage_type          = \"gp3\"\n\n  db_name  = var.db_name\n  username = var.db_username\n  password = var.db_password\n\n  multi_az               = var.environment == \"production\"\n  db_subnet_group_name   = aws_db_subnet_group.main.name\n  vpc_security_group_ids = [aws_security_group.db.id]\n\n  backup_retention_period = 7\n  skip_final_snapshot     = var.environment != \"production\"\n  deletion_protection     = var.environment == \"production\"\n\n  storage_encrypted = true\n\n  tags = {\n    Name = \"${var.project}-db\"\n  }\n}\n\n# S3 Bucket\nresource \"aws_s3_bucket\" \"uploads\" {\n  bucket = \"${var.project}-uploads-${var.environment}\"\n}\n\nresource \"aws_s3_bucket_versioning\" \"uploads\" {\n  bucket = aws_s3_bucket.uploads.id\n  versioning_configuration {\n    status = \"Enabled\"\n  }\n}\n```\n\n### Variables\n\n```hcl\n# variables.tf\nvariable \"aws_region\" {\n  description = \"AWS region\"\n  type        = string\n  default     = \"us-east-1\"\n}\n\nvariable \"environment\" {\n  description = \"Environment (dev, staging, production)\"\n  type        = string\n  validation {\n    condition     = contains([\"dev\", \"staging\", \"production\"], var.environment)\n    error_message = \"Environment must be dev, staging, or production.\"\n  }\n}\n\nvariable \"project\" {\n  description = \"Project name\"\n  type        = string\n  default     = \"myapp\"\n}\n\nvariable \"instance_type\" {\n  description = \"EC2 instance type\"\n  type        = string\n  default     = \"t3.small\"\n}\n\nvariable \"db_password\" {\n  description = \"Database password\"\n  type        = string\n  sensitive   = true       # Won't show in logs\n}\n\nvariable \"db_instance_class\" {\n  description = \"RDS instance class\"\n  type        = string\n  default     = \"db.t3.micro\"\n}\n```\n\n```hcl\n# terraform.tfvars (gitignored!)\naws_region      = \"us-east-1\"\nenvironment     = \"production\"\ninstance_type   = \"t3.small\"\ndb_password     = \"super-secret-password\"\n```\n\n### Outputs\n\n```hcl\n# outputs.tf\noutput \"app_public_ip\" {\n  description = \"Public IP of the application server\"\n  value       = aws_instance.app.public_ip\n}\n\noutput \"db_endpoint\" {\n  description = \"RDS endpoint\"\n  value       = aws_db_instance.postgres.endpoint\n}\n\noutput \"s3_bucket\" {\n  description = \"S3 bucket name\"\n  value       = aws_s3_bucket.uploads.id\n}\n\noutput \"database_url\" {\n  description = \"Database connection URL\"\n  value       = \"postgresql://${var.db_username}:${var.db_password}@${aws_db_instance.postgres.endpoint}/${var.db_name}\"\n  sensitive   = true\n}\n```\n\n## Terraform State\n\nState is how Terraform tracks which real resources correspond to your configuration.\n\n```hcl\n# backend.tf - Store state remotely (recommended)\nterraform {\n  backend \"s3\" {\n    bucket         = \"myapp-terraform-state\"\n    key            = \"production/terraform.tfstate\"\n    region         = \"us-east-1\"\n    dynamodb_table = \"terraform-locks\"     # State locking\n    encrypt        = true\n  }\n}\n```\n\n## Terraform Commands\n\n```bash\n# Initialize (download providers, configure backend)\nterraform init\n\n# Preview changes\nterraform plan\n\n# Apply changes\nterraform apply\n\n# Apply with auto-approve (CI/CD)\nterraform apply -auto-approve\n\n# Destroy all resources\nterraform destroy\n\n# Show current state\nterraform show\n\n# List resources in state\nterraform state list\n\n# Import existing resource\nterraform import aws_instance.app i-1234567890abcdef0\n\n# Format files\nterraform fmt\n\n# Validate configuration\nterraform validate\n```\n\n## Terraform Plan Output\n\n```\n$ terraform plan\n\nTerraform will perform the following actions:\n\n  # aws_instance.app will be created\n  + resource \"aws_instance\" \"app\" {\n      + ami           = \"ami-0c55b159cbfafe1f0\"\n      + instance_type = \"t3.small\"\n      + tags          = {\n          + \"Name\" = \"myapp-server\"\n        }\n    }\n\n  # aws_db_instance.postgres will be created\n  + resource \"aws_db_instance\" \"postgres\" {\n      + engine         = \"postgres\"\n      + instance_class = \"db.t3.micro\"\n    }\n\nPlan: 2 to add, 0 to change, 0 to destroy.\n```\n\n## Common Pitfalls\n\n1. **Storing state locally** - Local state cannot be shared across teams and is not backed up. Always use remote state (S3, GCS).\n2. **Committing terraform.tfvars or .tfstate** - These contain secrets. Add to .gitignore.\n3. **Not using `terraform plan` before `apply`** - Always review what will change before applying.\n4. **Modifying infrastructure outside Terraform** - Manual changes create drift between state and reality. Always change through Terraform.\n5. **Not using state locking** - Without DynamoDB locking, two people running `terraform apply` simultaneously can corrupt state.\n",
    "intuition": "Terraform is like architectural blueprints for your cloud infrastructure. Instead of building a house by making decisions on the spot (clicking in the AWS console), you draw detailed blueprints first (write .tf files), review them (terraform plan), then build exactly what is specified (terraform apply). If you want to renovate, you update the blueprints and Terraform figures out what needs to change. The state file is like a property deed that records exactly what was built.",
    "exercises": [
      {
        "title": "Basic Infrastructure",
        "description": "Write Terraform configuration to create: a VPC with public and private subnets, an EC2 instance with a security group allowing HTTP and SSH, and an RDS PostgreSQL instance in the private subnet. Use variables for all configurable values.",
        "hint": "Start with the VPC, then subnets, internet gateway, route tables, security groups, and finally EC2 and RDS. Use count or for_each for multiple subnets."
      },
      {
        "title": "Remote State Setup",
        "description": "Configure Terraform to store state in an S3 bucket with DynamoDB locking. Create the S3 bucket and DynamoDB table manually (or with a separate Terraform config), then configure the backend.",
        "hint": "The S3 bucket for state must be created before configuring the backend. Use 'terraform init -migrate-state' to move existing local state to the remote backend."
      },
      {
        "title": "Multi-Environment Setup",
        "description": "Create a Terraform configuration that can deploy the same infrastructure to dev, staging, and production environments using different .tfvars files. Each environment should have different instance sizes and database configurations.",
        "hint": "Create dev.tfvars, staging.tfvars, and production.tfvars. Use 'terraform apply -var-file=production.tfvars'. Use conditionals for environment-specific settings (e.g., multi_az only in production)."
      }
    ],
    "problems": [
      {
        "problem": "Terraform destroy fails with dependency errors",
        "cause": "Resources have dependencies that prevent deletion (e.g., a security group is still attached to a running instance).",
        "solution": "Terraform usually handles dependency ordering. If it fails, try targeted destroy: `terraform destroy -target=aws_instance.app` first, then destroy the rest. Check for resources created outside Terraform that reference your resources."
      },
      {
        "problem": "State file out of sync with actual infrastructure",
        "cause": "Someone modified infrastructure manually through the AWS console, creating drift.",
        "solution": "Run `terraform plan` to detect drift. Use `terraform refresh` to update state with real-world data. For resources created outside Terraform, use `terraform import` to bring them under management. Going forward, enforce that all changes go through Terraform."
      },
      {
        "problem": "Terraform wants to destroy and recreate a resource instead of updating it",
        "cause": "Some resource attributes are immutable (forces new). Changing them requires destroying and recreating the resource.",
        "solution": "Check the `plan` output for 'forces replacement'. For databases, this is dangerous -- it will delete your data. Use lifecycle { prevent_destroy = true } to prevent accidental destruction. For immutable changes, use create_before_destroy lifecycle."
      }
    ],
    "further_reading": [
      {
        "title": "Terraform Documentation",
        "url": "https://developer.hashicorp.com/terraform/docs",
        "type": "documentation"
      },
      {
        "title": "Terraform AWS Provider",
        "url": "https://registry.terraform.io/providers/hashicorp/aws/latest/docs",
        "type": "documentation"
      },
      {
        "title": "Terraform Best Practices",
        "url": "https://www.terraform-best-practices.com/",
        "type": "guide"
      },
      {
        "title": "Learn Terraform - HashiCorp Tutorials",
        "url": "https://developer.hashicorp.com/terraform/tutorials",
        "type": "tutorial"
      }
    ]
  },
  "6.6.2": {
    "body": "# Kubernetes Introduction: Pods, Services, Deployments\n\n## What is Kubernetes?\n\nKubernetes (K8s) is a container orchestration platform that automates deployment, scaling, and management of containerized applications. It is the industry standard for running containers in production at scale.\n\n## When to Use Kubernetes\n\n```\nUse Kubernetes when:                 Skip Kubernetes when:\n────────────────────                 ─────────────────────\n- Multiple microservices             - Single monolith\n- Need auto-scaling                  - Small team (< 5 devs)\n- Multi-region deployment            - Simple deployment needs\n- High availability required         - Budget constraints\n- Team has K8s expertise             - PM2 + Nginx is sufficient\n- 10+ containers to manage           - < 5 containers\n```\n\n## Architecture Overview\n\n```\n┌─────────────────────────────────────────────────────────┐\n│                   Kubernetes Cluster                     │\n│                                                          │\n│  ┌─────────────────────────────────────────────────────┐│\n│  │              Control Plane (Master)                  ││\n│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           ││\n│  │  │API Server│ │Scheduler │ │Controller│           ││\n│  │  │          │ │          │ │ Manager  │           ││\n│  │  └──────────┘ └──────────┘ └──────────┘           ││\n│  │  ┌──────────┐                                      ││\n│  │  │  etcd    │ (cluster state database)              ││\n│  │  └──────────┘                                      ││\n│  └─────────────────────────────────────────────────────┘│\n│                                                          │\n│  ┌──── Node 1 ─────┐  ┌──── Node 2 ─────┐              │\n│  │ ┌─────┐ ┌─────┐ │  │ ┌─────┐ ┌─────┐ │              │\n│  │ │Pod  │ │Pod  │ │  │ │Pod  │ │Pod  │ │              │\n│  │ │(api)│ │(api)│ │  │ │(api)│ │(wrk)│ │              │\n│  │ └─────┘ └─────┘ │  │ └─────┘ └─────┘ │              │\n│  │    kubelet       │  │    kubelet       │              │\n│  │    kube-proxy    │  │    kube-proxy    │              │\n│  └──────────────────┘  └──────────────────┘              │\n└─────────────────────────────────────────────────────────┘\n```\n\n## Core Concepts\n\n### Pods\n\nA Pod is the smallest deployable unit -- one or more containers that share networking and storage.\n\n```yaml\n# pod.yaml (rarely created directly, use Deployments instead)\napiVersion: v1\nkind: Pod\nmetadata:\n  name: myapp\n  labels:\n    app: myapp\nspec:\n  containers:\n    - name: api\n      image: myorg/myapp:1.0.0\n      ports:\n        - containerPort: 3000\n      env:\n        - name: NODE_ENV\n          value: \"production\"\n        - name: DATABASE_URL\n          valueFrom:\n            secretKeyRef:\n              name: myapp-secrets\n              key: database-url\n      resources:\n        requests:\n          memory: \"128Mi\"\n          cpu: \"100m\"\n        limits:\n          memory: \"512Mi\"\n          cpu: \"500m\"\n      livenessProbe:\n        httpGet:\n          path: /health\n          port: 3000\n        initialDelaySeconds: 10\n        periodSeconds: 15\n      readinessProbe:\n        httpGet:\n          path: /ready\n          port: 3000\n        initialDelaySeconds: 5\n        periodSeconds: 10\n```\n\n### Deployments\n\nDeployments manage Pod replicas with rolling updates and rollbacks.\n\n```yaml\n# deployment.yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: myapp-api\n  labels:\n    app: myapp\n    component: api\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: myapp\n      component: api\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxUnavailable: 1        # At most 1 pod down during update\n      maxSurge: 1              # At most 1 extra pod during update\n  template:\n    metadata:\n      labels:\n        app: myapp\n        component: api\n    spec:\n      containers:\n        - name: api\n          image: myorg/myapp:1.0.0\n          ports:\n            - containerPort: 3000\n          env:\n            - name: NODE_ENV\n              value: \"production\"\n            - name: PORT\n              value: \"3000\"\n          envFrom:\n            - configMapRef:\n                name: myapp-config\n            - secretRef:\n                name: myapp-secrets\n          resources:\n            requests:\n              memory: \"256Mi\"\n              cpu: \"200m\"\n            limits:\n              memory: \"512Mi\"\n              cpu: \"500m\"\n          livenessProbe:\n            httpGet:\n              path: /health\n              port: 3000\n            initialDelaySeconds: 15\n            periodSeconds: 20\n          readinessProbe:\n            httpGet:\n              path: /ready\n              port: 3000\n            initialDelaySeconds: 5\n            periodSeconds: 10\n      imagePullSecrets:\n        - name: registry-credentials\n```\n\n### Services\n\nServices provide stable networking for Pods (which have dynamic IPs).\n\n```yaml\n# service.yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-api\nspec:\n  selector:\n    app: myapp\n    component: api\n  type: ClusterIP            # Internal only (default)\n  ports:\n    - port: 80               # Service port\n      targetPort: 3000       # Pod port\n      protocol: TCP\n\n---\n# External access via LoadBalancer\napiVersion: v1\nkind: Service\nmetadata:\n  name: myapp-api-public\nspec:\n  selector:\n    app: myapp\n    component: api\n  type: LoadBalancer          # Creates cloud load balancer\n  ports:\n    - port: 80\n      targetPort: 3000\n```\n\n### Service Types\n\n```\nClusterIP (default):\n  Internal only. Other pods can reach it by service name.\n  myapp-api:80 -> pods:3000\n\nNodePort:\n  Exposes on each node's IP at a static port.\n  <NodeIP>:30080 -> pods:3000\n\nLoadBalancer:\n  Creates external cloud load balancer.\n  external-ip:80 -> pods:3000\n\nExternalName:\n  Maps to a DNS name (CNAME).\n  mydb -> rds-endpoint.amazonaws.com\n```\n\n### ConfigMaps and Secrets\n\n```yaml\n# configmap.yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: myapp-config\ndata:\n  LOG_LEVEL: \"info\"\n  CACHE_TTL: \"3600\"\n  API_VERSION: \"v1\"\n\n---\n# secret.yaml\napiVersion: v1\nkind: Secret\nmetadata:\n  name: myapp-secrets\ntype: Opaque\nstringData:                    # Plain text (K8s encodes to base64)\n  database-url: \"postgresql://user:pass@postgres:5432/mydb\"\n  jwt-secret: \"super-secret-key\"\n  redis-url: \"redis://redis:6379\"\n```\n\n### Ingress (HTTP Routing)\n\n```yaml\n# ingress.yaml\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: myapp-ingress\n  annotations:\n    nginx.ingress.kubernetes.io/ssl-redirect: \"true\"\n    cert-manager.io/cluster-issuer: \"letsencrypt-prod\"\nspec:\n  ingressClassName: nginx\n  tls:\n    - hosts:\n        - api.myapp.com\n      secretName: myapp-tls\n  rules:\n    - host: api.myapp.com\n      http:\n        paths:\n          - path: /\n            pathType: Prefix\n            backend:\n              service:\n                name: myapp-api\n                port:\n                  number: 80\n```\n\n### Horizontal Pod Autoscaler\n\n```yaml\n# hpa.yaml\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: myapp-api-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: myapp-api\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target:\n          type: Utilization\n          averageUtilization: 70\n    - type: Resource\n      resource:\n        name: memory\n        target:\n          type: Utilization\n          averageUtilization: 80\n```\n\n## Essential kubectl Commands\n\n```bash\n# Cluster info\nkubectl cluster-info\nkubectl get nodes\n\n# Deployments\nkubectl apply -f deployment.yaml\nkubectl get deployments\nkubectl describe deployment myapp-api\nkubectl rollout status deployment/myapp-api\nkubectl rollout undo deployment/myapp-api        # Rollback\nkubectl scale deployment myapp-api --replicas=5\n\n# Pods\nkubectl get pods\nkubectl get pods -o wide                         # Show node and IP\nkubectl describe pod myapp-api-abc123\nkubectl logs myapp-api-abc123\nkubectl logs myapp-api-abc123 -f                 # Follow\nkubectl exec -it myapp-api-abc123 -- sh          # Shell into pod\n\n# Services\nkubectl get services\nkubectl describe service myapp-api\n\n# Apply all manifests in a directory\nkubectl apply -f k8s/\n\n# Delete resources\nkubectl delete -f deployment.yaml\nkubectl delete pod myapp-api-abc123              # Pod will be recreated\n```\n\n## Local Development with Kubernetes\n\n```bash\n# Minikube (local single-node cluster)\nminikube start\nminikube dashboard            # Web UI\nminikube service myapp-api    # Open service in browser\n\n# Kind (Kubernetes in Docker)\nkind create cluster --name myapp\nkind load docker-image myapp:latest --name myapp\n```\n\n## Common Pitfalls\n\n1. **Not setting resource requests and limits** - Without limits, a single pod can consume all node resources. Without requests, the scheduler cannot make good placement decisions.\n2. **Missing health checks** - Without liveness and readiness probes, K8s cannot detect unhealthy pods or know when a pod is ready for traffic.\n3. **Storing secrets in ConfigMaps** - ConfigMaps are not encrypted. Use Secrets (still base64, but can be encrypted at rest) or external secret managers.\n4. **Not using namespaces** - Without namespaces, all resources are in 'default'. Use namespaces to isolate environments (dev, staging, production).\n5. **Over-engineering** - Kubernetes adds complexity. If you have 1-3 services, PM2 + Nginx or Docker Compose may be simpler and more appropriate.\n",
    "intuition": "Kubernetes is like a fleet management system for shipping containers. You tell it 'I need 3 copies of my API running at all times' (Deployment), and K8s handles the rest: placing containers on ships (nodes), replacing ones that fall overboard (pod restart), balancing cargo (load distribution), and routing delivery trucks to the right dock (Services/Ingress). You describe the desired state, and Kubernetes continuously works to match reality to your description.",
    "exercises": [
      {
        "title": "Deploy Node.js to Minikube",
        "description": "Set up a local Kubernetes cluster with Minikube. Create a Deployment for a Node.js application with 3 replicas, a ClusterIP Service, and an Ingress. Verify you can access the application through the Ingress.",
        "hint": "Start with 'minikube start'. Enable the ingress addon: 'minikube addons enable ingress'. Build your Docker image inside Minikube: 'eval $(minikube docker-env) && docker build -t myapp:latest .'"
      },
      {
        "title": "Rolling Update and Rollback",
        "description": "Deploy version 1.0 of your application, then update to version 2.0 using a rolling update strategy. Monitor the rollout with kubectl rollout status. Then simulate a bad deployment and practice rolling back.",
        "hint": "Update the image tag in the deployment: 'kubectl set image deployment/myapp api=myapp:2.0'. Rollback with: 'kubectl rollout undo deployment/myapp'. View history: 'kubectl rollout history deployment/myapp'."
      },
      {
        "title": "Complete Application Stack",
        "description": "Create Kubernetes manifests for a full application: Node.js API Deployment + Service, PostgreSQL StatefulSet + Service, Redis Deployment + Service, ConfigMap for app config, Secrets for database credentials, and an HPA for the API.",
        "hint": "Use StatefulSet for PostgreSQL (needs stable storage). Use PersistentVolumeClaim for database storage. Use envFrom to inject ConfigMap and Secret into pods."
      }
    ],
    "problems": [
      {
        "problem": "Pod stuck in CrashLoopBackOff",
        "cause": "The container starts and then crashes repeatedly. Usually a misconfiguration, missing environment variable, or application error.",
        "solution": "Check logs: `kubectl logs pod-name --previous` (shows logs from the crashed container). Check events: `kubectl describe pod pod-name`. Common causes: wrong CMD/ENTRYPOINT, missing env vars, failed database connection. Fix the issue and redeploy."
      },
      {
        "problem": "Pod stuck in Pending state",
        "cause": "Kubernetes cannot schedule the pod. Usually insufficient resources (CPU/memory) or no matching nodes.",
        "solution": "Check events: `kubectl describe pod pod-name`. Look for 'Insufficient cpu' or 'Insufficient memory'. Reduce resource requests, add nodes, or use cluster autoscaler. Check node taints and pod tolerations."
      },
      {
        "problem": "Service cannot reach pods (connection refused)",
        "cause": "The service selector does not match the pod labels, or the targetPort does not match the container port.",
        "solution": "Verify labels match: `kubectl get pods --show-labels`. Check the service endpoints: `kubectl get endpoints service-name`. If endpoints are empty, the selector is wrong. Verify targetPort matches the container's port."
      }
    ],
    "further_reading": [
      {
        "title": "Kubernetes Documentation",
        "url": "https://kubernetes.io/docs/home/",
        "type": "documentation"
      },
      {
        "title": "Kubernetes the Hard Way",
        "url": "https://github.com/kelseyhightower/kubernetes-the-hard-way",
        "type": "tutorial",
        "note": "Deep dive into K8s internals by setting it up manually"
      },
      {
        "title": "Learn Kubernetes Basics",
        "url": "https://kubernetes.io/docs/tutorials/kubernetes-basics/",
        "type": "tutorial"
      },
      {
        "title": "kubectl Cheat Sheet",
        "url": "https://kubernetes.io/docs/reference/kubectl/cheatsheet/",
        "type": "reference"
      }
    ]
  },
  "6.6.3": {
    "body": "# Docker Swarm vs Kubernetes: Choosing an Orchestrator\n\n## Why Container Orchestration?\n\nWhen you move beyond a single server running Docker Compose, you need an orchestrator to manage containers across multiple machines, handle failover, scaling, networking, and updates. The two main options are Docker Swarm (simpler) and Kubernetes (more powerful).\n\n## Comparison at a Glance\n\n```\nFeature              Docker Swarm              Kubernetes\n──────────────────────────────────────────────────────────────\nComplexity           Low                       High\nLearning curve       Hours                     Weeks/Months\nSetup                docker swarm init         kubeadm/EKS/GKE\nScaling              Good                      Excellent\nAuto-scaling         Manual                    Built-in (HPA)\nService discovery    Built-in DNS              Built-in DNS + Ingress\nLoad balancing       Built-in (round-robin)    Multiple options\nRolling updates      Yes                       Yes (more options)\nRollbacks            Yes                       Yes (automatic)\nSecrets management   Built-in                  Built-in + integrations\nMonitoring           Limited                   Prometheus ecosystem\nCommunity            Smaller                   Massive\nJob market           Limited                   Very large\nCloud managed        No                        EKS, GKE, AKS\nBest for             Small-medium apps         Large-scale apps\n```\n\n## Docker Swarm\n\n### Setting Up a Swarm\n\n```bash\n# Initialize swarm on manager node\ndocker swarm init --advertise-addr 192.168.1.100\n\n# Output gives you a join token for workers:\n# docker swarm join --token SWMTKN-1-xxx 192.168.1.100:2377\n\n# On worker nodes:\ndocker swarm join --token SWMTKN-1-xxx 192.168.1.100:2377\n\n# List nodes\ndocker node ls\n```\n\n### Deploying Services\n\n```bash\n# Create a service\ndocker service create \\\n  --name myapp-api \\\n  --replicas 3 \\\n  --publish 3000:3000 \\\n  --env NODE_ENV=production \\\n  --update-delay 10s \\\n  --update-parallelism 1 \\\n  myorg/myapp:1.0.0\n\n# Scale\ndocker service scale myapp-api=5\n\n# Update (rolling)\ndocker service update \\\n  --image myorg/myapp:2.0.0 \\\n  --update-delay 10s \\\n  --update-failure-action rollback \\\n  myapp-api\n\n# Rollback\ndocker service rollback myapp-api\n\n# View services\ndocker service ls\ndocker service ps myapp-api\ndocker service logs myapp-api -f\n```\n\n### Docker Stack (Swarm's Compose)\n\n```yaml\n# docker-stack.yml\nversion: '3.8'\n\nservices:\n  api:\n    image: myorg/myapp:1.0.0\n    deploy:\n      replicas: 3\n      update_config:\n        parallelism: 1\n        delay: 10s\n        failure_action: rollback\n      rollback_config:\n        parallelism: 1\n        delay: 5s\n      restart_policy:\n        condition: on-failure\n        delay: 5s\n        max_attempts: 3\n      resources:\n        limits:\n          cpus: '0.5'\n          memory: 512M\n        reservations:\n          cpus: '0.25'\n          memory: 256M\n      placement:\n        constraints:\n          - node.role == worker\n    ports:\n      - \"3000:3000\"\n    environment:\n      NODE_ENV: production\n      DATABASE_URL: postgresql://user:pass@postgres:5432/mydb\n    networks:\n      - app-network\n    secrets:\n      - db-password\n      - jwt-secret\n\n  postgres:\n    image: postgres:16-alpine\n    deploy:\n      replicas: 1\n      placement:\n        constraints:\n          - node.role == manager\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n    environment:\n      POSTGRES_PASSWORD_FILE: /run/secrets/db-password\n    networks:\n      - app-network\n    secrets:\n      - db-password\n\n  redis:\n    image: redis:7-alpine\n    deploy:\n      replicas: 1\n    command: redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru\n    networks:\n      - app-network\n\n  nginx:\n    image: nginx:1.25-alpine\n    deploy:\n      replicas: 2\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./nginx.conf:/etc/nginx/nginx.conf:ro\n    networks:\n      - app-network\n\nnetworks:\n  app-network:\n    driver: overlay              # Overlay network for multi-node\n\nvolumes:\n  pgdata:\n\nsecrets:\n  db-password:\n    external: true\n  jwt-secret:\n    external: true\n```\n\n```bash\n# Create secrets\necho \"super-secret\" | docker secret create db-password -\necho \"jwt-secret-key\" | docker secret create jwt-secret -\n\n# Deploy stack\ndocker stack deploy -c docker-stack.yml myapp\n\n# List stacks\ndocker stack ls\n\n# List stack services\ndocker stack services myapp\n\n# Remove stack\ndocker stack rm myapp\n```\n\n## Decision Framework\n\n### Choose Docker Swarm When:\n\n```\n1. Small to medium team (2-10 developers)\n2. < 20 services to orchestrate\n3. Already using Docker Compose (easy migration)\n4. Need a quick setup (minutes, not days)\n5. Limited DevOps resources\n6. Single-region deployment\n7. Simple scaling needs\n```\n\n### Choose Kubernetes When:\n\n```\n1. Large team or multiple teams\n2. 20+ microservices\n3. Need auto-scaling (HPA, VPA, cluster autoscaler)\n4. Multi-region / multi-cloud deployment\n5. Advanced networking (service mesh, network policies)\n6. Complex deployment strategies (canary, A/B testing)\n7. Rich ecosystem needed (Helm, Operators, CRDs)\n8. Hiring (K8s skills are more marketable)\n```\n\n### Choose Neither When:\n\n```\n1. Single server is sufficient\n2. < 5 containers\n3. PM2 + Nginx handles your needs\n4. Docker Compose on a single host works fine\n5. Using a PaaS (Heroku, Railway, Render)\n6. Serverless (Lambda, Cloud Functions) fits better\n```\n\n## Migration Path\n\n```\nStart Simple                     Scale Up\n─────────────────────────────────────────────────────►\n\nPM2 + Nginx    Docker Compose    Docker Swarm    Kubernetes\n(1 server)     (1 server)       (3-5 servers)   (5+ servers)\n                                                  \nSingle Node.js  Multi-container  Multi-node       Enterprise\nprocess         single host      cluster          orchestration\n```\n\n### From Docker Compose to Swarm\n\n```bash\n# Your docker-compose.yml works with minor additions:\n# 1. Add 'deploy' sections\n# 2. Change network driver to 'overlay'\n# 3. Use 'docker stack deploy' instead of 'docker compose up'\n\ndocker stack deploy -c docker-compose.yml myapp\n```\n\n### From Swarm to Kubernetes\n\n```\nSwarm Concept          Kubernetes Equivalent\n──────────────────────────────────────────\nService                Deployment + Service\nStack                  Namespace + multiple resources\ndocker-stack.yml       Multiple YAML manifests (or Helm chart)\nOverlay network        Pod networking (CNI)\nSwarm secrets          Kubernetes Secrets\nConfig                 ConfigMap\nNode constraints       Node affinity / tolerations\nReplicas               Replica count in Deployment\n```\n\n## Real-World Architecture Comparison\n\n### Swarm Setup for a Node.js App\n\n```\n┌───────────────────────────────────────────┐\n│              Docker Swarm Cluster          │\n│                                           │\n│  Manager Node                             │\n│  ┌─────────┐ ┌─────────┐ ┌─────────┐   │\n│  │ Nginx   │ │ Postgres│ │ Redis   │   │\n│  │ (2 rep) │ │ (1 rep) │ │ (1 rep) │   │\n│  └─────────┘ └─────────┘ └─────────┘   │\n│                                           │\n│  Worker Node 1          Worker Node 2     │\n│  ┌─────────┐           ┌─────────┐      │\n│  │ API     │           │ API     │      │\n│  │ (rep 1) │           │ (rep 2) │      │\n│  └─────────┘           └─────────┘      │\n│  ┌─────────┐           ┌─────────┐      │\n│  │ API     │           │ Worker  │      │\n│  │ (rep 3) │           │ (rep 1) │      │\n│  └─────────┘           └─────────┘      │\n│                                           │\n│  Overlay Network (all connected)          │\n└───────────────────────────────────────────┘\n```\n\n### Kubernetes Setup for the Same App\n\n```\n┌───────────────────────────────────────────────┐\n│           Kubernetes Cluster                   │\n│                                                │\n│  Namespace: myapp-production                   │\n│  ┌──────────────────────────────────────────┐ │\n│  │ Ingress Controller (nginx-ingress)       │ │\n│  │ ┌────────┐                               │ │\n│  │ │ Ingress│──► Service (myapp-api)        │ │\n│  │ └────────┘   ┌──────────────────────┐    │ │\n│  │              │ Deployment (3 pods)   │    │ │\n│  │              │ ┌─────┐┌─────┐┌─────┐│    │ │\n│  │              │ │Pod 1││Pod 2││Pod 3││    │ │\n│  │              │ └─────┘└─────┘└─────┘│    │ │\n│  │              └──────────────────────┘    │ │\n│  │                                          │ │\n│  │  StatefulSet (postgres, 1 pod + PVC)     │ │\n│  │  Deployment (redis, 1 pod)               │ │\n│  │  HPA (auto-scale API 2-10 pods)          │ │\n│  │  ConfigMap + Secrets                      │ │\n│  └──────────────────────────────────────────┘ │\n└───────────────────────────────────────────────┘\n```\n\n## Common Pitfalls\n\n1. **Starting with Kubernetes when Swarm would suffice** - K8s is powerful but complex. If Swarm meets your needs, use it. You can always migrate later.\n2. **Running stateful services (databases) in orchestrators** - Databases prefer stable hosts. Use managed services (RDS, Cloud SQL) unless you have expertise with StatefulSets.\n3. **Not considering managed K8s** - Self-managing a K8s control plane is hard. Use EKS (AWS), GKE (GCP), or AKS (Azure).\n4. **Ignoring the operational cost** - Kubernetes requires ongoing maintenance: upgrades, security patches, monitoring, and an on-call team.\n5. **Resume-driven development** - Choosing K8s because it looks good on a resume rather than because the project needs it.\n",
    "intuition": "Docker Swarm is like a family-run restaurant chain with 3-5 locations. The owner (manager node) knows every employee, every dish, and can coordinate everything easily. Kubernetes is like a multinational fast-food corporation with hundreds of locations, regional managers, automated supply chains, and complex logistics. Both serve food (run containers), but the level of complexity and capability is vastly different. Pick the one that matches the scale of your business.",
    "exercises": [
      {
        "title": "Docker Swarm Deployment",
        "description": "Set up a 3-node Docker Swarm cluster (1 manager, 2 workers) using Docker Machine or VMs. Deploy a Node.js application stack with PostgreSQL and Redis. Test rolling updates and rollbacks. Verify that the application survives a worker node going down.",
        "hint": "Use 'docker swarm init' on the manager. Join workers with the token. Deploy with 'docker stack deploy'. Simulate failure by stopping Docker on a worker node."
      },
      {
        "title": "Swarm to K8s Migration Plan",
        "description": "Take a docker-stack.yml file and convert it to equivalent Kubernetes manifests: Deployment, Service, ConfigMap, Secret, and Ingress. Document the mapping between Swarm and K8s concepts.",
        "hint": "Each Swarm service becomes a K8s Deployment + Service pair. Stack secrets become K8s Secrets. Deploy config maps for environment variables. Overlay network is handled by K8s CNI automatically."
      },
      {
        "title": "Architecture Decision Document",
        "description": "Write a decision document for a hypothetical project: 3 microservices, 2 developers, expecting 1000 requests/day initially, growing to 100,000/day in a year. Evaluate: Docker Compose, Docker Swarm, and Kubernetes. Recommend a starting point and migration path.",
        "hint": "Consider team size, operational complexity, scaling needs, and timeline. Start simple (Compose or Swarm) and plan migration triggers (when to move to K8s)."
      }
    ],
    "problems": [
      {
        "problem": "Docker Swarm services not reachable across nodes",
        "cause": "The overlay network is not properly configured, or ports required for Swarm communication are blocked by the firewall.",
        "solution": "Ensure these ports are open between all Swarm nodes: TCP 2377 (cluster management), TCP/UDP 7946 (node communication), UDP 4789 (overlay network). Check: `docker network ls` for overlay networks. Verify services are on the same network."
      },
      {
        "problem": "Kubernetes cluster is too expensive",
        "cause": "Running K8s control plane nodes + worker nodes + managed service fees adds up, especially for small applications.",
        "solution": "Consider alternatives: Use a managed K8s with smaller node pools, use spot/preemptible instances for workers, right-size your nodes, or switch to Docker Swarm or a PaaS for cost-sensitive workloads. EKS alone costs ~$72/month for the control plane."
      },
      {
        "problem": "Cannot decide between Swarm and Kubernetes",
        "cause": "Analysis paralysis from trying to pick the 'right' tool before understanding the actual requirements.",
        "solution": "Start with the simplest option that works (often Docker Compose on a single server). Move to Swarm when you need multi-node. Move to Kubernetes when you need auto-scaling, complex networking, or a rich ecosystem. The migration path from Compose -> Swarm is very smooth."
      }
    ],
    "further_reading": [
      {
        "title": "Docker Swarm Documentation",
        "url": "https://docs.docker.com/engine/swarm/",
        "type": "documentation"
      },
      {
        "title": "Kubernetes vs Docker Swarm - Comparison",
        "url": "https://www.atlassian.com/microservices/microservices-architecture/kubernetes-vs-docker",
        "type": "article"
      },
      {
        "title": "Docker Swarm Tutorial",
        "url": "https://docs.docker.com/engine/swarm/swarm-tutorial/",
        "type": "tutorial"
      },
      {
        "title": "Helm - Kubernetes Package Manager",
        "url": "https://helm.sh/",
        "type": "tool",
        "note": "Helm charts make it easier to package and deploy K8s applications"
      }
    ]
  }
}